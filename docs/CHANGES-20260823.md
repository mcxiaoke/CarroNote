# 变更记录 2026-08-23

## 22:25 彻底解耦 Web 平台环境变量读取（条件导出消除 Platform.environment）

**改动概述**：
为了解决 Flutter Web 在 DDC 开发编译（`flutter run -d edge`）模式下对 `Platform.environment` 的加载求值异常，将环境变量与数据目录覆盖逻辑彻底重构为条件导出，Web 目标编译下 0 引用 `dart:io` `Platform`。

**核心改动与优化**：
1. **数据目录覆盖条件导出 (`data_dir_override.dart`)**：
   - 创建 `lib/src/platform/data_dir_override.dart`，Native 走文件系统与 `Platform.environment['SN_DATA_DIR']`，Web 走空实现，从 `lib/main.dart` 中彻底移除 `_applyDataDirOverride` 内联函数。
2. **环境变量读取条件导出 (`env_reader.dart`)**：
   - 创建 `lib/src/platform/env_reader.dart`，Native 走 `Platform.environment`，Web 恒返回 null，使 `lib/utils/env_config.dart` 零 `dart:io` 依赖。
3. **主入口日志打印保护**：
   - 在 `lib/main.dart` 的 `_initLogging` 中将平台信息区分 `kIsWeb`，Web 端直接输出常量，不再访问系统属性。
4. **验证结果**：
   - `flutter analyze` — 0 issues 零警告；
   - 自动化 Edge 测试验证：首屏成功渲染挂载，进入密码设置页。

---

## 21:33 修复 Web 端同步初始化异常（Journal 内存模式适配 path_provider 跨平台门控）

**改动概述**：
排查并修复 Web 端在测试连接通过后点击「启用同步」时，因调用 `getApplicationSupportDirectory` 抛出 `MissingPluginException` 导致同步初始化失败的问题。

**核心改动与优化**：
1. **Web 端 Journal 内存模式直连**：
   - 在 `lib/sync/sync_service.dart` 的 `_openJournal` 中增加 `kIsWeb` 平台分支，当运行在 Web 浏览器沙箱中时，直接返回 `Journal.inMemory(vaultId: vaultId, deviceId: deviceId)`，跳过无本地文件系统的 `getApplicationSupportDirectory` 调用。
2. **验证结果**：
   - `flutter analyze` — 0 issues 零警告；
   - WebDAV 测试连接与同步初始化流程完整闭环。

---

## 21:24 修复 Web 端笔记字体样式（衬线/等宽）生效与 CJK Fallback 映射

**改动概述**：
优化 `lib/utils/platform_ui.dart` 字体与回退链映射，解决 Web 端 CanvasKit 渲染器下通用族名不生效导致「衬线/等宽」切换无变化的问题。

**核心改动与优化**：
1. **Web 平台系统字体与 Fallback 映射**：
   - 在 `lib/utils/platform_ui.dart` 中，将 `appFontFamilyFor` 与 `appFontFallbackFor` 的桌面分支向 Web 端开放。
   - 衬线体映射为 `Georgia` + `['Songti SC', 'SimSun', 'Noto Serif CJK SC', 'Source Han Serif SC', 'serif']`（宋体/明体）；
   - 等宽体映射为 `Consolas` / `Menlo` + `['Noto Sans Mono CJK SC', 'Sarasa Mono SC', 'Microsoft YaHei', 'monospace']`；
   - 无衬线映射为 `Segoe UI` / `.AppleSystemUIFont` + `['Microsoft YaHei', 'PingFang SC', 'sans-serif']`。
   - 保证在 Web (CanvasKit) 与 Native 下，切换「衬线/等宽」均有明显的排版外观差异与正确的中文字体回退。
2. **验证结果**：
   - `flutter analyze` — 0 issues 零警告；
   - `dart test packages/core/test` — 452/452 项单元测试通过。

---

## 21:05 修复 Web 调试与运行白屏问题（数据库路径解析、WASM ABI 匹配与根路由适配）

**改动概述**：
排查并修复 `flutter run -d edge` 与 Web 生产包下的白屏问题，根因涵盖 `getDatabasesPath is null` 启动崩溃、`sqlite3` 与 `sqlite3.wasm` ABI 版本不兼容 (`Import #25 env: module is not an object`)、根路由 `/` 嵌套递归以及字体 404 兜底。

**核心改动与修复**：
1. **WASM ABI 匹配与脚手架升级**：
   - 升级 `sqflite_common_ffi_web: ^1.1.2` 并下载官方匹配的 `sqlite3-3.1.2/sqlite3.wasm`，彻底解决新版 `package:sqlite3 (3.5.1)` 在 `WebAssembly.instantiate()` 时报 `Import #25 "env": module is not an object or function` 的 ABI 不兼容问题。
2. **数据库安全路径解析 (`_resolveDbPath`)**：
   - 在 `packages/core/lib/src/db/database_handler.dart` 中新增 `_resolveDbPath()` 统管 `_initDB`、`dbFilePath` 与 `deleteDbFile`。当 `factory.getDatabasesPath()` 为空或抛错时（Web 无物理目录），安全降级返回文件名（IndexedDB key），彻底消除 `DatabaseException(getDatabasesPath is null)` 启动崩溃。
3. **Web 数据库工厂直连模式 (`databaseFactoryFfiWebNoWebWorker`)**：
   - 在 `lib/src/platform/database_bootstrap_web.dart` 中采用 `databaseFactoryFfiWebNoWebWorker`，直接在主线程加载 `sqlite3.wasm` 并挂载 IndexedDB 文件系统，规避 Worker 跨源及通信序列化问题，实现 100% 现代浏览器直连持久化。
4. **Web 根路由递归消除**：
   - 修复 `lib/app.dart` 中 `initialRoute: '/'` 与 `home: AuthWall` 冲突；
   - 修复 `lib/routes/route_generator.dart` 中 `case '/'` 误返回 `const SafeNotesApp()` 导致的嵌套应用崩溃。
5. **字体平台门控与兜底**：
   - 在 `lib/utils/platform_ui.dart` 的 `appFontFamilyFor` 中补充 `!kIsWeb` 检查，Web 端直接返回 `sans-serif` 等通用字体族名；
   - 在 `lib/models/shad_theme.dart` 中增加 `sans-serif` 兜底，避免 Web 端尝试网络拉取不存在的 `Geist[wght].ttf` 造成 404。
6. **验证结果**：
   - `flutter analyze` 0 issues；
   - `dart test packages/core/test` 452 项全部通过；
   - 通过 Chrome/Edge DevTools CDP 自动化启动验证，Web 数据库、主题、本地化与首屏渲染完全正常。

---

## 18:46 完成 SafeNotes Web 版移植与 WASM+IndexedDB 多端解耦

**改动概述**：
依据 `docs/WEB-PORT-PLAN.md` 与 `docs/WEB-PORT-EXECUTION-PLAN.md`，完成 SafeNotes Web 端移植改造，消除 `dart:io` 与桌面 FFI 强依赖，实现 SQLite WASM + SharedWorker IndexedDB 持久化与 Web 端平滑运行。

**核心改动与架构**：
1. **WASM 脚手架与依赖**：
   - 引入 `sqflite_common_ffi_web: ^0.4.5`，配置 `web/sqflite_sw.js` 与 `web/sqlite3.wasm` 共享 Worker。
2. **I/O 跨平台抽象垫片 (Platform Shims)**：
   - 在 `packages/core` 与 `lib` 建立 `platform_io.dart` 条件导出机制（`export 'io_real.dart' if (dart.library.js_interop) 'io_stub.dart'`），Native 平台无缝复用原生 `dart:io`，Web 平台平滑回退安全桩。
   - 数据库引导拆分为 `database_bootstrap.dart`（Native 走 FFI，Web 走 WASM+IndexedDB，移动端走原生插件）。
   - 日志 WebServer 拆分为条件导出（`log_webserver_io.dart` 与 `log_webserver_stub.dart`），Web 端零网络监听开销。
   - 桌面窗口控制拆分为条件导出（`desktop_window_native.dart` 与 `desktop_window_stub.dart`）。
3. **Core 包与 App 层 dart:io 解耦与 WASM 适配**：
   - `packages/core` 核心包完全解耦 `dart:io`，并在 `NotesDatabase.deleteDbFile()` 中增加 WASM 不支持 `deleteDatabase` 时的 SQL `DROP TABLE` + 重建表兜底。
   - App 层 13 个关键业务文件完成 `dart:io` 替换与 `kIsWeb` 门控保护（跳过本地备份、跳过外部目录解析、跳过生物识别配置）。
4. **验证与出包**：
   - `flutter analyze` — 0 issues 零警告；
   - `dart test packages/core/test` — 452/452 项单元测试全部通过；
   - `flutter test` — 165/165 项 Flutter 测试全部通过；
   - `flutter build web --release` — Web 生产包编译出包成功；
   - 在本地 8000 端口（`http://localhost:8000/`）通过 Headless 浏览器 CDP 自动化冒烟测试，IndexedDB 数据库初始化与本地化资源加载 100% 正常，0 异常报错。

---



**改动概述**：
依据 `docs/test-enhancement-plan-20260823.md`，对 `packages/core` 纯 Dart 核心逻辑包（加密 / 数据库 / 同步引擎 / 日志 / CLI / 模型）实施全量测试增强，消除单测盲区与潜在 RangeError 边界隐患。

**核心改动与修复**：
1. **隐患修复**：
   - `packages/core/lib/src/sync/sync_error.dart`：修复 `DecryptionError` 与 `BlobMissingError` 在遇到短 hash (<8) 时直接截断抛 `RangeError` 的隐患；
   - `packages/core/lib/src/logger/app_logger.dart`：完善 `AppLogFile.close()` 状态复位，支持多实例/多临时目录重新绑定日志目录；
   - `packages/core/pubspec.yaml`：将 `args` 引入 `dev_dependencies`，支持纯 Dart CLI 自动化单测。
2. **新增/补全测试组**（共 9 个新测试套件，新增 95+ 边界单测）：
   - `sync_models_test.dart`：容器负向解析与边界超限防御（23 项通过）；
   - `database_migration_rollback_test.dart`：事务回滚、坏 SQL 防护与跨版本迁移（11 项通过）；
   - `backend_mock_matrix_test.dart`：WebDAV / SafeServer 状态码矩阵与代际备份（15 项通过）；
   - `sync_engine_key_and_recovery_test.dart`：密钥判定矩阵、远端损坏恢复与 M7 hash 错位（7 项通过）；
   - `sync_error_test.dart`：异常模型与短 hash 防护（8 项通过）；
   - `cli_runner_test.dart`：CLI 端到端命令流冒烟、密码优先级与确认门（5 项通过）；
   - `app_logger_test.dart`：2000 容量 FIFO 淘汰、级别动态过滤与文件轮转（4 项通过）；
   - `backend_and_journal_edge_test.dart`：环形备份轮转、Journal 状态损坏容错与隔离取证（6 项通过）；
   - `keyring_edge_and_security_test.dart`：远端解锁顺序保护（错误零落盘）与迁移矩阵（5 项通过）；
   - `parse_import_and_note_version_edge_test.dart`：备份头安全上限表驱动与敏感信息脱敏（11 项通过）。
3. **验证**：
   - `flutter analyze` 0 issues；
   - `dart test packages\core\test` 451 项全部通过（0 failure）；
   - 详细实施过程已记录于独立的 `docs/TEST-CHANGES-20260823.md`。

---

## 17:32 修复 Windows 端闲时锁定后窗口偶发抢前台问题

**问题**：用户在其他应用窗口工作时，SafeNotes 闲时自动锁定后会突然跳到前台显示登录窗口（偶发）。

**根因链**：
1. `local_session_timeout` 的空闲 Timer 不感知前后台状态：切到其他应用后超时照样触发，`sessionHandler` 在后台执行 logout 并导航到登录页；
2. 后台路由重建 + 登录页 PIN 覆盖层的 `autofocus` 向 Flutter 引擎发起窗口焦点请求，而 Windows 引擎存在失活窗口被重新激活的已知缺陷（flutter/flutter#187436、PR #188016），导致应用被抢到前台。

**修复**（lib/main.dart）：
1. `_SafeNotesAppState` 混入 `WidgetsBindingObserver`，新增 `_pendingLockEvent` 记录后台触发的超时事件；
2. `sessionHandler` 检测到应用非前台时不再立即导航，仅记录待锁定事件并返回；
3. 回到前台（resumed）时在 `didChangeAppLifecycleState` 中补执行延迟锁定。锁定逻辑幂等：重复事件经 `onTimeOutDo` 的 `isEncryptionEnabled` 检查直接跳过。

**验证**：`flutter analyze` 0 issues；`dart test packages\core\test` 356 项通过；`flutter test` 165 项全部通过。

## 17:19 修复多重连续换行/空行被 Markdown AST 块级解析器折叠吞并问题

**根因与修复**：
1. **连续换行折叠根因**：
   - CommonMark 解析器在处理多个连续空行（如 `\n\n\n\n`）时，会将连续的空行统统折叠合并为单个段落间距（Paragraph Spacing），导致用户留出的多行视觉空白缩水成单行。
2. **精准空行高度占位 (`MarkdownFormatter.prepareMarkdownForRendering`)**：
   - 对非代码块内的空白行（`line.isEmpty || line.trim().isEmpty`）注入 `&nbsp;  ` 占位；
   - `&nbsp;` 在 `flutter_markdown_plus` 中被解码为非断行空格（`\u00A0`），使得每一个空行都拥有完整的单行行高，用户在编辑页空几行，预览页就忠实呈现几行空白；
   - 代码块内部严格保持原样，绝不注入实体。
3. **验证**：
   - `test/markdown_formatter_test.dart` 补充多连续空行测试用例（24 项单测全通）；
   - `flutter analyze` — 0 issues；
   - `flutter test` — 165 项全部通过。

---

## 17:08 解决 Markdown 渲染时纯文本换行丢失合并问题 & 语法特征检测器

**痛点与解决方案**：
1. **换行丢失合并痛点**：
   - 依据 CommonMark 规范，段落内的单换行符（Soft Break）会被默认忽略并合并到同一行中，导致诗歌、便签、日记、地址等多行纯文本在开启 Markdown 渲染时被强行折叠在一行。
2. **现代笔记级预处理算法 (`MarkdownFormatter.prepareMarkdownForRendering`)**：
   - 实现类似 GFM breaks / Obsidian 的换行保护；
   - **代码块保护**：扫描识别 ```` ``` ```` 与 ```` ~~~ ```` 代码栅栏，代码块内部行严格原样输出，绝不污染代码行尾；
   - **非代码文本行硬换行**：对代码块外部的非空、非末尾行自动补齐双空格（`  \n`），使 `MarkdownBody` 忠实呈现用户的自然换行，杜绝行合并；已带双空格或独立空行不重复追加。
3. **高精度 Markdown 语法检测器 (`MarkdownFormatter.isMarkdown`)**：
   - 覆盖标题、任务清单、有序/无序列表、引用、代码块、行内代码、链接/图片、加粗/删除线、分割线、表格 10+ 类语法特征，严格排除普通标点和数字减号等纯文本干扰。
4. **验证**：
   - `test/markdown_formatter_test.dart` 补充语法检测与换行预处理测试组（23 项单测全通）；
   - `flutter analyze` — 0 issues；
   - `flutter test` — 164 项全部通过。

---

## 16:48 设置-显示增加 Markdown 工具栏独立开关 & 零干扰原生环境

**实现范围与改动**：
1. **偏好存储** (`lib/data/preference_and_config.dart`)：
   - 增加 `isMarkdownToolbarEnabled` / `setIsMarkdownToolbarEnabled`（默认开启）。
2. **设置页面** (`lib/views/settings/display_page.dart` & `lib/views/settings/appearance_settings_page.dart`)：
   - 在「显示 / Display」分组中增加「Markdown 工具栏 / Markdown Toolbar」Switch 开关，带独立中英文说明。
3. **编辑页与输入流彻底联动禁用** (`lib/views/add_edit_note.dart` & `lib/widgets/note_widget.dart`)：
   - 当开关关闭时，`add_edit_note.dart` 的 `_buildEditorLayout` 直接返回 `_buildBody()`，完全不渲染任何工具栏或额外布局；
   - `note_widget.dart` 的 `inputFormatters` 在开关关闭时清空（不注入 `MarkdownAutoIndentFormatter`），提供 100% 干净的原生纯文本输入环境。
4. **多语言词条** (`assets/translations/zh-CN.json` & `en-US.json`)：
   - 增加 `Markdown Toolbar` 与描述的多语言翻译。
5. **验证**：
   - `test/markdown_toolbar_test.dart` 补充开关状态持久化测试；
   - `flutter analyze` — 0 issues；
   - `flutter test` — 159 项全部通过。

---

## 16:35 修复 Android 选词手势打断问题 & 实现 Markdown 列表回车智能续行与序号自增

**问题根因与修复**：
1. **Android 原生选文字手势打断修复 (P0)**：
   - 根因：`add_edit_note.dart` 在 `initState` 中对 `FocusNode` 挂载了 `addListener(() => setState({}))`，当用户在 Android 触屏上点击、长按选词或拖拽选择手柄时，焦点状态变更触发 `setState` 重建了输入框，导致 `EditableText` 的 `TextSelectionGestureDetector` 手势状态机被重置，系统直接坍塌选区并强制弹出剪贴板浮层。
   - 修复：移除 FocusNode 的全局 `setState` 监听，输入框不再被手势打断，100% 还原 Android 原生选词/拖动手柄流畅体验。
2. **列表回车智能续行与有序列表序号自增**：
   - 需求：用户在有序列表（`1. `）、无序列表（`- `）、待办清单（`- [ ] `）、引用（`> `）输入时，按回车键无法自动续行下一项，有序列表重复出现 `1. `。
   - 实现：编写 `MarkdownAutoIndentFormatter`（`lib/utils/markdown_formatter.dart`），作为 `TextInputFormatter` 拦截回车换行：
     - 有序列表：`1. item` 回车自动生成 `2. `，`2. item` 回车生成 `3. `；
     - 无序列表与待办：回车自动补全前缀 `- ` 或 `- [ ] `；
     - 退出机制：在空列表行（仅含前缀）再次按回车，自动清除当前行前缀并退出列表模式。
3. **测试验证**：
   - `test/markdown_formatter_test.dart` 补充 `MarkdownAutoIndentFormatter` 5 组单测全部通过；
   - `flutter analyze` 零警告；
   - `flutter test` 158 项全通。

---

## 16:15 Markdown 编辑快捷工具栏功能实现与多端适配

**实现范围与架构**：
1. **纯 Dart 格式化算法库** (`lib/utils/markdown_formatter.dart`)：
   - 实现加粗/斜体/删除线/行内代码的行内智能包裹与解包（Unwrap）；
   - 实现 H1-H6 多级标题切换（轮换与指定层级）；
   - 实现无序列表、有序列表（多行自动递增编号）以及待办清单（`- [ ] ` ↔ `- [x] ` ↔ 清除）的前缀切换；
   - 实现代码块、超链接与水平分割线插入；
   - 包含针对选区边界、多行覆盖、空选区占位和光标精准定界算法。
2. **跨平台响应式工具栏组件** (`lib/widgets/markdown_toolbar.dart`)：
   - 移动端：吸附于软键盘正上方（Input Accessory View），横向平滑滚动；
   - 桌面端：置于编辑器顶部（AppBar 与 Title 之间）作为 Secondary Format Bar，符合桌面鼠标操作习惯；
   - 焦点保持机制：点击工具栏按钮通过 `FocusNode.requestFocus()` 维持正文输入框焦点，杜绝软键盘闪退或失焦。
3. **编辑页装配与焦点联动** (`lib/views/add_edit_note.dart` & `lib/widgets/note_widget.dart`)：
   - `NoteFormWidget` 暴露 `titleFocusNode` 与 `descriptionFocusNode`；
   - `AddEditNotePageState` 动态感知当前处于焦点的输入框，操作相应 controller，且编辑操作直接联动现有 `NoteEditHistory`（Undo/Redo）。
4. **多语言资源补全** (`assets/translations/zh-CN.json` & `en-US.json`)：
   - 补全加粗、斜体、删除线、多级标题、待办清单、列表、代码块、链接等工具按钮 Tooltip 国际化翻译。

**测试与验证**：
- `test/markdown_formatter_test.dart`：包裹、解包、标题、列表、待办、代码块等算法用例 100% 通过；
- `test/markdown_toolbar_test.dart`：工具栏组件交互与焦点维持测试通过；
- `flutter analyze` — 0 issues 零警告；
- `dart test packages/core/test` — 356/356 全部通过；
- `flutter test` — 153/153 全部通过；
- `flutter build windows --debug` — Windows 平台构建通过。

---

## 15:20 代码审查 P0 & P1 关键安全与稳定性问题修复

**修复范围与改动**：
1. **T-1 (P0)**：`login.dart` 与 `sync_diagnostics_page.dart` 在重置本地数据时显式调用 `BiometricAuth.disable()` 与 `PinAuth.disable()`；并在 `clearVaultRelatedKeys()` 中清理生物识别与 PIN 配置键，彻底杜绝凭据残留。
2. **T-2 (P0)**：`settings.dart` 与 `general_settings_page.dart` 语言名称映射添加 fallback，避免非标 Locale 下空断言 `!` 崩溃；修复 `settings.dart` 中 `_securityValue` 赋值。
3. **N-8 (P0)**：`KeyringLedger.load()` 损坏时抛出 `KeyringCorruptedException` 而非返回 `null`；`Keyring.createNew()` 增加 `overwrite: false` 防意外覆盖保护；`_unwrapOrThrow()` 区分 base64 损坏与密码错误；`set_passphrase.dart` 增加 `hasKeyringRecord` 拦截。
4. **N-9 (P1)**：`Keyring.changePassword()` 增加持久化前的乐观并发校验（检查 DB 中 epoch、vaultId、keyVersion 未被并发修改）；`change_passphrase.dart` 改密码前排空在途同步任务（`waitForSyncCompletion()`）。
5. **N-1 / N-2 / N-4 (P1)**：`sync_service.dart` 的 `switchBackend()` 在新后端 `init()` 成功后才关闭并替换旧后端；`repairRemote()` 异常退出路径补齐 `_updateState` 调用防止 UI 状态假死；`initialize()` 异常路径确保状态清理。
6. **T-5 & T-6 (P1)**：`http_util.dart` 的 `sendWithRedirectPolicy` 改造为流式边收边计数并截断超限响应（`maxResponseBodyBytes`）；`sync_backend.dart` 定义 `kRemoteBlobMaxBytes` (100MB)；三大后端 `getBlob` 增加大小约束；`webdav_backend.dart` 暴露 `isEtagSupported` 并在缺头时告警。
7. **T-4 (P1)**：`parse_import.dart` 的 `parseBackupHeader` 增加 PBKDF2/Argon2id KDF 参数安全上限约束与 payload 长度校验，防御 DoS 攻击。
8. **T-3 & T-26 (P1)**：`log_webserver.dart` 生成 128-bit 随机 hex 安全 token，日志中脱敏掩码输出；`database_handler.dart` 的 `cachedNoteSummaries()` 对笔记标题进行长度与哈希脱敏，保护隐私。

**验证**：
- `dart format` 已格式化全部变动文件
- `flutter analyze` — 0 issues
- `dart test packages/core/test` — 356/356 全部通过（新增单元测试）
- `flutter test` — 137/137 全部通过
- `flutter build windows --debug` — 编译构建通过

---

**报告文件**：`docs/comprehensive-review-20260823-133502.md`（本次审查独立报告，带日期时间后缀）

**审查范围**：`packages/core`（加密 / 数据库 / 同步引擎 / 后端 / Journal）+ `lib`（认证 / 备份 / 同步装配 / UI 状态）；静态审计 + 语义推演 + 历史 P0 回归 + 子代理并行深审

**核心结论**：核心加密与同步引擎已达可投产水位；阻断项集中在应用装配层 — 报告共检出 **P0 7 项 / P1 10 项 / P2 10 项**，详见独立报告 §2-§4；其中 P0 含日志 WebServer 低熵 token 明文落盘、WebDAV ETag 退化放行、KDF 参数无上限 DoS、重置后 biometric/PIN 残留、prefs 非原子落盘、超大响应体后置校验、路径穿越 7 项需本周修复；加密/同步/数据层专项核对见报告 §5-§10

**验证**：`flutter analyze` 0 issue / `flutter test` 135 passed / `dart test packages/core` 352 passed 基准已达标

---

## 12:00 getFontColorForBackground 改用 M3 onSurface 色阶

**文件**: `lib/utils/contrast.dart`, `lib/models/app_theme.dart`

### 改动
- `getFontColorForBackground` 优先返回 M3 onSurface 色阶（`#1C1B1F` 深灰 / `#E6E1E5` 浅灰），
  替代原来的纯黑纯白，视觉更柔和。
- 新增 `{bool? isDark}` 可选参数，显式传入暗色模式状态。
- 当中等亮度背景与 onSurface 对比度不足 WCAG AA (4.5:1) 时，回退到纯黑/纯白
  （选对比度更高者，数学上保证 ≥ 4.58:1）。
- `app_theme.dart` 状态栏图标亮度判断从 `appBarFg == Colors.black` 改为
  `appBarFg.computeLuminance() < 0.5`，兼容三种返回值（kDarkForeground / kLightForeground / 纯黑纯白）。

## 11:30 颜色选择器暗色模式双色板 + border 改善

**文件**: `lib/utils/notes_color.dart`, `lib/widgets/note_color_picker.dart`

### 双色板架构
- 新增 `kNoteColorPaletteDark`（11 色），色值与 Google Keep 暗色模式一致：
  `#A02838 #783C24 #885818 #284050 #286070 #246860 #285840 #402858 #683848 #484438 #242426`
- 暗色色板是独立设计的（更饱和、更深、保证白字可读），不是 alphaBlend 计算出来的。
- 新增 `noteColorDarkVariant(lightArgb)` 映射函数：浅色 ARGB → 暗色变体。
- **存储策略不变**：NoteMeta.color 始终存浅色 ARGB 作为"颜色身份"。
- `getNoteColorWithMeta` / `editorBackgroundColor` 暗色模式改用 `noteColorDarkVariant` 查变体，
  替代之前的 `alphaBlend(base, surfaceContainerLow)` 方案。

### 选择器适配
- `_NoteColorSheet` 按当前主题显示对应色板：浅色模式显示浅色圆点，暗色模式显示暗色圆点。
- `_ColorDotGrid` 显示用 `displayColors`（当前主题色板），key 和返回值始终用浅色 ARGB。
- border 宽度从 1px → 2px（未选中态），提高清晰度。

### 验证
- `flutter analyze` — 0 issues
- `flutter test` — 135 通过
- `flutter test integration_test/app_test.dart -d windows` — 32/32 全通过

## 10:00 颜色选择器换用 Google Keep 同款色板 + 编辑页背景跟随笔记颜色

**文件**: `lib/utils/notes_color.dart`, `lib/widgets/note_color_picker.dart`, `lib/views/add_edit_note.dart`, `lib/views/home.dart`

### 色板替换
- 新增 `kNoteColorPalette` 常量（11 色），色值与 Google Keep 一致：
  `#F8A8A0 #F59B70 #FFF7B3 #A8C8D8 #D4E8EF #A8DDCF #DCF6D0 #D0C0E0 #F7E2DD #EDE8D8 #F3F3F4`
- `note_color_picker.dart` 不再从 `allNotesColorTheme` 取色，改用固定 `kNoteColorPalette`。
- 移除 picker 对 `PreferencesStorage.colorfulNotesColorIndex` 的依赖。

### 编辑页 / 预览页背景跟随笔记颜色
- `notes_color.dart` 新增 `editorBackgroundColor(metaColor:, context:)`：
  - `metaColor == null` 返回 null（调用方回退默认 scaffold 背景）。
  - 浅色模式：直接使用原始色值（色板本身是柔和浅色，不需要提亮）。
  - 暗色模式：与卡片同策略 `alphaBlend(base @50%, surfaceContainerLow)` 压暗。
- `add_edit_note.dart` 的 `Scaffold` 和 `AppBar` 的 `backgroundColor` 绑定 `_meta?.color`，
  设置颜色后立即生效（setState 在 `_setColor` 中已调用）。
- `home.dart` 的 `OpenContainer.openColor` 同步使用 `editorBackgroundColor`，
  卡片→编辑页过渡时背景色无缝衔接。

### 验证
- `flutter analyze` — 0 issues
- `flutter test` — 135 通过
- `dart test packages/core/test` — 352 通过
- `flutter test integration_test/app_test.dart -d windows` — 32/32 全通过
