# 变更日志 - 2026-07-28

## [2026-07-28 19:23:52] - 修复 change_passphrase.dart 的 use_build_context_synchronously 警告

修复改密码保护改动引入的 2 个 info 级 lint 警告，确保 analyzer 输出干净（仅剩既有 use_super_parameters，非本次改动）。

**改动内容**：

### 1. 捕获 navigator 位置前移
- **修改 [lib/views/change_passphrase.dart](file:///c:/Home/Projects/safenotes/lib/views/change_passphrase.dart)**: `final navigator = Navigator.of(context);` 从 `await _preChangeCheck()` 之后移到之前，消除 line 322 的 `use_build_context_synchronously` 警告

### 2. 改用 if (!mounted) return 模式
- **修改 [lib/views/change_passphrase.dart](file:///c:/Home/Projects/safenotes/lib/views/change_passphrase.dart)**: `if (mounted) showSnackBarMessage(scaffoldContext, ...)` 改为 `if (!mounted) return; showSnackBarMessage(context, ...)`，analyzer 识别此模式后消除 line 360 的警告；移除不再需要的 `scaffoldContext` 本地变量

**验证**：
- `flutter analyze` → 0 error / 0 warning（仅剩 1 个既有 info 级 use_super_parameters，line 36 旧代码）
- `flutter test` → 65/65 通过（crypto 31 + vault 35 + sync_engine 10 + multi_device 5，含 H1/M7 多设备场景）
- `flutter build apk --debug` → 成功（15.8s）

## [2026-07-28 19:18:19] - 改密码前置保护 + 后置同步

为改密码流程增加数据安全保护：前置强制备份+同步+服务器在线检测，后置立即同步推送新 encryptedDataKey。

**改动内容**：

### 1. 改密码后立即同步
- **修改 [lib/views/change_passphrase.dart](file:///c:/Home/Projects/safenotes/lib/views/change_passphrase.dart)**: `_finalSublmitChange` 在 vault.changePassword 成功后调用 `SyncService.instance.sync()`，立即把新 encryptedDataKey 推送到远端

### 2. SyncBackend 新增 ping() 方法
- **修改 [lib/sync/sync_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/sync_backend.dart)**: 抽象接口加 `Future<bool> ping()`，轻量级健康检查，不抛异常
- **修改 [lib/sync/safe_server_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/safe_server_backend.dart)**: SafeServer 实现 ping()，GET /api/v2/health
- **修改 [lib/sync/local_fs_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/local_fs_backend.dart)**: LocalFS 实现 ping()，检查根目录存在+写入探测文件
- **修改 [lib/sync/webdav_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/webdav_backend.dart)**: WebDAV 实现 ping()，PROPFIND 深度 0

### 3. ScheduledTask.forceBackup() 强制备份
- **修改 [lib/utils/scheduled_task.dart](file:///c:/Home/Projects/safenotes/lib/utils/scheduled_task.dart)**: 新增 `forceBackup()` 方法，绕过 isBackupOn/isBackupNeeded 开关，返回 bool

### 4. 改密码前置检查 _preChangeCheck()
- **修改 [lib/views/change_passphrase.dart](file:///c:/Home/Projects/safenotes/lib/views/change_passphrase.dart)**: 新增 `_preChangeCheck()` 和 `_showWarningDialog()` 方法
  - 强制本地完整备份（失败警告用户是否继续）
  - 若启用同步：ping 服务器确认在线（不在线警告用户）
  - 若在线：强制 sync 一次 + 检查本地是否 clean（有未同步笔记警告用户）
  - 所有警告对话框可由用户选择"继续改密码"或"取消"

### 5. 测试 FakeBackend 实现 ping()
- **修改 [test/sync/sync_engine_test.dart](file:///c:/Home/Projects/safenotes/test/sync/sync_engine_test.dart)**: FakeBackend 加 `ping() => true`
- **修改 [test/sync/multi_device_test.dart](file:///c:/Home/Projects/safenotes/test/sync/multi_device_test.dart)**: FakeBackend 加 `ping() => true`

**测试结果**：81 个核心测试 + 15 个 SafeServer 集成测试全部通过，flutter analyze 0 error/0 warning，build apk 成功

## [2026-07-28 18:28:18] - H1 修复 + 多设备测试 + SafeServer 集成测试环境修复

修复 H1（远端 encryptedDataKey 回写）场景的关键 bug，完善多设备测试，修复 SafeServer 集成测试环境问题。

**核心修复**：
- **SyncEngine.vault 改为非 final**：迁移后 `_executeMigration` 接收 `migrateToRemote` 返回的新 Vault 实例，避免重试时用旧 dataKey 解密 manifest 失败
- **_syncOnce 迁移逻辑优化**：区分 3 种场景
  1. MK 解不开远端 encryptedDataKey → 用本地 dataKey 尝试解远端 manifest items 判断场景
  2. MK 能解开且 remoteDataKey == 本地 dataKey → 只更新 encryptedDataKey（他端改密码本端用新密码登录）
  3. MK 能解开且 remoteDataKey != 本地 dataKey → 执行 reEncryptAllNotes（新设备加入）
- **删除 1g 死代码**：noMigrationNeeded 时远端 encryptedDataKey 必等于本地，回写条件永不满足
- **新增 `_bytesEqual`**：常数时间比较 dataKey，避免时序攻击

**SafeServer 集成测试环境修复**：
- `startGoServer` 改为在 `server/go/` 目录下运行 `go run .`（workingDirectory 参数），确保能找到 go.mod

**测试结果**：96 个测试全部通过（crypto 31 + vault 35 + sync_engine 10 + multi_device 5 + safe_server 15）

**改动文件**：
- **修改 [lib/sync/sync_engine.dart](file:///c:/Home/Projects/safenotes/lib/sync/sync_engine.dart)**: vault 改非 final；_executeMigration 更新 vault 引用；_syncOnce 迁移逻辑优化；新增 _bytesEqual
- **修改 [test/sync/safe_server_integration_test.dart](file:///c:/Home/Projects/safenotes/test/sync/safe_server_integration_test.dart)**: startGoServer 用 workingDirectory 在 server/go/ 下运行
- **修改 [test/sync/multi_device_test.dart](file:///c:/Home/Projects/safenotes/test/sync/multi_device_test.dart)**: H1 测试设备 B 传新密码派生的 mk

## [2026-07-28 17:47:31] - P0-1 多端 join + dataKey 迁移（同步协议重构核心改造）

实现多设备同步的核心密钥架构改造，解决"相同账号密码不同设备同步显示 cipher 错误"的根本问题。

**核心架构变更**：两层密钥架构（MK + dataKey + encryptedDataKey）
- MK = PBKDF2-HMAC-SHA256(password, 固定 salt 'safenotes-v1', 200000 次迭代) ← 改密码时变化
- dataKey = 随机 32 字节 ← 永不变化，真正加密笔记
- encryptedDataKey = AES-GCM(MK, dataKey) ← 存 manifest header（明文）和本地 meta

**关键改动**：
- **修改 [lib/sync/crypto.dart](file:///c:/Home/Projects/safenotes/lib/sync/crypto.dart)**: 新增 `kFixedSalt = 'safenotes-v1'` 常量；`deriveMasterKey` 默认使用固定 salt（保证多端派生相同 MK）；新增 `deriveMasterKeyAsync` 用 Isolate 后台执行 PBKDF2 避免阻塞 UI
- **修改 [lib/sync/vault.dart](file:///c:/Home/Projects/safenotes/lib/sync/vault.dart)**: 两层密钥架构落地；新增 `MigrationResult`、`checkMigrationNeeded`、`migrateToRemote`；新增 `unlockFromRemoteManifest`（新设备首次加入场景）；缓存 `_mk`（仅内存，logout 清零）
- **修改 [lib/sync/sync_models.dart](file:///c:/Home/Projects/safenotes/lib/sync/sync_models.dart)**: Manifest 拆分为 `ManifestHeader`（明文，含 vaultId/encryptedDataKey/KDF 参数/lastModifiedBy）+ `items`（加密）
- **修改 [lib/sync/sync_engine.dart](file:///c:/Home/Projects/safenotes/lib/sync/sync_engine.dart)**: 5 步同步流程增加 dataKey 迁移检测（Step 1）；迁移成功后抛 `_MigrationRequiredException` 触发重新同步；构造函数改用 `vault` 参数
- **修改 [lib/data/database_handler.dart](file:///c:/Home/Projects/safenotes/lib/data/database_handler.dart)**: 新增 `reEncryptAllNotes`（事务保护，crash 安全；内存中先解密→重新加密→事务写入）
- **新增 [lib/utils/device_id.dart](file:///c:/Home/Projects/safenotes/lib/utils/device_id.dart)**: 获取 `<platform>-<id>` 格式设备 ID（写入 manifest header.lastModifiedBy）
- **修改 [lib/sync/sync_service.dart](file:///c:/Home/Projects/safenotes/lib/sync/sync_service.dart)**: `logout` 清 dataKey + MK 缓存；`updateVault` 用于迁移后重建 engine
- **修改 [test/sync/safe_server_integration_test.dart](file:///c:/Home/Projects/safenotes/test/sync/safe_server_integration_test.dart)**: 修复 `_makeEngine` 编译错误，改用 `Vault` 构造 `SyncEngine`

**验证**：
- `flutter analyze lib/sync/ lib/data/database_handler.dart lib/utils/device_id.dart test/sync/` → 0 error / 0 warning（9 个 info 级 prefer_const_declarations，不影响功能）
- `flutter test test/sync/crypto_test.dart test/sync/vault_test.dart test/sync/sync_engine_test.dart` → 76/76 通过
- `flutter build apk --debug` → 成功
- ⚠️ `safe_server_integration_test.dart` 因 `server/go/` 缺 `go.mod` 无法启动 Go server，13 个集成测试待修复环境后补测

**待后续任务**：P0-2 Go server 乐观锁竞态 / P1 凭据改 secure_storage / P2 blob hash 校验 / 多设备集成测试

## [2026-07-28 16:07:42] - 修复 Windows 桌面端启动崩溃（sqflite_ffi 未初始化）

修复 Windows 端登录后立即崩溃 `Bad state: databaseFactory not initialized` 的问题。

**根本原因**：`sqflite` 原生只支持 Android/iOS，桌面平台（Windows/macOS/Linux）必须用 `sqflite_common_ffi` 并手动调用 `sqfliteFfiInit()` + `databaseFactory = databaseFactoryFfi` 初始化。原版 main.dart 没有桌面端初始化逻辑。

- **修改 [lib/main.dart](file:///c:/Home/Projects/safenotes/lib/main.dart#L39-L47)**: 在 `main()` 开头添加桌面平台 sqflite_ffi 初始化
  - 新增 `dart:io` (Platform) 和 `package:flutter/foundation.dart` (kIsWeb) 导入
  - 新增 `package:sqflite_common_ffi/sqflite_ffi.dart` 导入
  - 在 `WidgetsFlutterBinding.ensureInitialized()` 后判断：`!kIsWeb && (Platform.isWindows || Platform.isLinux || Platform.isMacOS)` → 调用 `sqfliteFfiInit()` + `databaseFactory = databaseFactoryFfi`

- **修改 [pubspec.yaml](file:///c:/Home/Projects/safenotes/pubspec.yaml#L43)**: `sqflite_common_ffi` 从 dev_dependencies 移到 dependencies
  - 原因：运行时也需要此包（main.dart 直接导入），不只是测试用

- **验证**: `flutter analyze` 0 error，55/55 单元测试通过，`flutter build windows --debug` 成功

## [2026-07-28 16:00:16] - 修复 local_auth 3.0 API + Windows MSVC 14.51 coroutine 错误

升级 local_auth 和 permission_handler 后遇到两个问题：

1. **Dart API 破坏性变更**：local_auth 3.0.0 移除了 `AuthenticationOptions` 和 `options` 参数，改为直接传 `persistAcrossBackgrounding` 等参数
2. **Windows 构建失败**：MSVC 14.51 (VS 2026) 将 `<experimental/coroutine>` 从弃用改为硬错误，影响 local_auth_windows 和 permission_handler_windows 插件

- **修改 [lib/views/authentication/login.dart](file:///c:/Home/Projects/safenotes/lib/views/authentication/login.dart#L490-L493)**: 生物认证调用适配 local_auth 3.0 新 API
  - `options: const AuthenticationOptions(stickyAuth: true)` → `persistAcrossBackgrounding: true`

- **修改 [windows/CMakeLists.txt](file:///c:/Home/Projects/safenotes/windows/CMakeLists.txt#L9-L14)**: 添加全局编译定义抑制 coroutine 弃用错误
  - `add_definitions(-D_SILENCE_EXPERIMENTAL_COROUTINE_DEPRECATION_WARNINGS)`
  - 此宏将 MSVC 14.51 的 `<experimental/coroutine>` 硬错误降级为警告
  - 参考：https://github.com/baseflow/flutter-permission-handler/issues/1534
  - 待 local_auth_windows 和 permission_handler_windows 迁移到 C++20 coroutine 后可移除

- **验证**: `flutter analyze` 0 error，`flutter build windows --debug` 成功

## [2026-07-28 15:40:10] - 修复退出登录崩溃（恢复原版退出逻辑）

修复退出登录时 `DataKeyNotSetException` 和 `Navigator _history.isNotEmpty` 断言失败。

**根本原因**：`Session.logout()` 中加了 `NotesDatabase.instance.clearDataKey()`，而原版退出逻辑是先 `logout()` 后导航。dataKey 被清除后，HomePage 之前 fire-and-forget 启动的 `refreshNotes()` 异步操作继续执行 `readAllNotes()`，触发 `_requireDataKey` 抛异常，导致 widget rebuild 时 Navigator 状态不一致。

**修复方案**：恢复原版退出逻辑，`Session.logout()` 不清除 dataKey（与原版一致）。重新登录时 `Vault.unlockLocal` → `database.setDataKey()` 会覆盖旧 dataKey，安全性足够。

- **修改 [lib/models/session.dart](file:///c:/Home/Projects/safenotes/lib/models/session.dart)**: `Session.logout()` 移除 `clearDataKey()` 调用，只清密码（与原版一致）
- **恢复 [lib/views/settings/settings.dart](file:///c:/Home/Projects/safenotes/lib/views/settings/settings.dart)**: 退出按钮逻辑恢复原版（先 `logout` 后导航）
- **恢复 [lib/views/home.dart](file:///c:/Home/Projects/safenotes/lib/views/home.dart)**: `onLogoutCallback` 恢复原版顺序；`_sortAndStoreNotes` 移除 try-catch
- **恢复 [lib/views/deleted_notes.dart](file:///c:/Home/Projects/safenotes/lib/views/deleted_notes.dart)**: `_refresh` 移除 try-catch

- **验证**: `flutter analyze` 0 error（3 个已有 info），`flutter build apk --debug` 成功

## [2026-07-28 15:21:10] - 修复退出登录时 DataKeyNotSetException 崩溃

修复在设置页面点击退出时报 `DataKeyNotSetException: dataKey 未设置` 的问题。

**根本原因**：退出登录时先 `await Session.logout()` 清除了 dataKey，然后才导航到登录页。但 HomePage 之前 fire-and-forget 启动的 `refreshNotes()` 异步操作可能还没完成，`readAllNotes()` 在 dataKey 清除后继续执行解密，触发 `_requireDataKey` 抛异常。该未捕获异常进一步导致 Navigator `_history.isNotEmpty` 断言失败。

- **修改 [lib/views/settings/settings.dart](file:///c:/Home/Projects/safenotes/lib/views/settings/settings.dart)**: 退出按钮 onPressed 逻辑调整
  - 原序：`Session.logout()` → `add(stopListening)` → `pushNamedAndRemoveUntil`
  - 新序：`add(stopListening)` → `pushNamedAndRemoveUntil` → `Session.logout()`
  - 先导航移除当前路由（HomePage 被 dispose 不会再 rebuild），导航完成后再清除 dataKey

- **修改 [lib/views/home.dart](file:///c:/Home/Projects/safenotes/lib/views/home.dart)**: 两处修复
  - drawer 的 `onLogoutCallback`：同样调整为先导航再清除 dataKey
  - `_sortAndStoreNotes()`：添加 `try-catch DataKeyNotSetException`，防御性处理之前启动的异步操作在 dataKey 清除后继续执行的情况，静默返回不更新列表
  - 添加 `if (!mounted) return` 防止 dispose 后调用 setState

- **修改 [lib/views/deleted_notes.dart](file:///c:/Home/Projects/safenotes/lib/views/deleted_notes.dart)**: `_refresh()` 添加 `try-catch DataKeyNotSetException`，同样防御退出登录时的异步操作崩溃

- **验证**:
  - `flutter analyze`：3 个 info（已有 `use_super_parameters` lint，非本次引入），0 error/warning
  - `flutter build apk --debug`：构建成功

## [2026-07-28 15:04:38] - PBKDF2 迭代次数降至 200k + Isolate 后台派生

解决小米14 首次输入密码后界面卡顿 4-5 秒的问题。根本原因是 PBKDF2 600k 迭代在主线程执行，纯 Dart 实现无硬件加速，手机端耗时过长。

- **修改 [lib/sync/crypto.dart](file:///c:/Home/Projects/safenotes/lib/sync/crypto.dart)**:
  - `_pbkdf2Iterations` 常量 600,000 → 200,000（手机端约 1-1.5 秒，桌面 ~50ms）
  - 安全性评估：RTX 4090 约 6,000 H/s，8 位混合密码理论破解需 ~1,153 年，个人笔记场景足够
  - 新增 `deriveMasterKeyAsync()` 方法：通过 `compute()` 在后台 Isolate 执行 PBKDF2，不阻塞 UI 线程
  - 新增 `_DeriveParams` 类：Isolate 参数载体（password + salt + iterations），可序列化跨 Isolate 传递
  - 新增 `_deriveMasterKeyIsolate()` 静态方法：Isolate 入口函数，执行 PBKDF2 派生
  - 保留原同步 `deriveMasterKey()` 供单元测试使用
  - 新增 `package:flutter/foundation.dart` 导入（`compute` 函数）

- **修改 [lib/sync/vault.dart](file:///c:/Home/Projects/safenotes/lib/sync/vault.dart)**:
  - `_deriveMk()` 从同步改为 `async`，内部调用 `deriveMasterKeyAsync()`
  - 4 个调用点加 `await`：`createNew` (L101)、`unlockLocal` (L188)、`changePassword` 旧密码验证 (L233)、`changePassword` 新密码派生 (L244)
  - 效果：登录/解锁/改密码时 UI 线程不阻塞，可正常显示加载动画

- **效果对比**:
  | 场景 | 改前 | 改后 |
  |------|------|------|
  | 小米14 解锁耗时 | 4-5 秒（卡死） | 1-1.5 秒（UI 流畅） |
  | UI 线程 | 阻塞 | 不阻塞 |
  | GPU 抗性 | 2,000 H/s | 6,000 H/s（仍足够） |

- **验证**:
  - 单元测试：71/71 通过（crypto 31 + vault 23 + sync_engine 11 + local_fs_backend 6）
  - `flutter analyze` lib/sync/ + database_handler.dart：0 issues
  - `flutter build apk --debug` 构建成功

## [2026-07-28 14:47:57] - sync-protocol-spec.md 升级到 v2 + 集成测试重构

基于用户确认的三个设计决策（WebDAV 子目录常量 `safenotes-vault`、providerKey 16 位哈希、无需迁移兼容），更新协议规范文档并修复集成测试超时问题。

- **修改 [docs/sync-protocol-spec.md](file:///c:/Home/Projects/safenotes/docs/sync-protocol-spec.md)**: v1 → v2
  - §四 HTTP API 重构：从单一 WebDAV 协议改为按后端类型分别描述（§4.1 通用语义 / §4.2 WebDAV / §4.3 SafeServer / §4.4 LocalFs）
  - 移除 MKCOL 作为通用要求：改为 WebDAV 仅（§4.2 端点表中标注）
  - 认证方式按后端区分：WebDAV 用 Basic Auth，SafeServer 用 Bearer Token
  - 新增 §十二 后端隔离（providerKey）：16 字符 SHA-256 哈希，按 `"<backendType>:<baseUrl>"` 计算，用于隔离 manifest version，切换后端时自动重置
  - §十二 新增 WebDAV 子目录常量说明：`kWebDavVaultSubdir = 'safenotes-vault'`，多设备共享时自动一致
  - §十一 修复测试文件引用：`webdav_integration_test.dart` → `safe_server_integration_test.dart`，补充完整测试套件列表
  - §九 错误处理：状态码表新增"适用后端"列，MKCOL 405 标注为 WebDAV 仅
  - §十 安全考量：补充 Bearer Token + constant-time compare、路径穿越防护

- **修改 [test/sync/safe_server_integration_test.dart](file:///c:/Home/Projects/safenotes/test/sync/safe_server_integration_test.dart)**: 集成测试重构
  - 新增库级 `@Timeout(Duration(seconds: 120))` 注解（替代未使用的局部变量）
  - server 启动从 `setUp` 移到 `setUpAll`：所有测试共享一个 server 进程，避免每个测试重复 `go run` 编译
  - 新增 `ServerProcess.clearData()` 方法：测试间清理 manifest + blobs 文件，比重启 server 快得多
  - 移除未使用的 `_integrationTimeout` 和 `t` 变量
  - 效果：15/15 测试通过，从原来的 5 个超时失败 → 全部通过，总耗时从 5+ 分钟降至 2 秒（首次 go run 编译后）

- **验证**:
  - 单元测试：71/71 通过（crypto 31 + vault 23 + sync_engine 11 + local_fs_backend 6）
  - 集成测试：15/15 通过（SafeServer + Go server，2 秒）
  - `flutter analyze` lib/sync/ + database_handler.dart + 集成测试：0 issues
  - `flutter build apk --debug` 构建成功

## [2026-07-28 13:35:51] - 拆分独立的服务端 API 规范文档

将原 sync-protocol-spec.md 中服务端相关部分抽取为自包含文档，让实现者无需阅读客户端逻辑即可实现一个兼容的 server；客户端也不需要关心 server 内部实现，只要接口符合规范即可互操作。

- **新增 [docs/server-api-spec.md](file:///c:/Home/Projects/safenotes/docs/server-api-spec.md)**:
  - 17 章节的自包含服务端接口规范，约 600 行
  - 涵盖：目标与边界（明确不定义客户端逻辑）、设计原则、术语（服务端视角）、认证（HTTP Basic Auth + constant-time compare）、资源模型（vault-root/manifest/blob 三类资源）、HTTP API（MKCOL/GET/PUT manifest/blob + /health 共 6 个端点，逐一给出请求示例/响应表/实现要点）、ETag 规范（强 ETag、变化时机、3 种实现方案对比、引号处理规则）、乐观锁规则（If-Match/If-None-Match/都不带/并发安全）、错误处理（状态码总表/重试语义/响应 body）、存储实现指引（物理布局/路径穿越防护/原子写入/并发控制/孤儿 blob/目录自动创建）、安全要求（HTTPS/凭证安全/零知识保证/ETag 不泄露/blob 不可枚举/速率限制）、幂等性保证、与 WebDAV 的兼容性映射、测试验证（必过测试套件/14 类场景/测试适配方法/curl 手动验证脚本）、参考实现、v1 不强制支持的特性清单

- **修改 [docs/sync-protocol-spec.md](file:///c:/Home/Projects/safenotes/docs/sync-protocol-spec.md)**: 顶部新增"文档分工"说明，明确本文档为完整客户端侧协议规范，服务端实现者推荐阅读独立的 server-api-spec.md

- **修改 [server/README.md](file:///c:/Home/Projects/safenotes/server/README.md)**（不入 Git）: 协议规范章节更新，将 server-api-spec.md 作为推荐入口，sync-protocol-spec.md 标注为完整协议参考

- **验证**: 文档拆分后 sync/ 测试 84/84 仍全部通过（无代码改动，仅文档变更）

## [2026-07-28 13:27:10] - 同步协议规范文档 + Go/Node.js 参考服务端 + 集成测试

将 Flutter 客户端使用的同步协议提炼为语言无关的规范文档，并实现两个独立参考服务端用于交叉验证客户端协议兼容性。`server/` 子目录已加入 `.gitignore`，仅供本地测试。

- **新增 [docs/sync-protocol-spec.md](file:///c:/Home/Projects/safenotes/docs/sync-protocol-spec.md)**:
  - 语言无关的同步协议规范 v1，供第三方参考实现
  - 涵盖：设计原则（零知识/无状态/后端无关/内容寻址）、术语表、存储模型（manifest + blobs/）、HTTP API（MKCOL/GET/PUT）、ETag 乐观锁（If-Match/If-None-Match）、manifest 与 blob 数据格式、AES-256-GCM + PBKDF2 两层密钥架构、客户端同步算法（拉取→比对→LWW 冲突解决→推送）、错误处理与安全要求

- **新增 [server/go/main.go](file:///c:/Home/Projects/safenotes/server/go/main.go)**（不入 Git）:
  - Go 参考服务端实现，纯标准库（net/http + crypto/sha256 + path/filepath）
  - Basic Auth（constant-time compare）、路径穿越防护、原子写入（tmp + rename）
  - manifest.json 返回 SHA-256 强 ETag、支持 If-Match / If-None-Match: * 乐观锁
  - MKCOL 幂等（201 新建 / 405 已存在）、/health 健康检查端点

- **新增 [server/nodejs/server.js](file:///c:/Home/Projects/safenotes/server/nodejs/server.js)**（不入 Git）:
  - Node.js 参考服务端，纯内置模块（http + fs + path + crypto），无任何 npm 依赖
  - 与 Go server 协议语义完全一致，互操作可替换
  - manifest 写入用 Promise 链串行化，避免并发 PUT 竞争

- **新增 [server/README.md](file:///c:/Home/Projects/safenotes/server/README.md)**（不入 Git）:
  - 服务端运行说明、目录结构、快速启动命令、集成测试用法

- **新增 [test/sync/webdav_integration_test.dart](file:///c:/Home/Projects/safenotes/test/sync/webdav_integration_test.dart)**:
  - 13 项端到端集成测试，自动启动/停止 server 子进程并轮询 /health 就绪
  - 覆盖：空库首次同步、上传 3 条笔记、新设备下载、双方增量互同步、LWW 冲突解决（远端 updatedAt 大胜出）、墓碑软删除传播、连续两次同步幂等性
  - 原始 HTTP 协议验证：404/401/405/412 状态码、If-None-Match: * 首次上传、If-Match 错误 etag 拒绝、blob PUT/GET 端到端
  - 通过 `SN_SERVER=node` 环境变量切换 Go/Node.js server

- **修改 [.gitignore](file:///c:/Home/Projects/safenotes/.gitignore)**: 新增 `/server/` 规则，参考服务端不入版本控制

- **验证**:
  - Go server: 13/13 集成测试通过（耗时约 16s，含 go run 首次编译）
  - Node.js server: 13/13 集成测试通过（耗时约 6s）
  - sync/ 全量测试: 84/84 通过
  - `flutter build apk --debug` 构建成功

## [2026-07-28 12:46:43] - Effective Dart 规范对照修复

对照 `F:\Downloads\darl-ang-site\src\content\effective-dart` 指南，修复 4 个文件共 19 处违规：

- **[lib/data/database_handler.dart](file:///c:/Home/Projects/safenotes/lib/data/database_handler.dart)**: 
  - 1 处裸 `catch (e)` → `on Exception catch (e)`（Effective Dart: 避免捕获 Error 类型）
  - 3 处裸 `Future` → `Future<void>`（`_createDB` / `_upgradeDB` / `close`，Effective Dart: 必须指定类型参数）
  - 2 处不必要的 `async =>` → `=>`（`exportAllEncrypted` / `encryptAndStore`，Effective Dart: body 只是返回 Future 时无需 async）

- **[lib/sync/webdav_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/webdav_backend.dart)**: 
  - 5 处裸 `catch (e)` → `on Exception catch (e)`（getManifest / putManifest / getBlob / putBlob / _mkcol）

- **[lib/sync/crypto.dart](file:///c:/Home/Projects/safenotes/lib/sync/crypto.dart)**: 
  - 5 处函数体改为 `=>` 表达式体（`generateDataKey` / `generateSalt` / `generateNonce` / `contentHash` / `hashString`，Effective Dart: 简单成员优先用 =>）

- **[lib/sync/vault.dart](file:///c:/Home/Projects/safenotes/lib/sync/vault.dart)**: 
  - 2 处 `async { return ... }` → `=>`（`getVaultId` / `getEncryptedDataKey`，Effective Dart: body 只是返回 Future 时无需 async）
  - 1 处函数体改为 `=>` 表达式体（`_generateVaultId`）

- **验证**: 71 项单元测试全部通过；`flutter analyze` 对 sync/ + database_handler.dart 0 issues；`flutter build apk --debug` 构建成功

## [2026-07-28 10:40:34] - 阶段四：SyncBackend 抽象接口 + LocalFsBackend + WebDavBackend 实现

- **新增 [lib/sync/sync_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/sync_backend.dart)**: 
  - 定义 `SyncBackend` 抽象接口（init / getManifest / putManifest / getBlob / putBlob / close）
  - 定义三个异常类：`ConflictException`（乐观锁冲突）、`BackendUnavailableException`（网络/服务端错误）、`BackendNotInitializedException`（未初始化）
  - 设计要点：后端无关（SyncEngine 只依赖此接口）、纯存储（无业务逻辑）、乐观锁只在 PUT manifest 时检查一次、blob 按 hash 内容寻址天然去重

- **新增 [lib/sync/local_fs_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/local_fs_backend.dart)**: 
  - 基于本地文件系统的后端实现，用于单机测试和离线沙箱
  - 存储布局：`<rootPath>/manifest.json` + `<rootPath>/blobs/<hash>`
  - ETag 策略：用文件内容的 SHA-256 作为强 ETag，与 WebDAV 语义一致
  - 乐观锁实现：PUT manifest 时读取当前文件算 hash 比对，不匹配抛 `ConflictException`
  - 不依赖 path_provider，由上层传入绝对路径，便于单元测试

- **新增 [lib/sync/webdav_backend.dart](file:///c:/Home/Projects/safenotes/lib/sync/webdav_backend.dart)**: 
  - WebDAV 后端实现，支持坚果云 / NextCloud / ownCloud / Apache mod_dav 等
  - 使用 HTTP Basic Auth 鉴权（坚果云用应用专用密码）
  - `MKCOL` 创建远端目录（幂等：201 新建 / 405 已存在均正常）
  - `If-Match: "<etag>"` 实现 manifest 乐观锁，412/409 抛 `ConflictException`
  - `If-None-Match: *` 实现首次上传防覆盖
  - ETag 规范化：去掉 `W/` 弱前缀和引号，服务器未返回 ETag 时退化为内容 SHA-256
  - 可注入 `http.Client` 便于测试

- **新增 [test/sync/local_fs_backend_test.dart](file:///c:/Home/Projects/safenotes/test/sync/local_fs_backend_test.dart)**: 
  - 16 个单元测试，覆盖：初始化、manifest CRUD、乐观锁冲突、blob 幂等性、完整同步流程模拟
  - 全部通过

- **修改 [pubspec.yaml](file:///c:/Home/Projects/safenotes/pubspec.yaml)**: 
  - SDK 约束从 `>=2.12.0 <4.0.0` 提升到 `>=3.0.0 <4.0.0`
  - 原因：`safenote.dart` 已使用 records（Dart 3.0+ 特性），新增的 `sync_backend.dart` 也需要 records 返回 manifest 密文+etag
  - 影响：项目语言版本从 2.12 提升到 3.0，启用 records / patterns 等新特性，不破坏 2.12 代码

- **测试结果**: 
  - `flutter test test/sync/` 全部通过（41 个测试：25 crypto + 16 backend）
  - crypto_test.dart 验证加密层正确性
  - local_fs_backend_test.dart 验证后端基础功能和乐观锁逻辑
