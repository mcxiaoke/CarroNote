# 2026-08-14 变更记录

## 修复：宽屏侧边栏背景丢失（回归自 43d05ed）（2026-08-14 11:33 GMT+8）

用户反馈"宽屏侧边栏背景变纯白"。经 stash 对比确认非本次 UI/UX 改动引入，定位为 `43d05ed feat(ui): migrate home/notes, drawer and sidebar to shadcn_ui`：该 commit 将 `HomeSidebar` 背景从 Material `colorScheme.surfaceContainerLow`（浅灰）改为 `ShadTheme.colorScheme.background`（亮色下纯白），而移动端 Drawer 仍走 Material 主题背景，故 Drawer 正常、宽屏侧边栏与浅灰 body 背景割裂。

- **`lib/widgets/home_navigation_rail.dart`**：还原背景/前景/分隔线为 Material 主题——`bg = colorScheme.surfaceContainerLow`、`fg = colorScheme.onSurface`、`divider = colorScheme.outlineVariant`；移除 `ShadTheme.of(context)` 与 `shadcn_ui` import（不再使用）。菜单项（shadNavMenuItem）仍为 Shad 品牌色图标容器，与 Drawer 视觉一致。
- 验证：`flutter analyze` 无问题 → `flutter build windows --debug` 成功 → `flutter run -d windows` 启动无 crash → 进程已结束。

## UI/UX 第一、二梯队落地：设计令牌系统 + 卡片质感 + 状态三件套（2026-08-14 10:42 GMT+8）

按 `docs/ui-ux-improvement-report-20260814.md`（修订版）§1.1/§1.2 清单实施；P2-1（双设计系统收敛）按用户指示暂缓。

**新增令牌文件（P0-2 / P1-1 / P1-11 / P1-14 / P1-20）**

- `lib/utils/spacing.dart`：`AppSpace`（间距 4/8/12/16/20/24 + cardPadding）、`AppShape`（input/card/button/dialog 圆角 + 输入/搜索高度 48）、`AppIcon`（sm16/md20/lg24）。
- `lib/utils/text_styles.dart`：`AppText`（title 20 / titleCompact 18 / body 16 / caption 13 / label 12）。
- `lib/utils/motion.dart`：`AppMotion`（fast 120 / normal 220 / slow 320 / pageTransition 250）。

**第一梯队（视觉止血）**

- P0-1：`note_card.dart` 删除 `getMaxLine(index%4)`，摘要固定 `maxLines: 3`（网格卡高度锯齿消除；列表卡本就固定行数）。
- P0-2：4 个笔记卡 padding 统一 `AppSpace.cardPadding`（原 all(10) / h20×v10），行间距走 AppSpace。
- P0-3：新增 `lib/widgets/note_card_press_feedback.dart`（`NoteCardPressFeedback`：MouseRegion 手型 + hover 5% / press 10% 黑/白遮罩 + 点击回调），`home.dart` 的 `OpenContainer.closedBuilder` 由 GestureDetector 换用该组件（InkWell ripple 会被不透明卡片盖住故不用）。
- P0-4：4 个卡片 `TextOverflow.clip` → `ellipsis`。
- P1-14：卡片圆角 10→12（`AppShape.cardRadius`）；`search_widget.dart` 高度 44→48、圆角 7→8、内部垂直 padding 10→12；6 个旧 Dialog 圆角常量 10→12（`dialogs/generic|logout_alert|confirm_import|backup_import|backup_passphrase|backup_password_input`）。
- P0-5/P3-11：新增 `lib/widgets/states.dart`（`emptyState`/`loadingState`/`errorState`），主页 `_handleAndBuildNotes` 空态/加载态与回收站 `_buildBody` 接入；新增翻译键 `New Note`（en-US/zh-CN）。

**第二梯队（设计令牌）**

- P1-1：4 个卡片字号走 `AppText`（数值不变：20/13/16、compact 18）。
- P1-11：动画时长令牌化——`add_edit_note` 150→normal、`login`/`set_passphrase` 500→slow、`change_passphrase` 300→normal、`sync_backend_config_page` 100→fast；`home.dart` OpenContainer 250 保持数值但改 `AppMotion.pageTransition`（注释注明：FittedBox 缩放渲染掉帧权衡，不上调 slow）。
- P1-20：`login.dart` 指纹图标 `isDesktop ? 22 : 28` → `AppIcon.lg`（删除平台分支）；`styles.dart` `kInputIconSize` = `AppIcon.md`；`shad_settings_tiles.dart:325`、`shad_nav_items.dart:47` size 18 → `AppIcon.sm`(16)；`search_widget.dart` 搜索图标 → `AppIcon.md`。

**验证**：`flutter analyze` 无问题 → `flutter build windows --debug` 成功 → `flutter run -d windows` 启动至本地化初始化（登录页前）无 crash → 已结束进程。未 git 提交（等用户指示）。

## 拆分 README 与开发指南（2026-08-14 GMT+8）

大幅精简 `README.md` / `README.zh.md`，仅保留项目简介、特性、顶层目录结构与文档指针；
将详细的代码结构（逐目录职责表、加密/密钥架构、同步 5 步流程、服务端）、CLI、日志、技术栈、
构建运行、构建信息注入、测试流程等全部迁移至新增开发指南
`docs/DEVELOPMENT.md`（EN）与 `docs/DEVELOPMENT.zh.md`（中文）。README 末尾新增对开发指南的引用。

## 新增 WSNS Linux/amd64 一键安装包（2026-08-14 10:10 GMT+8）

为 linux amd64 服务器提供「本地打包 zip → scp → 解压 → 一键安装」流程。

- **`server/deploy/install.sh`**：在目标服务器以 root 运行，把同目录的
  `wsns`（二进制）、`wsns.service`、`config.json` 安装到
  `/usr/local/bin/wsns`、`/etc/systemd/system/wsns.service`、`/etc/safenotes/config.json`；
  自动创建专用用户 `safenotes`、数据目录 `/var/lib/safenotes`、备份目录
  `/var/backups/wsns`；启用并启动服务，最后打印各文件路径与生成的 Token。
  - Token 默认用 `openssl rand -hex 24` 强随机生成；可用 `WSNS_TOKEN=xxx` 覆盖，
    或保留 `config.json` 中已有真实 Token。
  - 支持 `--no-start` 仅安装不启动；无 systemd 时打印手动启动命令。
  - 已有 `/etc/safenotes/config.json` 会先备份为 `.bak.<时间戳>` 再覆盖。
- **`server/deploy/config.json`**：生产配置模板（数据目录/备份/超时/日志），
  Token 用占位符 `__WSNS_TOKEN__` 由安装脚本注入。
- **`server/deploy/package.sh`**：开发机交叉编译 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64`
  并打包 `server/dist/wsns-linux-amd64.zip`（含二进制+服务+配置+安装脚本），
  zip 缺失时回退 7z/tar.gz。
- **`server/deploy/wsns.service`**：`ExecStart` 改为
  `/usr/local/bin/wsns -config /etc/safenotes/config.json`；`ReadWritePaths` 增加
  `/var/backups/wsns`（Tier 2 归档目录）。
- **文档**：`docs/server-implementation.md` §6.6 新增一键安装包说明。

### 验证

- `bash server/deploy/package.sh`：成功产出 `wsns-linux-amd64.zip`，
  内含 `wsns`（ELF 64-bit x86-64 静态链接）、`wsns.service`、`config.json`、`install.sh`。
- `bash -n install.sh` / `package.sh`：语法检查通过。

## Go server 二进制/包名重命名 safeserver → wsns（2026-08-14 09:48 GMT+8）

将 Go 参考服务端的产物名与 Go module 名统一为 `wsns`（含义：WebDAV-compatible
SafeNotes Server），便于分发与跨平台一致。

- **Go module**：`safenotes-server` → `wsns`（`server/go/go.mod` + 全部 `import`
  路径 `safenotes-server/internal/...` → `wsns/internal/...`）。
- **构建产物名**：`server/build.go` 输出 `safeserver-<os>-<arch>[.exe]` →
  `wsns-<os>-<arch>[.exe]`；`server/Makefile` 的 `BIN` 同步改为 `wsns`；
  `server/justfile`、`server/README.md` 注释同步。
- **systemd 服务**：`server/deploy/safeserver.service` 重命名为
  `server/deploy/wsns.service`，内容里二进制路径、`systemctl` 单元名、
  `SyslogIdentifier` 全部改为 `wsns`。
- **开发日志**：`server/go/dev.json`、`dev.config.json` 的 `logFile` 由
  `./temp/safeserver-debug.log` → `./temp/wsns-debug.log`。
- **Flutter 集成测试**：`packages/core/test/sync/safe_server_integration_test.dart`
  的 Go 二进制路径 `$kTestRoot\wsns.exe` 与注释同步；
  `test/scripts/test-cleanup.ps1` 进程匹配新增 `wsns\.exe`（保留 `safeserver\.exe`、
  `safenotes-server\.exe` 历史兼容），日志匹配新增 `wsns-*.log`。
- **文档**：`docs/server-implementation.md`、`docs/sync-feature-design.md`
  （`syncserver` → `wsns`）、`docs/server-backup-design.md`（import 示例）同步；
  `server/README.md` 模块名/服务文件名/产物路径同步。

注意：Dart/CLI 侧的同步后端类型名 `safeserver`（协议概念）与 Node.js 参考实现
（`server/nodejs`，含 `package.json` name 与 node 调试日志）保持不变。

### 验证

- `cd server/go && go build ./...`：通过。
- `git diff` 仅含预期的 import / 产物名改动；`engine_test.go` 等仅 2 行 import
  路径变化（修复了首次 `sed` 误改 CRLF 行尾导致的整文件 diff）。

## 非 shadcn 组件收尾：deleted_notes + sync_diagnostics 等页面替换为 shadcn（2026-08-14 08:57 GMT+8）

承接 `docs/theme-fcs-removal-analysis-20260813.md` 的「低风险高收益」+「中风险」两类
替换（SnackBar 大工程按约定搁置）。FCS 此前已移除，本次只清 Material 组件残余。

### 1. `lib/views/settings/sync_diagnostics_page.dart`（Debug 面板，中风险集中地）

- **TabBar/TabBarView → ShadTabs<String>**：`SingleTickerProviderStateMixin` + `TabController`
  移除，改 `_selectedTab` 字符串 + `expandContent: true`（内容撑满）。5 个 tab 设
  `maintainState: false` 与原 TabBarView 一致（非激活 tab 不常驻构建）。
- **ExpansionTile → ShadAccordion**（`_buildActionCard`）：操作记录折叠项改用
  `ShadCard + ShadAccordion`，title 改 Row（图标 + 类型 + uuid 摘要），详情进
  `ShadAccordionItem.child`。
- **PopupMenuButton（日志级别过滤）→ ShadPopover**：`ShadIconButton.raw(ghost)` 触发，
  弹层内多选行直接 `setState` 切换级别，点外部关闭（多选过滤场景优于原单选菜单）。
- **裸按钮 → ShadButton**：`ElevatedButton.icon`（导出诊断）→
  `ShadButton.raw(outline)`；`FilledButton.icon`（启动/停止 Web 服务器）→
  `ShadButton`（`_isStarting` 时 `onPressed: null` 禁用）。

### 2. `lib/views/deleted_notes.dart`（回收站）

- **ListTile ×3 → 自定义 Row 布局**：项主体改 `ShadCard` 内 `Row`（删除图标 + 标题/描述/
  删除时间列 + 更多菜单）；菜单内两个 `ListTile` 一并改 `_menuRow`（图标 + 文字）。
- **PopupMenuButton → ShadPopover**：`_DeletedNoteTile` 由 StatelessWidget 改为
  StatefulWidget 持有 `ShadPopoverController`，恢复/永久删除动作先 `hide()` 再回调。

### 3. `lib/views/settings/theme_setting.dart`

- **BottomSheet → ShadSheet**：`showModalBottomSheet` 改 `showShadSheet(side: bottom)`；
  `ShadSheet` 关闭默认 padding/边框/背景，保留顶部 16 圆角与桌面端 `maxWidth`
  居中约束。

### 4. `lib/views/home.dart`

- **FloatingActionButton → ShadButton**：新建笔记入口改为 56×56 圆形 ShadButton
  （`ShadBorder.all(radius: 28)` + `width/height: 56`）。

### 5. `lib/views/authentication/login.dart`

- **TextButton → ShadButton.raw(link)**：忘记密码逃生入口改 link 变体，
  `padding: EdgeInsets.zero` 保持紧凑。

### 验证

- `flutter analyze`：No issues found。
- `flutter test`：40 项全部通过。
- `dart test`（packages/core）：266 通过；3 失败均为既有集成/混沌测试
  （`safe_server_integration_test` 需外部服务、`chaos_multi_client_test` 多 seed 压力），
  与本次 UI 改动无关（core 包未改动）。

### 修复：进入 Debug 面板崩溃（2026-08-14 09:20 反馈）

现象：进入面板时 `_LogsTabState.initState` 的 `addPostFrameCallback` 里
`ScrollPosition.maxScrollExtent` 空指针。根因：ShadTabs 默认 `maintainState: true`
让日志 tab 在未选中（Offstage 未布局）时也被提前构建，滚动视图未完成首次布局
（`_maxScrollExtent` 为 null）。修复：

- ShadTabs 设 `maintainState: false`，恢复原 TabBarView「非激活 tab 不常驻构建」语义；
- 自动滚动逻辑加 `position.hasContentDimensions` 防护，未布局时跳过。

### 说明

- 替换后 `lib/` 中 PopupMenuButton / ListTile / ExpansionTile / FloatingActionButton /
  TabBar / TabBarView / showModalBottomSheet 均为 0 处（仅 home.dart 残留一句注释）。
- 尚未处理：SnackBar（31 处）→ ShadSonner/ShadToast，按约定搁置。
