# 变更记录 2026-08-22

## 11:15 修复调试面板「清空数据库」导致的密钥不一致崩溃循环

**文件**: `lib/views/settings/sync_diagnostics_page.dart`, `assets/translations/{zh-CN,en-US}.json`

- **问题复盘**: 原 `wipeAllData()` 只清数据表，但 keyring 账本存在 sync_meta 表中，一并被清。清空后内存 keyring 仍存活，autoSync 把远端数据拉回空库；重启后因账本丢失走 set-password 新建金库（新 dataKey），再同步时 scenario-d 迁移需用新 dataKey 解密旧 dataKey 加密的本地行 → GCM 认证失败 → 同步崩溃循环。
- **修复**: 「清空数据库」改为与登录页 `_performLocalDataReset` 同款完整重置：`SyncService.logout()`（停同步、释放内存密钥）→ 关连接后 `deleteDbFile()` 整库删除 → `clearVaultRelatedKeys()` → 置 `vaultInitialized=false` → 调用 `ServicesBinding.exitApplication(AppExitType.required)` 结束进程，用户手动重开进入首次设置向导。不做导航透传、不做备份（调试用途）。
- **清理**: 回退上一版为回 /authwall 而引入的 sessionStream 透传链（home/route_generator/页面构造参数）；core 的 `wipeAllData()` 保留但 UI 不再使用。
- **验证**: `flutter analyze` 无问题；`flutter test` 仅存量 `theme_neutral_verify_test` 失败（干净工作树同样复现，与本次无关）。

## 10:51 调试面板重构：合并同步结果、日志导出到下载目录、新增测试 tab

**文件**: `lib/views/settings/sync_diagnostics_page.dart`, `lib/sync/sync_service.dart`, `packages/core/lib/src/sync/journal.dart`, `packages/core/lib/src/db/database_handler.dart`, `packages/core/lib/src/logger/app_logger.dart`, `assets/translations/{zh-CN,en-US}.json`

- **Tab 精简**: 移除「同步结果」「操作记录」两个 tab（删除 `_SyncResultTab`/`_ActionsTab`）；最近一次同步结果以等宽纯文本块并入状态 tab 底部。
- **日志导出改为写文件**: 日志 tab 下载图标与状态 tab 导出按钮由"复制到剪贴板"改为调用新增的 `SyncService.exportAllLogsToFile()`——优先写入系统 Downloads 目录（`getDownloadsDirectory()`，桌面与 Android 均无需存储权限；平台不支持时回退应用文档目录），文件名 `safenotes-logs-<时间戳>.txt`，SnackBar 展示完整路径。桌面端无需弹出保存位置对话框，直接写 ~/Downloads 即可。
- **新增测试 tab**（三个危险操作按钮，均带确认框）：
  - 清空日志：内存缓冲 + 全部日志文件（core 新增 `AppLogFile.clearLogFiles()`，关闭当前 sink 后删除文件，sink 下次写入自动重建）
  - 清空 Journal：core 新增 `Journal.clear()`——丢弃缓冲与当前日志、删除归档和上传水位状态文件、seq 归 1，写盘走 `_writeChain` 串行化
  - 清空数据库：core 新增 `NotesDatabase.wipeAllData()`——单事务清空 safe_notes/note_meta/note_versions/sync_meta 四表并失效缓存，保留 schema 与连接（区别于整库销毁的 `deleteDbFile()`）
- **验证**: `flutter analyze` 仅剩存量的 `test/pin_keyboard_test.dart` 4 个无关错误；`dart test packages/core/test` 350 个全部通过；`flutter test` 除存量 pin_keyboard 编译错误外 70 个通过。

## 10:27 修复 findIncompleteOperations 对无 opId 条目的误报

**文件**: `packages/core/lib/src/sync/journal.dart`, `packages/core/test/sync/journal_test.dart`

- **问题**: 每次启动报告几十条 `sync.round/start` 未完成（实测 48 条），但同步实际都正常完成了。根因：`findIncompleteOperations` 只按 opId 配对 start↔done/failed，而 `sync.round` 条目从不携带 opId，done 永远无法闭合对应 start，导致每轮正常同步的 start 都被误报为悬挂，随启动次数不断累积。
- **修复**: 无 opId 的条目改为「同类型顺序配对」——start 之后遇到的第一条同类 done/failed 即视为其配对（sync.round 全程串行执行，同一时刻至多一轮在途，该规则是精确的）；带 opId 的条目仍按 opId 精确配对。真正中断的轮次（start 后无 done）依然会被正确报告。
- **测试**: 新增 2 个用例：两轮完整无 opId 同步不报告；第二轮中断仍报告。`dart test packages/core/test` 全部 350 个通过；`flutter analyze` 仅剩存量的 `test/pin_keyboard_test.dart` 4 个无关错误。

## 10:18 增强 IncompleteOperation 日志详细信息

**文件**: `packages/core/lib/src/sync/journal.dart`

- **问题**: `IncompleteOperation.toString()` 只输出 type/opId/age，未完成操作的 uuid、seq、设备、发生时间等关键排查信息全部缺失；且 `sync.round` 类条目本身无 opId，日志中显示 `opId=null` 无从定位。
- **修复**: toString 补充输出 phase、seq、opId（空显示 `-`）、uuid、hash、dataKeyEpoch、by（设备）、at（本地时间 ISO8601）字段，保留 age。
- **验证**: `flutter analyze`（仅存量的 `test/pin_keyboard_test.dart` 5 个无关错误）；`dart test packages/core/test` 全部 348 个测试通过。

## 09:52 修复 /panel 连接状态显示与 ZIP 下载挂起问题

**文件**: `lib/src/logger/log_webserver.dart`, `assets/web/dashboard.html`, `test/logger/log_webserver_test.dart`

- **下载挂起**: `/api/download/all` 与 `/api/download/db` 未设 `Content-Length`，HTTP/1.1 走 chunked 编码，部分下载工具（IDM 等）无法感知响应结束而一直挂起。修复：发送前显式设置 `headers.contentLength = bytes.length`。
- **连接状态**: dashboard 的 WebSocket 原本只在进入 Logs tab 时才建立、切 tab 即断开，导致页面顶部一直显示 Disconnected。修复：页面加载即建立 WebSocket 并全程保持（日志后台持续缓冲，进 Logs tab 直接渲染）；用户主动暂停后不再自动重连。

## 09:42 日志 Web 服务器新增 /api/download/all 一键打包下载

**文件**: `lib/src/logger/log_webserver.dart`, `assets/web/dashboard.html`, `pubspec.yaml`, `test/logger/log_webserver_test.dart`

- **新增端点** `GET /api/download/all`：将全部调试数据打包为单个 ZIP 附件（`safenotes_export_<时间戳>.zip`）一次性下载，成员包括：
  - `logs/`：全部日志文件（下载前先 flush；单文件读取失败以 `*.error.txt` 占位）
  - 数据库文件（`.db` 二进制）
  - `shared_preferences.json`：SP 快照
  - `journal.json`：Journal 转储
  - `diagnostics.txt`：同步诊断快照
  - Journal/诊断获取失败时以 `*.error.txt` 占位，不影响整体打包。
- **dashboard**: 原 "Download Data" + "Download Logs" 两个按钮合并为一个 "Download All"（走隐藏 iframe 触发，避免弹窗拦截）。
- **依赖**: `archive` 由传递依赖提升为直接依赖（^4.0.9，解析版本不变）。
- **测试**: 新增 `/api/download/all` 回归测试——断言 200、application/zip、ZIP 可解出且 `shared_preferences.json` 内容正确解码中文。

## 09:29 修复日志 Web 服务器下载端点中文内容编码错误

**文件**: `lib/src/logger/log_webserver.dart`, `test/logger/log_webserver_test.dart`（新增）

- **问题**: `GET /api/download/sp` 返回 500，报 `Invalid argument (string): Contains invalid characters.`。原因：`_sendAttachment` 用 `ContentType.parse('application/json')` 设置响应类型，无 charset 时 Dart `HttpResponse.write()` 默认按 Latin1 编码输出，SP 快照含中文标签（如 `"个人"`）即抛异常。
- **修复**: `_sendAttachment` 中当解析出的 ContentType 无 charset 时显式补 `charset: 'utf-8'`。
- **测试**: 新增 `test/logger/log_webserver_test.dart` 回归测试——mock SharedPreferences 含中文值，经原始 socket 请求 `/api/download/sp`，断言 200、UTF-8 charset 且 JSON 可正确解码中文。（用原始 socket 是因为 flutter test 环境会劫持 HttpClient）
- **验证**: `flutter analyze`（该文件无问题）、新增单测通过；core 包 348 项测试全部通过。

注：仓库中另有未提交的 note version 同步半成品改动（`packages/core/lib/src/db/database_handler.dart` 等），导致 `flutter test` 全量运行时部分用例编译失败，与本次修复无关，待用户完成其改动后再全量验证。
