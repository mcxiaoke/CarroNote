# 变更记录 2026-08-17

## 新增 make_square_yellow.py 并生成 icon_2 正方形黄底图标（2026-08-17 14:40）

文件：`scripts/make_square_yellow.py`、`temp/myicon/icon_2-standard v2-4x_square.png`

**需求**：Topaz 放大后的 `icon_2-standard v2-4x.png` 为 2304×2360（非正方形），需要做成正方形，
并把边缘白底用原图黄色背景填充。

**改动**：
- 新建 `scripts/make_square_yellow.py`：通过 scipy 连通域从图像边缘 flood-fill 找出页面白底，
  再做轻微膨胀消除圆角抗锯齿轮廓；将白底区域设为透明后贴到以原图较长边为边长、
  以图标主黄色填充的正方形画布上。
- 运行脚本生成 `temp/myicon/icon_2-standard v2-4x_square.png`（2360×2360）。

**验证**：输出为正方形，四角均为黄色 `(252, 220, 128)`，插画（胡萝卜、白色便签、黄色线条）
完整保留，无明显白边或浅色轮廓。

## 新增图标分离脚本 split_icons.py（2026-08-17 14:28）

文件：`scripts/split_icons.py`

**需求**：`temp/myicon/icon4in1.png` 为白底 2×2 图标合成图，需要自动分离为 4 张独立图标。

**改动**：
- 新建 `scripts/split_icons.py`：使用 Pillow 加载图片，numpy/scipy 做 8-连通域标记，
  自动识别 4 个非白前景图标主体并按左上/右上/左下/右下排序；支持 `--padding` 追加白边、
  `--threshold` 调整白色判定阈值、`-p` 设置输出前缀。
- 运行脚本生成 `temp/myicon/icon_1.png` ~ `icon_4.png`（四周各 20px 白边）。

**验证**：4 张输出图标尺寸合理、内容完整、顺序符合 2×2 布局。

## 移除登录页「密码错误次数锁定」功能（2026-08-17 14:21）

文件：`lib/views/authentication/login.dart`、`lib/data/preference_and_config.dart`、
`assets/translations/*.json`（27 个语言文件）

**需求**：尝试次数计数 `_noOfAllowedAttempts` 与锁定倒计时均为 State 纯内存字段，
进程/页面重启即重置，对「能重启进程」的攻击者无效；真正防线是 Argon2id 派生 + 服务端
限流。移除该功能以降低复杂度，无安全收益损失。

**改动**：
- `login.dart`：删除 `_isLocked`、`_noOfAllowedAttempts`、`_lockoutTime`、`_counter`、
  `_timer`、`_controller` 状态与 `_startTimer` / `_startLockoutTimer` / `_buildTimeOut`；
  `_onLoginFailure` 简化为只显示页内联「Wrong passphrase!」；移除输入框 / 登录按钮 /
  生物识别按钮的 `_isLocked` 禁用分支；dispose 中移除 Timer/StreamController 清理；
  移除 `services.dart` 多余 import（SystemChannels 已无引用）。
- `preference_and_config.dart`：删除 `noOfLogginAttemptAllowed`、`bruteforceLockOutTime`
  两个配置 key 与 getter（设置页无 UI 引用），配置快照日志同步精简。
- 翻译：27 个语言文件批量删除 3 个相关 key（`Number of attempt exceeded`、
  `Exceeded number of attempts...`、`Wrong passphrase {...} attempts left!`），
  保持 key 集一致（仅删行保留原始换行，Python 逐文件校验 JSON 合法）。

**验证**：`dart format` 无改动；`flutter analyze` 无 issue；`test/auth_flow_test.dart`
3 用例全过（错误密码用例 `textContaining('Wrong passphrase')` 兼容新文案）；
`flutter test` 35 全过；`flutter build windows --debug` 成功。
`dart test packages/core/test` 仅 `longrun_persistent_store_test.dart` 失败（长期存续
测试依赖跨运行真实 vault 目录，目标残留 manifest 冲突，与本次改动无关）。

## 登录页错误提示位置改为 label 行右侧 + 布局上移与键盘滚动修复（2026-08-17 13:20）

文件：`lib/views/authentication/login.dart`

**需求**：错误提示放在输入框下方会随有无错误改变输入框与按钮间距、且贴住输入框 outline，
布局不稳定；输入时登录按钮被软键盘遮挡。

**改动**：
- 删除 `_buildError()` 独立文案区，错误提示改渲染在密码输入框 `label` 行右侧（`label`
  用 `SizedBox(width: double.infinity)` 包 `Row(spaceBetween)`：左侧 "Passphrase"，右侧
  circleAlert 图标 + 错误文本，文本 `Flexible` + ellipsis 防溢出）。有/无错误不再改变
  输入框与按钮的垂直间距。
- 软键盘滚动修复：`_scrollController` 从外层 `CustomScrollView` 移到 `_buildLoginWorkflow`
  内部 `SingleChildScrollView`（外层 SliverFillRemaining 不可滚、maxScrollExtent 恒为 0，
  此前 `scrollToBottomIfOnScreenKeyboard` 实际无效，键盘弹出时按钮被遮挡）；外层
  CustomScrollView 不再绑定 controller。
- 整体上移：`_buildTopLogo` 顶部间距 24 → 8，输入框与提示区上移 16px。

**验证**：`flutter analyze lib/views/authentication/login.dart` 无 issue；
`test/auth_flow_test.dart` 3 用例全过。

## 登录页错误提示由全局 Toast 改为页内联文案（2026-08-17 12:50）

文件：`lib/views/authentication/login.dart`

**需求**：登录页提示（密码错误、剩余尝试次数、尝试超限锁定、远端网络不可达）不再使用
Toast；界面切换（登录成功进入 home）后提示必须立即消失，不得在 home 上方残留。

**问题根源**：`showErrorToast` 走 ShadSonner 全局浮层，其挂在 Navigator 之上（`app.dart`），
Toast 不随路由销毁；错误类 Toast 时长 6 秒，登录成功后 `pushReplacementNamed('/home')`
时旧 Toast 仍悬停在 home 上方。

**改动**：
- 新增状态 `String? _errorMessage`，及辅助方法 `_showError(msg)` / `_clearError()`，
  二者均带 `mounted` 守卫。
- 新增页内联错误文案 `_buildError()`：置于输入框下方（`_buildLoginWorkflow` Column 中
  `_inputField()` 之后），destructive 配色 + `LucideIcons.circleAlert` 小图标，无错误时
  返回 `SizedBox.shrink()` 保持布局稳定。提示 Widget 绑定登录页生命周期，路由切换即销毁。
- 4 处 `showErrorToast` 全部替换为 `_showError`：表单校验失败（Wrong passphrase!）、
  远端网络不可达、尝试次数超限锁定（Number of attempt exceeded）、密码错误剩余次数。
  锁定倒计时 `_buildTimeOut()`（本就为页内联）保持不变，与错误文案共存。
- 输入框新增 `onChanged: (_) => _clearError()`：用户重新输入时立即清除陈旧错误文案。
- 登录页布局修复：表单区域改由 `Expanded` 包裹（`_buildLoginWorkflow` 内部本就带
  `SingleChildScrollView`），原先 `Column + Spacer` 固定高度布局在错误提示动态增高表单时，
  窄视口（如测试 800x552）会 RenderFlex 溢出 17px；改为 Expanded 后表单可滚动兜底。
- 保留 `snack_message.dart` import：`_performLocalDataReset`（重置数据流程反馈）仍用
  `showSnackBarMessage` / `showErrorToast`，不在本次改动范围。

**验证**：`dart format` 通过；`flutter analyze lib/views/authentication/login.dart`
无 issue；`test/auth_flow_test.dart` 3 用例全过。
