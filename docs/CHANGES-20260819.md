# CHANGES 2026-08-19

## 阶段 2.1/2.2/2.4：首页置顶排序 + 卡片星标角标

### 修改
- `lib/views/home.dart` `_sortAndStoreNotes`（`home.dart:289`）叠加 pinned 维度：
  置顶笔记恒排最前，组内保持原时间序。置顶判定走 `NoteMeta.pinned`
  （`_metaCache`，不触发笔记解密，红线 5）。
- 四类外壳 widget（`note_card` / `note_tile` / `note_card_compact` /
  `note_tile_compact`）+ `note_card_body.dart` 新增置顶星标角标
  `_buildPinnedBadge`（`LucideIcons.star` 胶囊，`Key('ui-note-pinned-badge')`），
  `pinned` 状态透传各外壳。
- 修复角标裁切 + 换 pin 图标：初版 `Positioned(top:-4,right:-4)` 负偏移被
  ShadCard 圆角裁切成四分之一 → 改为标题行 Row 内嵌（Expanded 标题 + 角标），
  从布局上杜绝越界；图标由 `LucideIcons.star` 改为 `LucideIcons.pin`。
- `assets/translations/en-US.json` / `zh-CN.json` 补 `Pinned` key。
- 新增 `test/note_pinned_home_test.dart`：真实 App 层锁定「置顶恒最前 +
  星标角标」两契约（seed 三条不同 updatedAt 笔记，置顶最旧的）。
  角标完整可见性用 `cardRect.contains(badgeRect)` 断言 + 图标断言 `pin`。

### 验证
- `flutter analyze` → No issues；`dart format` + `import_sorter` 通过。
- `flutter test` → 49 通过（含新增 1 项）；`dart test packages/core/test` → 310 通过 + 1 skip。

## 测试环境重构：删除 FakeCrypto、initLightEnv 共享光标修复、settle 下线

### 修改
- `test/test_helpers.dart`：
  - 删除 `FakeCrypto`（假加密）与 `secureInitEnv`，认证集成测试全部改为
    真实 Keyring + 真实 SQLite；去掉冗余的 harness import（仅保留 export）。
  - 删除 `settle()`（有限时长 pump 辅助），下游改为 `pumpAndSettle`。
- `test/support/harness.dart`：
  - 新 `initLightEnv()`：普通 widget 测试的轻量环境。
  - 标题栏通道 mock 与 `EditableText.debugDeterministicCursor = true`
    （关闭光标 blink 周期调度帧）从全量环境下沉到 `initLightEnv`，
    普通测试也能用 `pumpAndSettle`。
- `test/auth_flow_test.dart`：删除 fake crypto 相关注释；三处 `settle()` 改为
  `pumpAndSettle`（依赖 initLightEnv 的 cursor 修复），并修正过时注释。
- `test/theme_color_setting_test.dart` / `test/note_actions_sheet_test.dart`：
  改用 `initLightEnv`。

### 验证
- `flutter analyze` → No issues。
- `flutter test`（48 用例）→ 全部通过，其中 `auth_flow_test` 跑在**真实加密**上。

## note_meta 数据层（阶段 1）实现与测试

> 设计文档：`docs/feature-note-meta-design.md` · 进度：`docs/PROGRESS-note-meta.md`

### 新增
- `packages/core/lib/src/models/note_meta.dart`
  - `NoteMeta` 模型 + `NoteMetaFields` + `tableNoteMeta` 常量。
  - payload 明文 JSON 序列化（`encodePayload`/`decodePayload`，容错不抛），
    `extra` 顶层键透传（前向兼容 + 零成本扩展）。
  - `normalizeTags`（去空白/丢空串/去重）、`copyWith`、`toRow`/`fromRow`、
    `defaults(uuid)`。调试 `toString` 不打印 tags / payload 内容。
- `packages/core/test/db/note_meta_test.dart`：14 项单测全通过。

### 修改
- `packages/core/lib/core.dart`：导出 `note_meta.dart`。
- `packages/core/lib/src/db/database_handler.dart`：
  - 抽出 `_schemaVersion = 5`；`_onUpgrade` 加 `oldVersion < 5` 分支
    （`CREATE TABLE IF NOT EXISTS note_meta`，不碰 notes 表）。
  - `_createNoteMetaTable` **单一建表副本**接入 `_createDBStatic` /
    `_createDB` / `_onUpgrade` 三处（从结构上消灭漏改风险）。
  - `_metaCache`（`Map<String,NoteMeta>`）与 `_notesCache` 完全隔离；
    `_invalidateCache` 一并清理；切 pinned 零笔记解密。
  - meta CRUD：`readAllNoteMeta` / `getNoteMeta` / `upsertNoteMeta` /
    `setNotePinned` / `setNoteTags` / `readAllTags` + 事务版墓碑
    `_markNoteMetaDeletedInTxn`（顺带擦除 payload，防标签明文残留）。
  - 硬删除三处（`hardDelete` / `hardDeleteByUuid` / `hardDeleteAllDeleted`）
    事务内写墓碑 + 事务后清缓存。
  - `reEncryptAllNotes` 同步重加密 `note_meta.payload`（新增
    `_readMetaPayloadsPlain`），修复 dataKey 轮换后标签永久无法解密的隐患。
  - payload 加入 `_inspectorHiddenColumns`（不打印密文）。

### 验证
- `flutter analyze packages/core` → No issues。
- `dart test packages/core/test/db/note_meta_test.dart` → 13 通过 + 1 skip
  （A 内存 CRUD / B 合成 v4→v5 / C 硬删墓碑 / D reEncrypt 重加密）。
- E 组「真实库迁移」为可选测试，需环境变量 `SAFENOTES_TEST_DB` +
  `SAFENOTES_TEST_PASSWORD` 指向真实库副本；已在本机对一份 v4 真实库
  （106 条笔记，含回收站）实测通过：升级成功、全部笔记可解密、meta CRUD 可用。
  未设置环境变量时自动 skip，仓库内不保存任何路径与口令。

### 红线核对
- 未触碰 `SafeNote.computeHash` / `toContentBytes` / `fromContentBytes` / blob AAD。
- 元数据变更只动 `note_meta.updated_at`，不改 `notes.updated_at` / `content_hash`。
- note_meta 无 `REFERENCES notes ... CASCADE`（墓碑须在 notes 行消失后存活）。
