# 变更日志 (2026-08-02)

## [2026-08-02 17:46:52] - 加密栈迁移 cryptography_flutter：登录/列表解密提速（硬件加速）
- **背景（性能目标）**：登录慢（PBKDF2-HMAC-SHA256 200k 迭代）与笔记列表解密慢（N 条 × 2 次 AES-GCM）。根因是纯 Dart pointycastle 实现无硬件加速（Android debug 下单次 PBKDF2 3-5 秒）。迁移至 `cryptography` + `cryptography_flutter` 后 AES-GCM / PBKDF2 走平台原生实现，Windows/Linux 自动回退后台 isolate 纯 Dart（不阻塞 UI）。
- **依赖**：`pubspec.yaml` 新增 `cryptography: ^2.9.0`、`cryptography_flutter: ^2.3.4`；`lib/encryption/aes_encryption.dart` 保留 pointycastle（旧备份导入的 AES-CBC 路径）。
- **互操作验证**：临时脚本 `temp/crypto_interop_check.dart` 验证 PBKDF2-200k 输出一致、AES-GCM 双向互操作、信封 68B = 12(nonce)+40(ct)+16(tag) → **存量 vault 零重加密**，AAD（blob=内容 hash / manifest-items / journal / datakey-wrap）与信封格式不变。
- **`lib/sync/crypto.dart` 异步化**：`deriveMasterKey` / `wrapDataKey` / `unwrapDataKey` / `seal` / `open` 改为返回 `Future<Uint8List>`（cryptography 的 `extractBytes`/`decrypt` 返回 `List<int>`，已 `Uint8List.fromList` 包装）；`_aesGcmDecrypt` 统一把 `Object` 包装为 `SyncDecryptionException`。
- **调用链异步化**：`lib/data/database_handler.dart`（`_encryptField`/`_decryptField`/`_toEncryptedRow`/`_fromEncryptedRow` 异步、3 处 `.map` 改 `Future.wait`、8 处调用点）、`lib/sync/keyring.dart`（`_unwrapOrThrow` 异步、`checkMigrationNeeded` → `Future<MigrationResult>`）、`lib/sync/sync_models.dart`（`ManifestCrypto.serialize/deserialize` → `Future`）、`lib/sync/journal.dart`（3 处）、`lib/sync/sync_engine.dart`（约 12 处）。
- **main.dart**：移除已废弃的 `FlutterCryptography.enable()`（cryptography_flutter 2.3.4 插件自动启用），保留 `_bootstrap()`。
- **测试适配**：同步测试补齐 `await`/`async`，`expect(() => await ...)` 改 `expectLater`；`makeTestKeyring` 默认 encryptedDataKey 改用同步假信封（60B 固定字节，语义不变：字符串相等即无需迁移）；`sync_engine_test.dart` FakeBackend 的 `onBeforePutManifestWrite` 回调改为 `Future<void> Function()` 并被 `await`——修复异步化后 fire-and-forget 回调的时序不确定性（编辑落盘可能晚于 `_updateLocalState` 白名单读取，导致 P1-A 误标）。
- **验证**：`flutter analyze` 0 error（4 个 info 为既有 lint，`path_provider_platform_interface` 修复需改 pubspec 故保留）；`crypto_test` 26 项、同步本地套件 94 项、SafeServer 集成 27 项、WebDAV 集成 38 项、journal/widget/encryption 全通过。

## [2026-08-02 17:11:29] - 修复：scenario-b（他端改密码）UI 未强制登出/无提示，同步静默失败
- **背景（日志实证）**：Windows 端改密码后（keyVersion 1→2），Android 端同步已正确检测到 scenario-b（`sync_engine.dart` 打印「他端改了密码…中止同步强制重登录」并返回 failure），但 **UI 无任何提示、不强制登出**，用户还能继续新建笔记，而新建的笔记每次同步都被中止、永远无法上传（零写入）。
- **根因**：v4（epoch 消除 §8.2[I] 选项 B）把 scenario-b 从「继续同步 + passwordEpochMismatch 标志」改为「中止 + failure errorMessage」，但 `home.dart` 的 B4 弹窗仍依赖已被引擎废弃的 `passwordEpochMismatch` 标志（现在恒为 false）→ 弹窗逻辑永不触发，错误只在日志里可见。
- **修复**：
  - `lib/sync/sync_models.dart`：`SyncResult` 新增 `requiresRelogin` 标志（默认 false；`SyncResult.failure` 工厂新增 `requiresRelogin` 参数；`copyWith`/`toString` 同步）；`SyncDiagnosticsSnapshot` 新增 `lastResultRequiresRelogin`。
  - `lib/sync/sync_engine.dart`：scenario-b（他端改密码，本地 MK 解不开远端包裹）与场景 c（密码不匹配）两个「必须重新登录」的分支返回 `SyncResult.failure(..., requiresRelogin: true)`。
  - `lib/views/home.dart`：B4 弹窗改由 `state.lastResult?.requiresRelogin` 触发；弹窗改为**强制**（`barrierDismissible: false` + `PopScope(canPop: false)`，不可点击遮罩/返回键跳过），仅保留「退出并重新登录」按钮；文案修正为「尚未同步的更改已保留在本地」（v4 选项 B 下同步被中止，不再声称"已正常同步"）。
  - `lib/sync/sync_service.dart`：诊断快照填充 `lastResultRequiresRelogin`；`lib/views/settings/sync_diagnostics_page.dart`：展示「需要重新登录」。
- **验证**：`flutter analyze` 无新增告警；`flutter test` 相关测试全过——`change_password_multi_client_test.dart` + `multi_device_test.dart`（20 项）、`sync_engine_test.dart` + `test/encryption`（33 项）、`chaos_multi_client_test.dart`（12 项）。

## [2026-08-02 16:12:53] - 统一孤儿 blob 隔离区位置为 vault 根下 blobs-orphan（WebDAV 对齐 localFs/SafeServer）
- **背景**：此前三套后端隔离区位置不一致——LocalFS / SafeServer（Go/Node 服务端实际实现）都是 `blobs-orphan/` 与 `blobs/` 同级，唯独 WebDAV 后端把隔离区建在 `blobs/blobs-orphan/`（子目录）；而 `server-api-spec.md` / `server-implementation.md` 又按 WebDAV 的子目录布局描述服务端实现，与服务端实际行为矛盾。
- **WebDAV 客户端**（`lib/sync/webdav_backend.dart`）：新增 `_orphanUrl => '$baseUrl/blobs-orphan'`，`deleteBlobSoft` / `listOrphanBlobs` / `purgeOrphans` 的隔离区 URL 从 `$_blobsUrl/blobs-orphan` 改为 `$_orphanUrl`。隔离区不再属于 `blobs/` 命名空间，避免 `listBlobs` 枚举污染。
- **文档修正**：`server-api-spec.md` §2 补充、§9.1 layout 树、§9.8 要点、`server-implementation.md` §5.2 布局——统一描述为 `blobs-orphan/` 与 `blobs/` 同级。
- **服务端注释**：Go `fs.go` / Node `fs.js` 的 `ListBlobs` 注释及 Node 存储布局头注释同步修正（明确隔离区为兄弟目录，非子目录）。
- **注**：存量 WebDAV 服务器上若已存在 `blobs/blobs-orphan/`，需手动把内容移到 vault 根下 `blobs-orphan/`。
- **验证**：`flutter analyze` 无新增告警；`flutter test test/encryption`、`test/sync/sync_engine_test.dart`（24 项）、`test/sync/webdav_integration_test.dart`（38 项）全部通过。

## [2026-08-02 16:00:19] - 重构：解密缓存轻量收敛（DAO 内收口 + 实例字段）
- **背景**：此前缓存维护散落在每个写路径（`storeNote`/`updateNote`/`softDelete`/`restoreNote`/`hardDelete`/`hardDeleteByUuid` 各自调 `_upsertCacheEntry`/`_patchCacheEntryById`/`_removeCacheEntryById`/`_removeCacheEntryByUuid`，`markSynced*` 三处内联重建），易漏改字段（已踩过 updatedAt 导致远端墓碑未生效的坑）。
- **收口为 4 个私有方法**：`_invalidateCache` / `_upsertCacheEntry`（按 uuid 整条覆盖）/ `_removeCacheEntry({id,uuid})` / `_applySyncedToCache({uuids,exclude})`（合并 markSynced 全系列）。删除 `_markCachedSynced` / `_patchCacheEntryById` / `_removeCacheEntryById` / `_removeCacheEntryByUuid` 及 markSynced* 内联重建。每个写路径只调其中之一。
- **结构杜绝漏改字段**：`softDelete`/`restoreNote` 改为「取缓存旧条目 → 叠加增量重建完整对象 → 整条 `_upsertCacheEntry` 覆盖」，updatedAt 随对象带入，写路径无法遗漏 → 从根上消除同步 LWW 时间戳误判。
- **`_notesCache` 由 `static` 改为实例字段**：去掉多 DB 实例（测试反复 setDatabaseForTesting 建新库）共享同一份缓存的隐性耦合；`setDatabaseForTesting` 改为 `instance._notesCache = null` 清空。`NotesDatabase` 仍是缓存持有者，UI / SyncEngine 调用点不变（最小改动面）。
- **验证**：`flutter test` 全量 250 项通过；`flutter analyze` 无告警。

## [2026-08-02 15:43:40] - 修复：缓存手术式更新遗漏 updatedAt，导致远端墓碑未生效（同步回归）
- **根因**：P1 的"手术式缓存更新"把 `softDelete` / `restoreNote` 改为调用 `_patchCacheEntryById`，但该方法只改 `deleted`/`synced`，**未同步 DB 里写入的新 `updatedAt`**。缓存条目保留旧时间戳，使同步引擎 `_buildLocalManifest` 读到过期 `updatedAt`，远端墓碑的 LWW 时间判定被打平（本地时间 == 远端时间），平局回退选本地 → 远端墓碑被忽略。
- **修复**：`_patchCacheEntryById` 新增 `updatedAt` 形参，并在 `softDelete` / `restoreNote` 调用处传入真实的 `now`，保证缓存与 DB 时间戳一致。等价于回归前 `_invalidateCache()`（每次都从 DB 重读）的行为，但保留零重解密收益。
- **清理**：移除诊断用的 `print('DBG ...')` 临时调试语句（`database_handler.dart` 的 `updateNoteByUuid`、`sync_engine.dart` 的 `_resolveConflict` / `_downloadNote`）。
- **验证**：`flutter test test/sync/sync_engine_test.dart` 24 项全过（含此前失败的"远端墓碑 → 本地也标记为软删除"）；`flutter test` 全量 250 项通过；`flutter analyze` 无告警。

## [2026-08-02 15:16:10] - P1 性能优化：解密结果缓存，消除主线程反复全量解密卡顿
- **根因**：`readAllNotes` 每次都同步解密全部 41 条笔记（AES-GCM，~2s/次），且 autoSync 每 ~3s 触发 `readAllNotesIncludingDeleted` ×2~3 + 同步完成后再 `refreshNotes` 一次，导致主界面周期性 2 秒冻结（日志实证）。P0 只修了渲染，未触碰解密。
- **新增解密缓存** `NotesDatabase._notesCache`（含墓碑的全量明文），`readAllNotes` / `readAllNotesIncludingDeleted` 命中缓存直接返回，跳过解密。
- **失效策略**：仅「内容写」失效（`storeNote` / `updateNote` / `updateNoteByUuid` / `softDelete` / `hardDelete` / `hardDeleteByUuid` / `restoreNote` / `reEncryptAllNotes*`）；`markSynced*` 系列**不**失效，而是直接刷新缓存条目的 `synced`/`syncedHash`/`syncedDeleted` 标记（正文不变，无需重解密），保证同步后的 `refreshNotes` 在内容无变化时零解密。
- **生命周期**：`clearDataKey` / `close` / `deleteDbFile` / `setDatabaseForTesting` / 重新打开数据库连接时统一清空缓存，避免跨会话或跨测试命中旧明文。
- **正确性验证**：同步引擎只用 `contentHash`/`deleted`/`updatedAt` 决定上传与收敛，不依赖缓存里的 `synced` 标记；`markSynced*` 已同步刷新缓存标记，现有断言 `note.synced==true` 的测试不受影响。

## [2026-08-02 15:05:33] - P0 性能优化：卡片用摘要文本 + 禁用 RTL 检测
- **新增 `SafeNote.abstractText`**：取正文前 200 字符（`abstractMaxLength`），用于列表卡片展示，避免把整篇大文本送入排版引擎。
- **四个卡片 widget**（`note_card` / `note_tile` / `note_card_compact` / `note_tile_compact`）改用 `note.abstractText` 替代完整 `note.description`。
- **禁用 RTL 检测**：`getTextDirecton` / `isRTL` 现恒返回 LTR，移除 `intl` 依赖。暂时不支持 RTL 排版，消除对每条正文做 Bidi 全文扫描的开销（根因 3）。

## [2026-08-02 14:22:41] - Makefile 新增 debug 构建目标（apk / exe / all）
- **新增 `apk` 目标**: `flutter build apk --debug`，构建 Android 调试版 APK（前置先注入构建信息）。
- **新增 `exe` 目标**: `flutter build windows --debug`，构建 Windows 桌面端调试版可执行文件。
- **新增 `all` 目标**: 按依赖顺序依次执行 `apk` + `exe`，一次产出 Windows + Android 的调试版二进制。
- **更新 `.PHONY`**: 登记 `apk` / `exe` / `all`，避免与目标同名文件冲突；默认仅覆盖 Windows + Android 两个平台。

## [2026-08-02 12:22:54] - 构建信息注入（Git 提交哈希 + 构建时间）与启动版本详情日志

- **新增 `scripts/generate_build_info.py`（构建期元数据生成器）**:
  - 从 `pubspec.yaml` 解析 `version: x.y.z+n`，调用 git 提取完整/短提交哈希、分支名、`git describe` 标签、累计提交数、工作区是否脏，并取本地与 UTC 构建时间。
  - 生成 `lib/utils/build_info.dart`（仅内容变化时才写文件，避免无谓重编译）；git 不可用（非仓库/无历史）时优雅降级为 `unknown`，不阻断构建。仅用 Python 标准库，跨平台。
- **新增 `lib/utils/build_info.dart`（生成文件，编译期常量 `BuildInfo`）**:
  - 字段：`version` / `buildNumber` / `versionString` / `gitHash` / `gitHashShort` / `gitBranch` / `gitTag` / `gitCommitCount` / `gitDirty` / `buildDate`(UTC) / `buildDateReadable`。
  - 便捷 getter：`summary`（单行，供启动日志）、`detail`（多行，供关于/调试面板）。文件始终存在，直接 `flutter` 命令亦可正常编译。
- **`lib/main.dart` `_initLogging()` 启动版本详情日志**:
  - 打印版本号、Git 短哈希+分支+脏标记、完整 commit、tag+累计提交、本地/UTC 构建时间、平台、Dart 版本、日志目录。
- **`Makefile` 构建流程接入**:
  - 新增 `gen-build-info` 目标；`get`/`analyze`/`test`/`run`/`build-apk`/`build-aab`/`build-windows`/`build-linux`/`build-macos`/`release` 均先执行生成器，确保每次运行/构建注入最新信息。
- **`scripts/release.py` 发布打包前注入**:
  - 在 `make_release()` 进入多轮 `clean`/`build` 前调用 `generate_build_info.py`，保证发布的 APK/AppBundle 内嵌准确 Git/构建时间。
- **约束**: 未引入任何新第三方依赖；`SafeNotesConfig` 运行时版本逻辑（`appVersionCode` 升级检测）保持不变，`BuildInfo` 仅作为构建元数据来源。

## [2026-08-02 12:10:22] - P3 低风险优化（blob 重试 / autoSync 互斥 / journal 事件 / 关键日志）

- **P3-c journal 事件扩展（commit `e4147ad`）**:
  - `lib/sync/journal.dart`：`JournalEventType` 新增 `syncManifestPut`（manifest PUT 成功，区分正常合并/损坏重建/repair 三路径）与 `syncOptimisticLockRetry`（ETag 不匹配触发的回退重试）两个事件类型，便于诊断「频繁冲突重试」「manifest 落地失败」类问题。
- **P3-a blob 重试退避 + P3-c 调用 + P3-log 同步引擎日志（commit `e9e6a79`）**:
  - `lib/sync/sync_engine.dart`：新增私有 helper `_withBlobRetry<T>`，对 `backend.putBlob`/`getBlob` 做指数退避重试（仅对 `BackendUnavailableException` 重试，2 次重试 / 200ms-400ms 退避；`getBlob` null 不重试，`putBlob` 幂等覆盖写安全）。替换 4 个 blob 操作调用点。
  - `lib/sync/sync_engine.dart`：三条 PUT manifest 路径（正常合并/损坏重建/repair）补 `syncManifestPut(done)` journal 落地；乐观锁冲突 catch 补 `syncOptimisticLockRetry` journal。
  - `lib/sync/sync_engine.dart`：P3-log 关键日志补全——`_gcOrphanBlobs` 入口/隔离/purge/三处异常（原静默吞掉改为 warn），`_updateLocalState` 落地结果（version/converged/skipped/purged），`_executeMigration`/`_executeMigrationVault` 入口/出口/失败，manifest GET 结果（debug）。
- **P3-b autoSync 互斥增强 + P3-log 生命周期日志（commit `5b52525`）**:
  - `lib/sync/sync_service.dart` `autoSync()`：race 修复——`then()` 回调内 re-schedule 前检查 `_autoSyncTimer?.isActive`，避免覆盖用户在 sync 期间新触发的 debounce timer；失败可重试——sync 返回 `success=false` 时排程 1 次重试，用 `_autoSyncFailureRetried` 标志位防死循环。
  - `lib/sync/sync_service.dart`：`dispose()`/`logout()` 清零 `_autoSyncFailureRetried` 避免状态泄漏。
  - `lib/sync/sync_service.dart`：P3-log 生命周期关键日志——`initialize` 整体完成、`updateKeyring` 完成（keyVersion 切换 + SyncEngine 重建状态）、`switchBackend` 拒绝（warn）+ 完成（含 SyncEngine 重建状态）。
- **P0-B 测试适配（commit `8a22af3`）**:
  - `test/sync/webdav_integration_test.dart`：「Layer 2b 持有明文时下载失败自愈重传」用例 P0-B 提交后即失败（非 P3 引入）。P0-B 修复后该场景走 fast-forward 单边本地变更，不进入 `_handleDownloadFailure`，因此不产生 heal 动作。测试断言更新为期望 `upload` 动作、不期望 `heal` 动作，注释说明 P0-B 前后路径差异。
- **测试**: sync 全套 248 tests passed / 2 skipped / 0 failed；`flutter analyze lib/sync lib/models lib/data` No issues found。
- **文档**: `docs/conflict-analysis-20260802.md` 追加 §10 P3 实施总结（含影响面分析与关键不变量核查）。
- **约束**: P3-a 仅对 `BackendUnavailableException` 重试，其他异常直接抛出，上层 catch 行为不变；P3-b 失败重试限制最多 1 次避免死循环；P3-c/P3-log 纯日志不改变任何控制流。

## [2026-08-02 11:49:38] - 单客户端同步误报 conflict 修复 + 同步期写入竞态修复

- **P0-B 单边变更走 fast-forward（commit `2b497f8`）**:
  - `lib/models/safenote.dart`：新增 `syncedDeleted` 字段，与 `syncedHash` 共同构成三方合并 base 的 `(hash, deleted)` 二元组（softDelete 不改 content_hash，单独比 hash 无法识别本地删除为变更）。
  - `lib/data/database_handler.dart`：schema v3→v4，`ALTER TABLE` 增加 `synced_deleted` 列；`_onUpgrade` 老库迁移；`markSynced`/`markAllSynced`/`markAllSyncedExcept` 同步刷新 `syncedDeleted`。
  - `lib/sync/sync_engine.dart` `_mergeAndTransfer`：`else` 分支三分流——本地单边/远端单边走 fast-forward（不记 conflict），双方都偏离 base 或 base=null 走真冲突（LWW + 副本保留）。
  - `test/sync/sync_engine_test.dart`：新增 fast-forward 单边变更 group（3 case）+ 真双方冲突测试，修正原 LWW 测试过时断言。
- **P1-A `_updateLocalState` 白名单 markSynced（commit `ca37f7b`）**:
  - `lib/data/database_handler.dart`：新增 `markSyncedForUuids(Set<String>)` 白名单方法。
  - `lib/sync/sync_engine.dart` `_updateLocalState`：改为读全量本地笔记逐条比对「当前 (hash, deleted) == merged.items[uuid]」才 markSynced，同步期间被编辑/新建的笔记跳过，避免 base 污染导致下次同步丢数据。
  - `test/sync/sync_engine_test.dart`：新增 P1-A 同步期写入竞态 group（3 case），FakeBackend 加 `onBeforePutManifestWrite` 钩子注入竞态窗口。
- **测试**: sync_engine_test 24 tests / multi_device + p0p1_self_heal 47 tests passed；`flutter analyze` No issues found。
- **文档**: `docs/conflict-analysis-20260802.md` 追加 §9 修复实施总结（含影响面分析与 P2/P3 未实施项说明）。

## [2026-08-02 10:44:51] - 变更概要
- **补全启动/生命周期/界面切换日志 (Task #1)**:
  - `lib/authwall.dart`：启动时记录路由决策（登录页 vs 设置密码页，依据 `AppBootState.vaultInitialized`）。
  - `lib/routes/route_generator.dart`：集中记录每一次路由解析（debug 级）及路由解析失败（warning 级）。
  - `lib/utils/lifecycle_handler.dart`：记录全部 OS 生命周期状态切换（resumed/inactive/paused/detached/hidden；detached 退出路径提升为 info）。
- **补全备份/导入导出日志 (Task #6)**:
  - `lib/views/settings/backup_setting.dart`：记录用户触发手动备份、打开 iOS 备份目录。
  - `lib/dialogs/backup_import.dart`：记录用户从导入对话框确认并开始选择备份文件。
  - 说明：`scheduled_task.dart` / `file_handler.dart` 的备份条数、目标路径、字节数日志已于前序轮次完成。
- **补全设置变更与同步配置日志 (Task #7)**:
  - `lib/sync/sync_config.dart`：为全部同步配置 setter 增加日志——后端类型、本地目录、WebDAV URL/用户名/密码（仅长度）、SafeServer URL/Token（仅长度）、自动同步开关。
  - **隐私红线**：WebDAV 密码与 SafeServer Token 仅记录长度/状态，绝不记录明文。
- **补全同步子系统细节日志 (Task #8)**:
  - `lib/sync/sync_service.dart`：记录 autoSync 排程、切换后端、登录密钥环准备、启用同步初始化等入口动作。
  - `lib/views/settings/sync_settings.dart`：记录用户从设置页触发手动同步与「修复同步数据」。
  - 说明：`sync_engine.dart` / `keyring.dart` 的逐条笔记上传/下载/删除/冲突/迁移及密钥环日志已于前序轮次完成。
- **约束**：全部为纯日志调用（Log.xxx）、计时 Stopwatch 与埋点，未改动任何业务逻辑、控制流、分支判断或行为；私密数据（密码/密钥/笔记明文）严格脱敏（仅 uuid/长度/哈希前缀/密钥指纹前 8 位/keyVersion/dataKeyEpoch）。

## [2026-08-02 09:06:23] - 变更概要

- **Windows 数据库路径修复**: 在 `lib/main.dart` 桌面初始化块中，针对 Windows 在 `databaseFactory = databaseFactoryFfi` 之后、首次访问数据库之前，显式调用 `databaseFactory.setDatabasesPath(await getApplicationSupportDirectory())`，将 sqflite FFI 默认的「相对 CWD 路径 `.dart_tool/sqflite_common_ffi/databases`」覆盖为应用支持目录 `%APPDATA%\<app>`。
  - 解决数据库位置随启动目录（exe 所在目录）漂移的问题；避免打包到 `Program Files` 后因目录只读而无法建库。
  - 新增 `package:path_provider/path_provider.dart` 导入。
  - 注意：本次仅覆盖 Windows（`if (Platform.isWindows)`）；macOS / Linux 仍为 FFI 默认的相对 CWD 路径，如需统一可放宽判断条件。
  - 配合更新 `docs/STORAGE-PATHS-RESEARCH-20260802.md` §2 / §7 的 Windows 路径说明。

