# 变更日志 (2026-08-02)

## [2026-08-02 11:49:38] - 单客户端同步误报 conflict 修复 + 同步期写入竞态修复

- **P0-B 单边变更走 fast-forward（commit `2b497f8`）**:
  - `lib/models/safenote.dart`：新增 `syncedDeleted` 字段，与 `syncedHash` 共同构成三方合并 base 的 `(hash, deleted)` 二元组（softDelete 不改 content_hash，单独比 hash 无法识别本地删除为变更）。
  - `lib/data/database_handler.dart`：schema v3→v4，`ALTER TABLE` 增加 `synced_deleted` 列；`_onUpgrade` 老库迁移；`markSynced`/`markAllSynced`/`markAllSyncedExcept` 同步刷新 `syncedDeleted`。
  - `lib/sync/sync_engine.dart` `_mergeAndTransfer`：`else` 分支三分流——本地单边/远端单边走 fast-forward（不记 conflict），双方都偏离 base 或 base=null 走真冲突（LWW + 副本保留）。
  - `test/sync/sync_engine_test.dart`：新增 fast-forward 单边变更 group（3 case）+ 真双方冲突测试，修正原 LWW 测试过时断言。
- **P1-A `_updateLocalState` 白名单 markSynced（commit `ca37f7b`）**:
  - `lib/data/database_handler.dart`：新增 `markSyncedForUuids(Set<String>)` 白名单方法。
  - `lib/sync/sync_engine.dart` `_updateLocalState`：改为读全量本地笔记逐条比对「当前 (hash, deleted) == merged.items[uuid]」才 markSynced，同步期间被编辑/新建的笔记跳过，避免 base 污染导致下次同步丢数据。
  - `test/sync/sync_engine_test.dart`：新增 P1-A 同步期写入竞态 group（3 case），FakeBackend 加 `onBeforePutManifestWrite` 钩子注入竞态窗口。
- **测试**: sync_engine_test 24 tests / multi_device + p0p1_self_heal 47 tests passed；`flutter analyze` No issues found。
- **文档**: `docs/conflict-analysis-20260802.md` 追加 §9 修复实施总结（含影响面分析与 P2/P3 未实施项说明）。

## [2026-08-02 10:44:51] - 变更概要
- **补全启动/生命周期/界面切换日志 (Task #1)**:
  - `lib/authwall.dart`：启动时记录路由决策（登录页 vs 设置密码页，依据 `AppBootState.vaultInitialized`）。
  - `lib/routes/route_generator.dart`：集中记录每一次路由解析（debug 级）及路由解析失败（warning 级）。
  - `lib/utils/lifecycle_handler.dart`：记录全部 OS 生命周期状态切换（resumed/inactive/paused/detached/hidden；detached 退出路径提升为 info）。
- **补全备份/导入导出日志 (Task #6)**:
  - `lib/views/settings/backup_setting.dart`：记录用户触发手动备份、打开 iOS 备份目录。
  - `lib/dialogs/backup_import.dart`：记录用户从导入对话框确认并开始选择备份文件。
  - 说明：`scheduled_task.dart` / `file_handler.dart` 的备份条数、目标路径、字节数日志已于前序轮次完成。
- **补全设置变更与同步配置日志 (Task #7)**:
  - `lib/sync/sync_config.dart`：为全部同步配置 setter 增加日志——后端类型、本地目录、WebDAV URL/用户名/密码（仅长度）、SafeServer URL/Token（仅长度）、自动同步开关。
  - **隐私红线**：WebDAV 密码与 SafeServer Token 仅记录长度/状态，绝不记录明文。
- **补全同步子系统细节日志 (Task #8)**:
  - `lib/sync/sync_service.dart`：记录 autoSync 排程、切换后端、登录密钥环准备、启用同步初始化等入口动作。
  - `lib/views/settings/sync_settings.dart`：记录用户从设置页触发手动同步与「修复同步数据」。
  - 说明：`sync_engine.dart` / `keyring.dart` 的逐条笔记上传/下载/删除/冲突/迁移及密钥环日志已于前序轮次完成。
- **约束**：全部为纯日志调用（Log.xxx）、计时 Stopwatch 与埋点，未改动任何业务逻辑、控制流、分支判断或行为；私密数据（密码/密钥/笔记明文）严格脱敏（仅 uuid/长度/哈希前缀/密钥指纹前 8 位/keyVersion/dataKeyEpoch）。

## [2026-08-02 09:06:23] - 变更概要

- **Windows 数据库路径修复**: 在 `lib/main.dart` 桌面初始化块中，针对 Windows 在 `databaseFactory = databaseFactoryFfi` 之后、首次访问数据库之前，显式调用 `databaseFactory.setDatabasesPath(await getApplicationSupportDirectory())`，将 sqflite FFI 默认的「相对 CWD 路径 `.dart_tool/sqflite_common_ffi/databases`」覆盖为应用支持目录 `%APPDATA%\<app>`。
  - 解决数据库位置随启动目录（exe 所在目录）漂移的问题；避免打包到 `Program Files` 后因目录只读而无法建库。
  - 新增 `package:path_provider/path_provider.dart` 导入。
  - 注意：本次仅覆盖 Windows（`if (Platform.isWindows)`）；macOS / Linux 仍为 FFI 默认的相对 CWD 路径，如需统一可放宽判断条件。
  - 配合更新 `docs/STORAGE-PATHS-RESEARCH-20260802.md` §2 / §7 的 Windows 路径说明。

