# 变更摘要 2026-08-29

> 本文件按项目规范记录当日关键代码变更，最新在顶部。

## 2026-08-29 22:40 评审第一批修复（P0-3 / P0-7 / P1-3 / P1-11 / P1-12 / P1-14 / P2-30）

> 依据：`docs/code-review-20260829.md` + 三份复核，综合裁定后实施第一批（A1–A7）。
> 完整优先级列表与修复计划见 `docs/fix-plan-20260829.md`。
> 关键裁定：P0-1（墓碑 GC）经源码验证为误报（`hardDeleteByUuid` 同事务已写
> purged_uuids）；P0-2（读路径删坏行）维持 M1 既定设计不改动。

- **P1-3 / A1**：`_inspectorHiddenColumns` 增加 `'value'`——`sync_meta.value`
  含 keyring JSON（encryptedDataKey + KDF salt + iterations），DB Inspector
  原样返回可离线爆破（LAN 日志服务可达）。与 `inspectMetadata` 脱敏口径对齐。
- **P0-3 / A2**：8 处零守卫写路径补 `_checkNotMigrating()`：
  `upsertNoteMeta`（payload 旧 key 密文风险，最关键）、`softDelete`、`hardDelete`、
  `hardDeleteByUuid`、`restoreNote`、`markSynced` / `markAllSynced` /
  `markAllSyncedExcept` / `markSyncedForUuids`（迁移快照整行覆盖风险）。
  新增 `@visibleForTesting set migratingForTesting` 测试钩子。串行锁根治方案
  留待后续评估（第二批后）。
- **P0-7 / A3**：`NoteEditorState.addOrUpdateNote` 返回值从 `SafeNote?` 改为
  `({SafeNote? saved, bool skipped})`，防重入跳过不再与「无改动」混淆；
  新增 `waitForSave()` / `isSaving`。`add_edit_note.dart _performAutoSave` 被
  跳过时等待在途保存完成后重试一次（用最新输入），重试仍被跳过则视为失败
  留在页面（H4 路径）——退出时最新输入不再静默丢失。
- **P1-11 / A4**：`http_util.dart` 响应体读取循环包 `Stream.timeout`（防
  「先回 200 头再慢吐 body」永久卡死）；`sync_service.dart` `engine.sync()`
  加 10 分钟总超时（超时抛 `TimeoutException` → 复位 `_syncInProgress`，
  互斥锁不再永久持有，下轮同步可自愈）。
- **P1-14 / A5**：`SafeServerBackend.backupCorruptManifest` 只认 204/404；
  其余状态码（5xx/401/403/405 等）抛 `BackendUnavailableException` 中止，
  删除原「move 失败退化为 DELETE manifest」兜底——旧服务端 405 或瞬时 500
  曾可直接物理删除远端 manifest。
- **P1-12 / A6**：`SyncBackendDraft.normalized()` 剥离 URL 内嵌 userinfo
  （`https://user:pass@host/`），抽出填入**为空**的 username/password（显式
  字段优先）；`setWebdavUrl` / `setSafeServerUrl` 日志统一走
  `_redactUrlForLog`（掩 userinfo + query）；WebDAV `_mkcol` 异常只拼
  `scheme://host[:port]` 且响应体截断 200 字符。密码不再明文常驻日志。
- **P2-30 / A7**：`Taskfile.yml` `gen-build-info` 从不存在的
  `python scripts/generate_build_info.py` 改为 `dart run scripts/generate_build_info.dart`
  （与上午 release.py/Makefile/CI 的同类修复对齐），`task get/run/build-*` 恢复可用。

测试与验证：
- 新增回归：`database_migration_rollback_test.dart`（P1-3 value 列隐藏 +
  P0-3 迁移窗口 8 写路径守卫）、`editor_state_guard_test.dart`（P0-7 跳过/等待/
  重试闭环，适配新返回类型）、`sync_config_test.dart`（P1-12 userinfo 剥离三例）。
- `flutter analyze lib test packages/core`：No issues found。
- `dart test packages/core/test`：506 通过（+2）。
- `flutter test`：246 通过（+5）。
- 未 commit（遵守规则，等用户确认）。

## 2026-08-29 11:43 发布链路修复 + v3.0.0 发布打包验证（未推送 GitHub）

### 发布脚本修复（generate_build_info 引用）
- `scripts/release.py` / `Makefile` / `.github/workflows/flutter-ci.yml` 三处把 `python scripts/generate_build_info.py` 改为
  `dart run scripts/generate_build_info.dart`。原引用指向不存在的 `.py`，会导致 `make release` / CI 第一步即失败
  （实际实现是纯 Dart 脚本，`dart run` 即可）。

### Windows release 构建修复（native assets 下载）
- 根因：sqlite3 3.5.1 的 native assets hook 默认从 GitHub Releases 下载预编译库
  （`https://github.com/simolus3/sqlite3.dart/releases/download/sqlite3-3.5.1/sqlite3.x64.windows.dll`）。
  Dart 的 HttpClient 不读 Windows 系统代理（本机 Clash `127.0.0.1:7890`），直连 github.com 超时
  （`SocketException errno=121`），`flutter build windows --release` 在 native assets 阶段失败。
- 修复：构建前设置 `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY=http://127.0.0.1:7890` 环境变量即可
  （hook 尊重代理 env，见 `CouldNotDownloadException` 提示）；未改动任何仓库代码。
- 验证：设置代理后 `flutter build windows --release` 通过，产出 `build/windows/x64/runner/Release/CarroNote.exe`。

### v3.0.0 发布打包（未推送 GitHub）
- `python scripts/release.py` 完整跑通：分 ABI APK（x86_64 / arm64-v8a / armeabi-v7a）+ fat APK + Windows zip + SHA256SUMS
  全部生成到 `releases/3.0.0/github/`，SHA256 校验 8/8 通过。
- 产物：CarroNote-3.0.0-all.apk（82.2 MB）、arm64-v8a 31.9 / armeabi-v7a 32.4 / x86_64 33.3 MB、
  windows-x64.zip 23.4 MB、android.zip 99.1 MB、metadata x2、SHA256SUMS.txt。
- 待办：dev→main 合并、打 v3.0.0 tag、`gh release create` 上传（均未执行，等用户确认）。
