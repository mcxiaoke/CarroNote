# 变更日志 (2026-08-09)

## [2026-08-09 18:18] Windows 窗口与选择对话框居中
- **主窗口启动居中**：`windows/runner/win32_window.cpp` 的 `Win32Window::Create` 在 `CreateWindow` 成功后，取主显示器工作区（`MONITORINFO.rcWork`，自动避开任务栏），用 `SetWindowPos(SWP_NOSIZE|SWP_NOZORDER|SWP_NOACTIVATE)` 将窗口居中。坐标为设备像素、与工作区一致，无需 DPI 缩放。原 `main.cpp` 的 `origin(10,10)` 不再决定最终位置（仍用于选择 DPI 显示器）。
- **文件/位置选择对话框居中**：`lib/views/settings/sync_backend_config_page.dart` 的「选择目录」走 `file_picker`，在 Windows 上是原生 Common Item Dialog，**默认以父窗口（本应用窗口）为中心居中**。主窗口居中后，这些对话框随之在桌面居中——采用"相对父窗口居中"方案（改动最小、无需干预 file_picker 内部）。
- **验证**：`flutter build windows --debug` 构建通过（C++ 改动编译无误）。

## [2026-08-09 18:09] 同步配置面板：桌面端改为弹框（不占满窗口），仅桌面端
- **改动**：`lib/views/settings/sync_backend_config_page.dart` 的 `SyncBackendConfigPage` 由「整页 Scaffold（AppBar + bottomNavigationBar）」重构为「与形态无关的表单内容」（可滚动表单 + 底部操作栏的 Column，不再自带 Scaffold/AppBar）。`showSyncBackendConfigPanel` 按平台分流：
  - **桌面端（windows/macOS/linux，`isDesktopPlatform`）**：用 `showDialog` 以 `Dialog` 呈现，限宽 `maxWidth:560`、限高 `maxHeight:760`，顶部加 `_DialogHeader`（标题「同步配置」+ 关闭/取消按钮），点遮罩即取消（返回 null），居中弹框而非占满窗口。
  - **移动端**：保持原整页（`MaterialPageRoute` fullscreenDialog + `Scaffold` AppBar），交互不变。
- **复用**：两套外壳共用同一份 `_SyncBackendConfigPageState` 表单状态（类型切换不丢配置、测试通过才能保存等逻辑均不变）；保存仍通过 `Navigator.pop(draft)` 交回调用方，弹框与整页的返回值语义一致。
- **验证**：`flutter analyze lib/ test/` → No issues found；`flutter test`（应用层）23/23 通过。

## [2026-08-09 17:04] 同步配置重构：统一配置面板 + 独立总开关 + 自动初始化
- **需求 1：统一配置面板（测试通过才能保存，切换类型不丢配置）**：新增 `lib/views/settings/sync_backend_config_page.dart`（独立面板 `showSyncBackendConfigPanel`）。面板持有全部 6 个字段的常驻 `TextEditingController`，类型切换时旧类型输入框的值原样保留；底部有「测试」和「保存」按钮，**保存仅在测试通过（`connectionSignature` 匹配上次通过结果）后可用**。新增不可变值对象 `SyncBackendDraft`（`lib/sync/sync_config.dart`）：同时持有全部类型字段，`fromConfig()`/`copyWith()`/`normalized()`（仅裁剪 URL/用户名/路径，**不裁剪密码/Token**）/`isComplete`/`connectionSignature`/`buildBackend()`/`save()`（全部类型字段写回、类型最后写，保证切回原类型仍可用）。
- **需求 2：独立同步总开关**：`SyncConfig` 新增 `sync_enabled` 键与 `isSyncEnabled`/`setSyncEnabled`（独立于后端配置）。`sync_settings.dart` 新增「同步开关」`SwitchTile`（`_toggleSyncEnabled`）。三个语义量明确区分：`isSyncEnabled`（用户意愿）、`hasBackendConfig`（字段完整）、`isSyncReady`=两者皆真（是否真正可同步）。所有"要不要初始化/显示同步 UI"的判断统一改用 `isSyncReady`；旧版本（无 `sync_enabled` 键）按 `backendType != none` 推断，升级后同步不会被静默关闭。
- **需求 3：开关开启且配置有效时自动初始化**：`SyncService.applyConfigToService` 重写为返回 `(success, error)`；在 `isSyncEnabled` 且 `hasBackendConfig` 且 `keyring` 已解锁、但 `_engine==null || _deviceId==null` 时走 `initBackend` 完整初始化（修复此前"配置好了却始终未初始化"的隐患）；`sync()`/`autoSync()` 增加总开关守卫。`SyncService.testBackendConfig(draft)` 用草稿构造临时后端做 `init()+getManifest()+close()` 两层连通性验证（SafeServer health 不校验 Token，故必须 getManifest 验证真实认证），与单例持有的 `_backend` 完全隔离、不污染生效配置。
- **其它**：`main.dart` `_bootstrap` 增加 `SyncConfig.init()`（修复启动期默认值问题）；`login`/`set_passphrase`/`home`/`settings`/`diagnostics` 各处的同步判断点改用新的语义量；诊断快照新增 `syncEnabled` 字段（`packages/core/lib/src/sync/sync_models.dart`）。
- **验证**：`flutter analyze lib/ test/` → No issues found；新增 `test/sync/sync_config_test.dart`（`flutter test` 14/14 通过），覆盖 `SyncBackendDraft`（`isComplete`/`connectionSignature`/`buildBackend`、切换类型字段保留、`normalized` 不裁剪密码）、总开关语义（含旧版迁移默认值）、`SyncService.testBackendConfig`（localFs 真实临时目录连通 + none/字段不全报错）。测试以内存 mock 注入 `SharedPreferences` 与 `flutter_secure_storage`（`plugins.it_nomads.com/flutter_secure_storage`）`MethodChannel`。
- **回归修复（全量测试发现）**：`sync()`/`autoSync()`/`applyConfigToService` 的总开关守卫原先用 `if (!SyncConfig.isSyncEnabled) return` 直接拦截。但 `change_password_multi_client_test` 的「Bug A」用例在**未初始化 SyncConfig** 的情况下直接调用 `SyncService.sync()`，此时 `_prefs` 为 null → `isSyncEnabled` 回退为 `backendType != none` = false → 守卫误杀导致 `sync()` 返回 null（断言失败）。新增 `SyncConfig.isInitialized`（`_prefs != null`），守卫改为 `if (SyncConfig.isInitialized && !SyncConfig.isSyncEnabled)`：**仅当用户配置系统已就绪、且明确关闭同步时才拦截**；未初始化时按历史行为放行（纯逻辑测试、启动早期窗口）。生产环境 `SyncConfig.init()` 在 `main._bootstrap` 早于任何同步调用，故语义不受影响。
- **全量测试结果（core 为纯 Dart 包，须从仓库根用 `dart test` 运行，否则 cwd 错致 `server/go` 解析不到、SafeServer 集成在 setUpAll 失败）**：
  - app 层 `flutter test`（根目录）→ 23/23 通过（含 `generate_real_db_test` / `change_password_multi_client_test` / 本次新增 `sync_config_test`）。
  - `dart test packages/core/test/`（仓库根）→ 241/241 通过，含 WebDAV/SafeServer 集成测试、混沌真实数据 3 seed 并行（seed 12/345/6789 全过）、P2 定向故障注入、多设备/keyring 等。

## [2026-08-09 15:58] 笔记列表：卡片时间/排序设置 + 颜色与紧凑模式一致性
- **① 卡片时间显示（默认绝对，可切相对）**：新增偏好 `isRelativeTime`（默认 false=绝对）。`note_card`/`note_tile` 及 compact 变体统一改用 helper `noteTimeLabel`（`lib/utils/time_utils.dart`）：默认显示绝对日期+时间（如 `Aug 9, 2026 2:30 PM`），开启「相对时间」后当天显示「x 分钟前」。设置 → General 新增开关 **Relative Time**。
- **② 列表排序（默认按修改日期，可切创建日期）**：新增偏好 `isSortByModified`（默认 true）。`home.dart` 的 `_sortAndStoreNotes` 排序键改为由该偏好决定（true→`SafeNote.modifiedTime`，由 `updatedAt` 转换；false→`createdTime`）；方向按钮（新→旧/旧→新）逻辑不变。设置 → General 新增开关 **Sort by Modified Date**。新增全局 `RouteObserver`（`lib/utils/route_observer.dart`，已在 `app.dart` 注册），主页 `RouteAware.didPopNext` 在从设置返回时按最新偏好自动重排。
- **③ 颜色提亮仅限浅色模式**：`NotesColor.getNoteColor` 仅浅色模式把主题色与白色按 40% 混合提亮；暗黑模式恢复为原色不变（用 `PreferencesStorage.isThemeDark` 判断），避免浅色卡片在深色背景上刺眼/违和。
- **④ 紧凑模式与卡片模式一致**：`note_card_compact`/`note_tile_compact` 补上与卡片相同的时间行（复用 `noteTimeLabel`）；颜色提亮为全局逻辑，两种模式已一致。
- **⑤ 文案修正**：`assets/translations/zh-CN.json` 的 `Biometric` 由「生物特征学」改为「生物识别登录」。
- **验证**：`flutter analyze` 全绿（仅剩 `sync_engine.dart:588` 预存 warning，非本次改动）；`flutter test` 9/9 通过（并修正 `test/widget_test.dart` 中过时的 `ElevatedButton` 断言为 `FilledButton`——该测试此前已因 `AppButton` 改版而失败，与本次功能无关）；`flutter build windows --debug` 构建成功。

## [2026-08-09 14:40] 修复：搜索框占位符偏下 + 侧栏 ListTile 报错
- **① 搜索框占位符（hint）偏下**：`lib/widgets/search_widget.dart` 改用 Row 布局（搜索图标 + 输入框 + 清除按钮同排，Row 统一垂直居中），并**移除 `textAlignVertical: TextAlignVertical.center`**。用真实 Windows 字体（Segoe UI + 微软雅黑）实测定位：`textAlignVertical: center` 在 `isCollapsed` + 零内边距下会把文字/hint 额外下移约 4px（图标居中、文字偏低）；移除后 `editableCenter/hintCenter` 与容器中心完全重合（diff=0）。此前"搜索框不居中"的历次尝试（textAlignVertical / expands / Align）均未命中根因。
- **② 侧栏 ListTile 背景/水波纹不可见报错**：`home_navigation_rail.dart` 侧栏原本用 `Container(color: bg)`（内部即 ColoredBox）包住 `ListTile`，而最近的 Material 在外层，触发 Flutter 断言。改为 `SizedBox(width: 240, child: Material(color: bg, child: Column(...)))`，让 ListTile 找到自己的 Material 祖先（水波纹恢复正常）。
- **验证**：真实字体 widget test 实测 hint/editable 中心 == 容器中心（diff=0）；`flutter analyze` 全绿（仅剩 `sync_engine.dart:588` 预存 warning）。临时测试已删除。

## [2026-08-09 13:39] Windows UI 细节修正（4 项）
- **① 搜索框垂直居中 + 亮色暗灰**：`lib/widgets/search_widget.dart` 的 `TextField` 增加 `textAlignVertical: TextAlignVertical.center`（中文两字此前不垂直居中）；填充色由 `surfaceContainerHighest` 改为按亮度区分——亮色用更浅的 `surfaceContainerLow`，暗色保留 `surfaceContainerHighest`，解决亮色模式下仍发灰的问题。
- **② 空状态"No Notes"居中**：`lib/views/home.dart` 的 `_handleAndBuildNotes()` 将 `Text(noNotes)` 包入 `Center`，原本贴在左侧、无左右边距。
- **③ 左侧 drawer / 侧栏底部三链接 → GitHub**：`lib/widgets/drawer.dart`（移动端）与 `lib/widgets/home_navigation_rail.dart`（Windows 侧栏）底部三个外链按钮的 `onPressed` 全部改为 `SafeNotesConfig.githubUrl`（`https://github.com/keshav-space/safenotes`），文案统一为 "GitHub"。
- **④ 登录/密码页 footer 改版**：`lib/widgets/footer.dart` 第一行 "Safe Notes" → 版本号（`v{SafeNotesConfig.appVersion}`），第二行 "Made with ♥ on Earth" → 构建时间 + git short hash（`{BuildInfo.buildDateReadable} · {BuildInfo.gitHashShort}`）。移除不再使用的 `showVersion` 参数与 `easy_localization` 导入；同步更新 `lib/views/settings/settings.dart` 的 `footer()` 调用。
- **验证**：`flutter analyze` 全绿（仅剩一个不属于本项目的预存 warning：`sync_engine.dart:588`）。

## [2026-08-09 12:50] Windows 原生化 UI/UX 改进（6 项，已分步提交并通过 `flutter build windows`）
- **背景**：Windows 端此前完全是 Material 移动端范式，按钮/颜色/转场/设置页/标题栏均不像原生。对应设计文档 `docs/windows-ux-native-improvements-20260809.md`。
- **① 字体（P0）**：新增 `lib/utils/platform_ui.dart`，用平台字体（Windows=Segoe UI、macOS=SF、Linux=Ubuntu）+ CJK 兜底（微软雅黑等）替换原先全局强制的 `NotoSerif` 衬线体与 `MerriweatherBlack` 重体；FCS `subThemesData.defaultRadius=8`；设置页深色背景由纯黑改 `#202020`。
- **② 转场（P1）**：`lib/routes/route_generator.dart` 桌面端由 `leftToRight/300ms` 改为淡入 `~180ms`，移动端保留滑动。
- **③ 按钮（P0）**：新增 `lib/widgets/app_button.dart`（桌面紧凑无投影 / 移动端触摸友好）与 `DialogActionBar`（右下对齐小号按钮对）；`login_button` 委托 `AppButton`；笔记编辑页 AppBar 保存按钮由凸起 `ElevatedButton` 改 `TextButton`；全部对话框的双撑满按钮改为原生操作条。
- **④ 去 iOS 化（P1）**：`lib/utils/settings_platform.dart` 把宿主平台映射到 `settings_ui` 的 `DevicePlatform`，9 个设置页不再写死 `DevicePlatform.iOS`；`CupertinoSwitchListTile` 改为 Material `SwitchListTile`，消除 iOS 控件泄漏。
- **⑤ 窗口（P1）**：`win32_window.cpp` 增加 `DWMWA_SYSTEMBACKDROP_TYPE`（Win11 Mica）与 `WM_SETTINGCHANGE`（系统主题切换时刷新标题栏）；修正 `Runner.rc` 元数据与窗口标题为 "Safe Notes"。
- **⑥ 侧栏接入主题（P1）**：`home_navigation_rail.dart` 硬编码灰色改为 `ColorScheme`（surfaceContainerLow / onSurface / outlineVariant）。
- **验证**：`flutter analyze` 全绿；`flutter build windows` √ Built（无报错）。移动端（Android/iOS）仅字体/转场走系统默认，行为不变。

## [2026-08-09 14:15] 修复：zxcvbnm 导入 en 模块编译错误（`Undefined name 'en'`）
- **问题**：`import 'package:zxcvbnm/languages/en.dart'` 直接导入时，`en` 标识符未被识别为已定义前缀，导致 `dart analyze` 和 `flutter build windows` 报错。
- **修复**：改为 `import 'package:zxcvbnm/languages/en.dart' as en;`（加 `as en` 别名），使 `en.dictionaries` 正确解析为 `Set<Dictionaries>` 类型。
- **验证**：`dart analyze lib/` → No issues found；`dart analyze lib/utils/passphrase_util.dart` → No issues found。

## [2026-08-09 14:00] 密码强度评估：用 zxcvbnm 替换自研 estimateBruteforceStrength
- **变更**：`lib/utils/passphrase_util.dart` 原先用简单的字符集正则 + logistic 曲线估算密码强度，现替换为 `zxcvbnm`（基于 Dropbox zxcvbn 算法，含 40k 常见密码/人名/Wikipedia 常用词字典 + 模式匹配）。
- **接口兼容**：`estimateBruteforceStrength()` 签名不变（`String → double`），返回 0.0~1.0 归一化分数，内部将 zxcvbnm 的 0-4 整数 score 映射为 `(score + 1) / 5.0`。调用方 `change_passphrase.dart` 和 `set_passphrase.dart` 无需修改。
- **阈值**：`minPassphraseStrength = 0.5` 不变，对应 zxcvbnm score ≥ 2（中等强度）。
- **依赖**：`pubspec.yaml` 新增 `zxcvbnm: ^1.1.1`（MIT 协议，依赖 `algorithmic` + `intl`）。
- **备份**：原始 `passphrase_util.dart` 已备份到 `temp/backups/passphrase_util.dart.20260809_original`。
- **验证**：`flutter pub get` 成功，`zxcvbnm 1.1.1` + `algorithmic 0.0.11` 已安装。

## [2026-08-09 11:30] 最终状态：保留标题栏暗色支持，撤销热切换 hack（待审查，未提交）
- **决策**：用户确认——保留 Windows 标题栏暗色支持，但**撤销为支持"应用内热切换"而加的全部 hack**（SWP_FRAMECHANGED / SetWindowPos / RedrawWindow / PostMessage / WM_APP_SET_DARK_MODE）。仅"重启进程后生效"即可，不追求实时刷新。
- **已撤销的 hack**：`win32_window.cpp` 中的 `WM_APP_SET_DARK_MODE` 宏、`MessageHandler` 里对应 `case`（含 `SetWindowPos(SWP_FRAMECHANGED)` + `RedrawWindow`）、以及 `SetDarkMode` 内的 `PostMessage` 线程封送。已全局检索 `windows/` 确认无残留。
- **保留的标题栏暗色支持（即用户 msg10 指定范围）**：
  - Dart：`lib/utils/window_title_bar.dart`（`MethodChannel('safenotes/window_title_bar')` + `syncWindowsTitleBar(bool)`，已捕获 `PlatformException`）；`lib/models/app_theme.dart` 的 `ThemeProvider` 构造与 `setIsDarkMode` 调用它推送主题。
  - 原生：`win32_window.{h,cpp}` 的 `SetDarkMode(bool)` + `dark_mode_` 成员；`UpdateTheme` 应用 `dark_mode_`（用 4 字节 `BOOL`，已修复 `bool`/`BOOL` sizeof 不一致）；`Create` 以系统 `AppsUseLightTheme` 注册表初始化；`WM_DWMCOLORIZATIONCOLORCHANGED` 重应用已保存的 `dark_mode_`。`flutter_window.{h,cpp}` 注册 `setDarkMode` 通道。
- **实际行为**：标题栏跟随【系统】主题；应用内切换暗/亮的改动在已开窗口不实时重绘（无 hack 使然），**重启进程后生效**——符合"重启能切换就够了"。
- **验证**：`flutter analyze lib` → No issues found；`flutter build windows` → √ Built（28.0s，无报错），已在本环境实测编译通过。搜索框暗色纯白刺眼问题同步修复。
- **git**：连同 提交B 与 ① 一起，均**未提交**，待你大屏亮/暗双模式实机回归确认后再提交。

## [2026-08-09 10:40] 协调修复①：搜索框暗黑刺眼 + ②：Windows 标题栏跟随应用主题（待审查，未提交）
- **① 搜索框暗黑刺眼**：`lib/widgets/search_widget.dart` 原搜索框用 `Colors.white` 纯白底 + `Colors.black/black54` 文字 + `Colors.black26` 边框，暗黑模式下纯白刺眼且不通配。
  - 改为读取 `Theme.of(context).colorScheme` 语义令牌：`surfaceContainerHighest`（输入框底色）、`onSurface`（输入文字）、`onSurfaceVariant`（hint 文字）、`outlineVariant`（边框）。亮/暗两模式对比度均合规，彻底消除刺眼白。
- **② Windows 标题栏不跟随应用主题**：Flutter Windows runner 默认 `UpdateTheme` 只按【系统】`AppsUseLightTheme` 注册表决定标题栏明暗，与应用内暗/亮开关不联动（即官方所说 3.7+ 默认支持，但仅跟随系统，手动切应用主题无效）。
  - Dart 侧：新增 `lib/utils/window_title_bar.dart`（`MethodChannel('safenotes/window_title_bar')` + `syncWindowsTitleBar(bool)`，`PlatformException` 已捕获，非 Windows 平台无副作用）；在 `lib/models/app_theme.dart` 的 `ThemeProvider` 构造与 `setIsDarkMode` 中调用，把当前主题推送到原生层。
  - 原生侧（windows/ 已用最新 Flutter 重新生成）：`win32_window.{h,cpp}` 新增 `SetDarkMode(bool)` + `dark_mode_` 成员；`UpdateTheme` 改为应用 `dark_mode_` 而非每次读系统注册表；`Create` 时仍以系统偏好初始化；`WM_DWMCOLORIZATIONCOLORCHANGED` 改为重应用已保存的 `dark_mode_`（避免系统配色变化把应用选择覆盖回系统值）。`flutter_window.{h,cpp}` 注册 `setDarkMode` 方法通道，解析 bool 后调 `SetDarkMode`。
  - 效果：Windows 标题栏**跟随系统**主题；应用内切换暗/亮须**重启进程**才生效（见 [11:30] 撤销热切换 hack 说明）。
- **验证**：`flutter analyze lib` → No issues found；`flutter build windows` 已在本环境实测通过（见 [11:30]）。
- **git**：未提交，连同 提交B 一起待你大屏亮/暗双模式实机回归确认协调后再提交。

## [2026-08-09 11:13] ⚠️ 已撤销：标题栏应用内切换强制非客户区重绘（SWP_FRAMECHANGED）
- **说明**：本段为支持"应用内热切换标题栏"而尝试的 hack（`SetDarkMode` 内 `SetWindowPos(SWP_FRAMECHANGED)`）。按用户 [11:30] 决策已**整体撤销**，不再保留。`SWP_FRAMECHANGED` 相关代码已从 `win32_window.cpp` 删除。

## [2026-08-09 11:17] ⚠️ 已撤销：标题栏实时刷新（二）线程封送 + 双重重绘（PostMessage / WM_APP_SET_DARK_MODE）
- **说明**：本段为支持"应用内热切换标题栏"而尝试的 hack（`SetDarkMode` → `PostMessage(WM_APP_SET_DARK_MODE)` + `MessageHandler` 内 `SetWindowPos(SWP_FRAMECHANGED)` + `RedrawWindow`）。按用户 [11:30] 决策已**整体撤销**，不再保留。`WM_APP_SET_DARK_MODE` 宏、`PostMessage`/`RedrawWindow` 调用均已删除。

## [2026-08-09 09:53] 提交B：清理 NordColors 硬编码 + 移除 safenotes_nord_theme 依赖（待审查，未提交）
- **目标**：消除"没改完"导致的主题不协调——把全仓残留的 Nord 硬编码色全部换成 `Theme.of(context).colorScheme.*` 语义色，并彻底移除 Nord 主题包。
- **语义映射**（均在 widget build 内、`context` 可用处）：
  - `NordColors.aurora.red`（错误/危险红）→ `colorScheme.error`：登出/删除/取消/导入等破坏按钮底色、登录失败文案、"重置本地数据"红色文字按钮（login.dart ×4，logout_alert/delete_confirmation/confirm_import/backup_passphrase 各 1）。
  - `NordColors.aurora.yellow`（删除确认警告图标）→ `colorScheme.error`，与红色破坏按钮构成统一的"危险"对话框语义。
  - `NordColors.frost.darker`（备份路径链接文字/图标，蓝调强调）→ `colorScheme.primary`（正是种子蓝 #5E81AC，自然协调）。
  - 条件对比 hack `isThemeDark ? snowStorm.lightest : polarNight.darkest`（login_button/change_passphrase/login 的 `shadowColor`、add_edit_note 的 `backgroundColor`、styles.buttonTextStyle）→ 移除，改由 M3 默认（按钮用 `onPrimary`、阴影用默认）或 `colorScheme.onPrimary`；`note_card.dart`/`note_card_compact.dart` 同样的 `isThemeDark ? Colors.white : Colors.black` 阴影 hack 一并移除（同 anti-pattern）。
- **styles.dart**：`appBarTitle` 删除硬编码 `Colors.white`——交给 AppBar 自带的 `titleTextStyle` 提供正确 on-surface 色（M3 亮色下白字不可见的问题根治）；`buttonTextStyle()` 改为接收 `BuildContext` 返回 `colorScheme.onPrimary`，`generic.dart` 相应传 `context`。
- **清理依赖**：`pubspec.yaml` 删除 `safenotes_nord_theme` git 依赖，`flutter pub get` 同步（lock 已无该包）；删除全部 10 处 `import 'package:safenotes_nord_theme/...'`；移除因本次改动变成未使用的 `preference_and_config` 导入（login_button/add_edit_note/note_card/note_card_compact；backup_passphrase 的 `ImportPassPhraseHandler` 仍需该包，故保留）。
- **验证**：`flutter analyze lib` → No issues found。工作区 15 文件改动（净删 82 行）。**未提交 git**，等你大屏亮/暗双模式实机回归确认协调后再提交。
- **遗留待你确认**：另有若干硬编码 `Colors.white/black` 非 Nord 残留（search_widget 搜索框、drawer/home_navigation_rail 的条件文字色、notes_color_setting/backup_setting/change_passphrase 的条件文字色、ios_style_list_tiles 的 Cupertino 白、notes_color.dart 按亮度算对比色、darkSettingsScaffold 故意黑底）——语义各异、非 Nord 遗留，本次未动；若 review 后觉得某处仍不协调，指出即改。

## [2026-08-09 09:17] 主题引擎切换：接入 flex_color_scheme 作可插拔主题引擎，移除 dim 主题（提交 9b36ea6）
- **方案**：方案 B——弃用 Nord 硬编码 `ThemeData`，改用 Material Design 3 + `flex_color_scheme` (FCS) 作为统一主题引擎；只保留亮/暗两套主题，简化此前 Nord 的 light/dim/out 多暗色变体。
- **插拔点设计**（`lib/models/app_theme.dart`）：FCS 仅在 `AppThemes` 被引用，注释明确"业务代码一律通过 `Theme.of(context).colorScheme.*` 取色，不直接依赖任何主题库"。将来要替换主题引擎（如换用其它配色方案库），只需重写 `AppThemes` 一处，UI/组件层零改动——满足"可替换可插拔"诉求。
- **种子色与生成**：品牌种子色 `_brandSeed = Color(0xFF5E81AC)`（Nord frost 蓝调，换主题库通常只改这一处）；`ColorScheme.fromSeed(seedColor:_brandSeed, brightness)` 生成对比度合规的 M3 调色板，再交给 `FlexThemeData.light/dark(colorScheme: scheme, useMaterial3:true, textTheme/primaryTextTheme: NotoSerif)` 包装（应用表面色调、组件默认值等增强）。
- **移除 dim 暗色变体**（`lib/views/settings/theme_setting.dart`）：删除"暗色变体选择"bottom sheet 及其 `CupertinoCheckListTile`、`setModalStateDarkTheme`、`DarkThemeEnum` 等；`ThemeProvider` 移除 `setIsDarkDimTheme` 方法。现设置页仅保留"Dark 模式开关"与"跟随系统开关"。`darkSettingsScaffold`/`darkSettingsCanvas` 简化为 `Colors.black`/`Colors.grey.shade900`。
- **依赖变更**：`pubspec.yaml` 新增 `flex_color_scheme: ^8.4.0`（要求 Flutter ≥3.44.0 / Dart ≥3.12.0）。注意：`safenotes_nord_theme` git 依赖**暂未移除**——其 `NordColors.*` 仍散落在其它文件，待 提交B 清理这些调用后再从 pubspec 删除（避免拆用报错）。
- **验证**：`flutter analyze lib` → No issues found。建议桌面大屏（≥600 宽）亮/暗双模式实机回归，重点确认左侧 `HomeSidebar` 与设置页文字/图标对比度正确。
- **git**：提交 `9b36ea6`（原 `use new app theme` 已 amend 为 conventional message：`refactor(theme): use flex_color_scheme as pluggable theme engine; drop dim theme`）。未推送到 origin/dev（本地领先 12 个提交）。
- **未完成（提交B，待用户确认）**：清理散落的 `NordColors.*` 调用改用 `Theme.of(context).colorScheme.*` 语义色，并移除 `safenotes_nord_theme` 依赖。
