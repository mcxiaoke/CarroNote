# 变更摘要 2026-08-27

> 本文件按项目规范记录当日关键代码变更，最新在顶部。

## 2026-08-27 17:40 桌面端快捷键补齐（RELEASE-REVIEW #7）

对应 `docs/RELEASE-REVIEW-20260826.md` #7 与用户增补需求“新建/保存/返回/复制/剪切”：

### 7.1 主页 `lib/views/home.dart:14-30,120-180,450-520`
- 新增 `FocusNode _pageFocusNode/_searchFocusNode`（`skipTraversal:true`）及 `dispose`。
- `SearchWidget` 扩展 `focusNode` 参数 `lib/widgets/search_widget.dart:22-33,91-93`，`_buildSearch` 传入 `_searchFocusNode`。
- 构建外层包 `CallbackShortcuts` + `Focus(_pageFocusNode)`，绑定 5 组快捷键（`control`+`meta` 双写兼容 Win/macOS）：
  - `Ctrl/Cmd+N` → `_handleNewNoteShortcut`：非加载/非多选时 `Log.ui.i` + `unawaited(_openAddNote())` 新建。
  - `Ctrl/Cmd+F` → `_handleSearchShortcut`：`_searchFocusNode.requestFocus()` 聚焦搜索。
  - `Esc` → `_handleEscShortcut`：多选→`_exitSelectionMode()`；有搜索词→`_searchNote('')` 清空并 `unfocus`；根页无 pop。
  - `Ctrl/Cmd+A` → `_handleSelectAllShortcut`：空列表跳过；非多选则进入多选并 `notes.map(uuid).toSet()`；多选则 `_toggleSelectAll()`。
  - `Ctrl/Cmd+S` → `_handleSaveShortcut`：`autoSync()+refreshNotes()`（主页无正文保存，对标编辑页 Ctrl+S）。
- TextField 原生 `Ctrl+A/C/V/X` 由 `EditableText` 优先处理，主页层 `Ctrl+A` 仅在搜索框未聚焦时生效（不抢文本全选）。

### 7.2 编辑页 `lib/views/add_edit_note.dart:14-18,120-135,180-185,250-270,430-530`
- 新增 `FocusNode _pageFocusNode` 及 `dispose`。
- 顶层包 `CallbackShortcuts` + `Focus(_pageFocusNode)`，复用 `_editorShortcuts` 7 组：
  - `Ctrl/Cmd+S` → `unawaited(_handleSaveShortcut())`：`_isLocked/_isSaving` 守卫；无改动 `showSnackBarMessage('No changes')`；有改动 `await _performAutoSave(keepEditing:true)`，成功 `showSnackBarMessage('Saved')`，失败 `showErrorToast('Failed to save')`。
  - `Ctrl/Cmd+N` → `_handleNewShortcut`：有脏数据先 `unawaited(_performAutoSave)`，再 `Navigator.pushNamed('/addnote', arguments: sessionStateStream)` 叠页新建。
  - `Esc` → `_handleEscShortcut`：`_isSaving/_isDeleting` 时忽略；编辑态首按切预览 `_previewMode=true`，次按 `maybePop()` 经 `PopScope` 走自动保存再关页。
  - `Ctrl/Cmd+F` → `_handleFindShortcut`：锁定跳过；预览态先切编辑并 `addPostFrameCallback(requestFocus)` 到标题，编辑态直接 `_titleFocusNode.requestFocus()`。
  - `Ctrl/Cmd+Shift+C` → `_handleCopyAllShortcut`：`unawaited(_copyAll())` 复制全文（标题+正文空行分隔）；`Ctrl/C/X/V/A` 仍由 `ShadInputFormField` 原生处理，不拦截。
- 保留原有 `Focus.onKeyEvent` 的 `Ctrl+Z/Y/Shift+Z` 撤销重做逻辑（先于系统 undo 且 `handled` 防双撤销），新增绑定不与之冲突；预览态 `_handleKeyEvent` 本就 `ignored`，新增顶层 shortcuts 在预览/编辑均生效。

### 7.3 文案 `assets/translations/{zh-CN,en-US}.json:391-395`
- 新增 `Saved/No changes/Failed to save/Empty note` 双语键，已在上节 `Empty note` 复用。

验证：`flutter analyze` No issues；`dart test packages\core\test` 504+1 skip；`flutter test` 241 通过。

## 2026-08-27 17:20 修复锁定前未保存丢弃 + 新建态更多菜单缺失（RELEASE-REVIEW #4）

对应 `docs/RELEASE-REVIEW-20260826.md` #4 与用户反馈 `add_edit_note.dart:568-581,217`：

### 4.1 锁定前未保存改动静默丢弃 `lib/views/add_edit_note.dart:607-635`
- 根因：`_toggleLock` 直接写 `note_meta.locked`，未先落库正文；锁定后 `_isLocked=true` 使 `_performAutoSave:329` 与 `_onPopInvoked:299-302` 均跳过保存，退出即丢弃。
- 修复：`_toggleLock` 锁定分支（`locked==true && isNoteNewOrContentChanged()`）先 `await _performAutoSave(keepEditing:true)`，失败则 toast 并中止锁定；成功后刷新 `note` 引用（`_effectiveNote`）再 `setNoteLocked`。解锁分支不变。保存失败不改锁定态，避免“内容丢了却已上锁”的不一致。

### 4.2 新建笔记未落库前“更多”菜单不存在 `lib/views/add_edit_note.dart:245-248,535-570`
- 根因：`AppBar.actions` 仅 `if (_effectiveNote!=null) _moreButton()`，新建态 `_effectiveNote==null` 时无入口；`_onMorePressed:537` 直接 `return`，无法星标/锁定/配色/标签/版本历史，且后台 30s 定时 `store` 前用户无感知。
- 修复：
  - `build` 改为始终展示 `_moreButton()`（注释更新为“新建未落库也展示，首次调起自动落库”）。
  - `_onMorePressed` 新建分支：空内容 `showSnackBarMessage('Empty note')` 直接返回；非空且非保存中则 `Log.note.i` + `await _performAutoSave(keepEditing:true)`，失败中止，成功后以新 `_effectiveNote` 继续查 `getNoteMeta` 并弹出 `showNoteActionsSheet`。后续 `toggleStar/Lock/Color/Tags/History/Delete` 复用同一 `note` 句柄，`_toggleLock` 内的二次保存会因已无脏数据而跳过，无重复写库。
- 行为：新建页点“更多”会先静默创建笔记（`title/description` 已归一化补空格，`NoteEditorState` 更新 `original`），菜单立即可用；空笔记点更多仅提示不建库。

验证：`flutter analyze` No issues；`dart test packages\core\test` 504 通过 +1 skip；`flutter test test/models/editor_state_guard_test.dart` 8 通过。

备注：#4 已完成；#7 见下一节 17:40 已补齐。

## 2026-08-27 11:40 中优先级 5 项修复（RELEASE-REVIEW 中优先）

对应 docs/RELEASE-REVIEW-20260826.md 中优先 7 项中的 5 项（4 与 7 暂缓）：

### 1. update 系列 rows==0 仍写缓存产生幻影笔记 `database_handler.dart:1270-1321`
- `updateNote`（按 id）与 `updateNoteByUuid`（按 uuid）此前无论 `db.update` 返回 rows 是否为 0 都调 `_upsertCacheEntry(note)`，DB 中不存在的行会凭空新增缓存条目，形成幻影笔记。
- 修复：两处 `db.update` 后加 `if (rows > 0)` 守卫，rows==0 时不写缓存，直接 return 0。DB 与缓存强一致。

### 2. 导入备份批内 uuid 重复导致整体回滚 `database_handler.dart:983-1030`
- `storeNotesInTransaction` 已有库中去重（`_existingUuids`），但未过滤批内重复 uuid，批内重复触发 `safe_notes.uuid` UNIQUE 整体回滚。
- 修复：先按 uuid 首现去重（`seenBatch` 去重，仅保留首个），再查库去重；日志区分批内/库中跳过数。复用现成 chunk 逻辑避免 IN 上限。

### 3. markSynced 系列 IN 子句超 SQLite 变量上限 `database_handler.dart:2165-2226`
- `markAllSyncedExcept` 与 `markSyncedForUuids` 曾用单条 `IN (?,?,?,?,...,?)` 拼全部 uuid，超过 SQLITE_MAX_VARIABLE_NUMBER（999）抛错。
- 修复：
  - `markSyncedForUuids`：按 400 分片，多 chunk 在事务内批量 `UPDATE ... IN (?)`，单 chunk ≤400。
  - `markAllSyncedExcept`：NOT IN 不能简单多语句分片（会把应排除的行在另一片中误标）。≤900 时单条 NOT IN；>900 时走补集路径：`SELECT uuid` 求全量补集，再复用白名单分片 IN 标记（事务内）。语义等价且不超限。

### 5. 回收站恢复成功无提示、"清空成功"用 error 样式 `deleted_notes.dart:152-228`
- `_restoreNote` 成功分支的 `showSnackBarMessage('Restored: ...')` 被注释，无成功提示。
- `_permanentDelete` 与 `_clearAll` 成功用 `showErrorToast`（destructive 红色），误导为失败。
- 修复：恢复时展示 `showSnackBarMessage('Restored: "{title}"')`；永久删除与清空成功改用 `showSnackBarMessage`（信息样式，3s）；error 样式仅保留给失败分支。

### 6. 版本历史/回收站页异常时永久转圈；宽屏无限宽约束无 Scrollbar
- 回收站 `_refresh` 已在 M1 修复；版本历史 ` _loadData` 仅 `readVersions` 有 try/catch，`getNoteMeta` 异常会使 `_versions` 保持 null 永久转圈；`_updateDiff` 的 `computeNoteDiffAsync` 异常未复位 `_isLoading`。
- 宽屏：两页面内容无 `maxWidth` 与 `Scrollbar`，宽屏拉伸过宽且无可见滚动条。
- 修复：
  - `version_history_page.dart: _loadData` 整段 try/catch（含 meta），异常降级为空列表 + toast，必置 `_versions`；`_updateDiff` 包 try/catch/finally，异常复位 `_isLoading` 并 toast。
  - `deleted_notes.dart`：`_scrollController` + `Scrollbar(thumbVisibility: isDesktopPlatform)` + `ListView(controller: ...)`；`Scaffold.body` 外包 `Center > ConstrainedBox(maxWidth:800) > SizedBox.expand` 限宽居中。
  - `version_history_page.dart`：双 `ScrollController`（full/diff）+ 双 `Scrollbar`；`Scaffold.body` 与 `bottomNavigationBar` 同包 `Center > ConstrainedBox(maxWidth:800) > SizedBox.expand`，桌面可视滚动条、移动端保持默认。

验证：`flutter analyze` No issues；`dart test packages\core\test` 504 通过 +1 skip；`dart test packages\core\test/db` 77 通过。

备注：4（锁定前未保存丢弃/新建态更多菜单）与 7（桌面快捷键 Ctrl+S/F/Esc）按用户要求暂缓，本次未动。

## 2026-08-27 00:00 占位（沿用 2026-08-26 M1 记录，见 CHANGES-20260826.md）

本文档仅记录 11:40 五项中优先修复；其余变更见 `docs/CHANGES-20260826.md`。
