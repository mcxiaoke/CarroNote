# 变更摘要 — 2026-08-11 (GMT+8)

## 自动旋转 / 安全显示 改为设置页内联开关（提交 · betterui · 20:04 GMT+8）

两个设置各自只有一个开关，独立页面纯属多余一次跳转，改为设置页内直接 toggle
（与「隐身键盘」一致），并删除独立页面与路由。

### 改动文件
- `lib/views/settings/settings.dart`
  - `Auto Rotate`：`shadNavigationTile` → `shadSwitchTile`，
    description 沿用 `'Close and open app for change to take effect'`。
  - `Secure Display`：`shadNavigationTile` → `shadSwitchTile`，
    图标 `LucideIcons.smartphone` → `LucideIcons.monitorOff`，
    description 沿用原长说明（翻译键不变，多行字面量拼接后与原键完全一致）。
- `lib/routes/route_generator.dart`
  - 删除 `/autoRotateSettings`、`/secureDisplaySetting` 两个 case 与对应 import。
- 删除 `lib/views/settings/autorotate_settings.dart`
- 删除 `lib/views/settings/secure_display_setting.dart`

### 验证
- `flutter analyze lib/` → No issues found! (6.0s)
- `flutter build windows --debug` → √ Built (56.9s)
- `flutter run -d windows --debug` → 启动到登录页无崩溃。

## 同步配置子界面样式修复（提交 · betterui · 19:52 GMT+8）

**问题**：设置 → 同步设置 → 同步配置 子界面「像是没有样式」，背景/边距与
已迁移的 shadcn 设置页不一致。

**根因**：`showSyncBackendConfigPanel` 中桌面端 `Dialog` 与移动端 `Scaffold`
均未指定 `backgroundColor`/`shape`，回落到 Material 默认（白色）背景，与
shadcn 主题背景色不符，视觉上像「裸」页面。

### 改动文件
- `lib/views/settings/sync_backend_config_page.dart`
  - 桌面端 `Dialog`：补 `backgroundColor: ShadTheme.colorScheme.background` +
    `RoundedRectangleBorder(radius 12)` + `insetPadding: 24`，并限制
    `maxWidth: 560 / maxHeight: 760`。
  - 移动端 `Scaffold`：补 `backgroundColor: ShadTheme.colorScheme.background`。
  - `_buildBottomBar` 外层包 `Container`，顶部加
    `Border(top: BorderSide(color: colorScheme.border))` 分隔线。
  - 业务逻辑零改动。

### 验证
- `flutter analyze lib/views/settings/sync_backend_config_page.dart` → No issues found!
- `flutter build windows --debug` → √ Built (44.8s)
- `flutter run -d windows --debug` → 启动/登录/主界面/设置/同步设置 均正常，无崩溃。

## 对话框 全量迁移至 shadcn_ui（提交 · betterui · 19:12 GMT+8）

将 8 个对话框底部操作区与输入控件统一到 shadcn 风格，复用共享组件库
`lib/widgets/shad_dialog.dart`。

### 改动文件
- `lib/widgets/shad_dialog.dart`（增强）
  - `ShadDialogAction` 新增 `enabled` 字段；`shadDialogActionBar` 透传给
    `ShadButton`（outline / 主操作 / 危险操作），disabled 时按钮置灰。
- `lib/dialogs/export_backup_dialog.dart`
  - 格式选择 `RadioGroup`/`RadioListTile` → `ShadRadioGroup`/`ShadRadio`
    （注意用 `items`，非 `child`）；
  - 密码 `TextField`（prefix/suffix Icon） → `ShadInput`
    （`leading` 锁图标 + `trailing` 可见性 IconButton + `placeholder`）；
  - 底部 `DialogActionBar` → `shadDialogActionBar`；加密且密码不匹配时
    Export 按钮 `enabled=false`。
  - 错误/明文警告文案改用语义色 `ShadTheme.colorScheme.destructive`。
  - 移除不再使用的 `_fieldDecoration` 辅助函数。
- `lib/dialogs/backup_password_input.dart`
  - 密码 `TextField` → `ShadInput`（锁图标 + 可见性切换）；`DialogActionBar` → `shadDialogActionBar`。
- `lib/dialogs/backup_passphrase.dart`
  - 保留 `Form` + `TextFormField` + `validator`（sha256 哈希校验），仅把
    `DialogActionBar` → `shadDialogActionBar`（Submit 触发表单校验）。
- 其余对话框（generic / delete_confirmation / logout_alert / backup_import /
  confirm_import）补完导入：移除冗余的 `shadcn_ui` 直接导入，仅保留
  `shad_dialog.dart`（按钮数据类来自该文件）。

### 验证
- `flutter analyze`：No issues found!
- `flutter build windows --debug`：构建成功（safenotes.exe）。

### 备注
- `lib/widgets/app_button.dart` 保持不动（登录页仍依赖 `AppButton`）。
- 未触碰各对话框业务逻辑（导入/导出/哈希校验），仅视觉层迁移。

## home/notes + 抽屉/侧边栏 迁移至 shadcn_ui（提交 · betterui · 18:46 GMT+8）

将主页导航与笔记列表视觉统一到 shadcn 风格，沿用「叠加」集成（根 ShadApp.custom）。

### 改动文件
- `lib/widgets/shad_nav_items.dart`（新增）
  - 共享导航菜单项 `shadNavMenuItem`：品牌色圆角图标容器 + 文字 + 整行点击反馈
    （Material + InkWell），抽屉与桌面侧边栏共用，风格与设置页设置项一致。
- `lib/widgets/drawer.dart`（移动端 Drawer）
  - 菜单项改用 `shadNavMenuItem`；分隔线改用 shadcn `colorScheme.border`。
- `lib/widgets/home_navigation_rail.dart`（桌面端 Sidebar）
  - 配色改用 shadcn 主题色板（`background`/`foreground`/`border`）；
    菜单项改用 `shadNavMenuItem`。
- `lib/widgets/note_card.dart` / `note_tile.dart` / `note_card_compact.dart` / `note_tile_compact.dart`
  - 保留「彩色笔记」招牌设计，外层容器改为 `ShadCard`
    （`backgroundColor`=按索引取色、`ShadBorder.none`、`radius:10`），
    获得一致圆角/边框/阴影；文字色仍按背景计算。

### 验证
- `flutter analyze`：No issues found!
- `flutter build windows --debug`：构建成功。

### 备注
- 未触碰笔记数据/排序/搜索等逻辑，仅视觉层迁移。

## Settings 页全量迁移至 shadcn_ui（提交 · betterui · 18:32 GMT+8）

完成 Settings 主页面 + 三个二级页（Backup / Sync / Sync Backend Config）及底部 footer 的
shadcn_ui 化，并抽取共享设置项组件库。沿用「叠加」集成模式（根 ShadApp.custom 包 MaterialApp），
旧 MD3 页面零改动继续工作。

### 改动文件
- `lib/widgets/shad_settings_tiles.dart`（新增）
  - 共享设置项组件：`shadSectionTitle` / `shadSettingsCard`（ShadCard + 分隔线）/
    `shadNavigationTile`（导航型）/`shadSwitchTile`（开关型）/`shadInfoTile`（只读信息行）。
  - 内部 `_TileSurface`（Material+InkWell 点击反馈）、`_TileIcon`（Nord 品牌色圆角图标）、
    `_TileDivider`。
- `lib/views/settings/settings.dart`
  - 底部 footer 精简为两行：版本号 + 构建日期·githash（取自 `BuildInfo`）。
  - 复用共享组件重写分区与设置项。
- `lib/views/settings/backup_setting.dart`
  - 弃用 `settings_ui`，改用共享 shadcn 组件；保留全部备份逻辑（自动备份开关、位置选择、
    立即备份、加密导出面板）。
- `lib/views/settings/sync_settings.dart`
  - 改用共享 shadcn 组件；保留同步开关、状态/上次同步、后端配置入口、自动同步、钥匙圈状态。
- `lib/views/settings/sync_backend_config_page.dart`
  - 弹框改为 ShadCard + ShadRadioGroup（后端类型选择）+ ShadInput（地址/账号/密码/Token）+
    ShadButton.outline（测试连接）/ ShadButton（保存）；结果横幅用 Lucide 图标。
- `lib/models/shad_theme.dart` / `lib/app.dart`（前序 spike 已建，本次一并提交）：
  `ShadApp.custom` 叠加 `MaterialApp`，Nord 品牌色桥接。
- `pubspec.yaml` / `pubspec.lock`：锁定 `shadcn_ui 0.56.1`。

### 验证
- `flutter analyze`：No issues found!
- `flutter build windows --debug`：构建成功。
- `flutter run -d windows --debug`：启动至登录页无 crash（vaultInitialized=true）。
- Backup / Sync / 配置弹框 位于登录后，需手动验证（密码 hello.1111）。

### 备注
- 本次按用户授权提交（仅 commit，不 push）。

## shadcn_ui 可行性验证 Spike（betterui 分支 · Settings 页）

目标：验证用 shadcn_ui（Flutter）替代/叠加现有 Material 3（FlexColorScheme）是否可行，
先在 Settings 页做参考实现（Option A）。结论：**可行，采用「叠加」而非「替换」集成**。

### 改动文件
- `pubspec.yaml`
  - 新增依赖 `shadcn_ui: ^0.56.1`（MIT，160/160 pub 分，Flutter >=3.41.0 与本项目 3.44.8 兼容）。
  - 已执行 `flutter pub get`。
- `lib/models/shad_theme.dart`（新增）
  - 桥接 shadcn 主题与项目 Nord 品牌色 `0xFF5E81AC`，定义 `ShadThemes.light/dark`
    （基于 `ShadSlateColorScheme`，覆盖 `primary/ring/selection/destructive`）。
- `lib/app.dart`
  - 根 `MaterialApp` 改为 `ShadApp.custom(...)` + `appBuilder` 内部再包一层 `MaterialApp`
    （沿用 `AppThemes.lightTheme/darkTheme`）。两套设计系统并存：旧 MD3 页面零改动继续工作。
- `lib/views/settings/settings.dart`（重写）
  - 弃用 `settings_ui`，改用 Shad 组件自定义：每分区一个 `ShadCard` + 行间细分隔线；
    导航项 `_navigationTile`（Lucide 图标 + 标题 + 值 + chevron），开关项 `_switchTile`
    （`ShadSwitch`），分区标题、品牌色圆角图标容器、底部 shad 风格 footer（含版本与构建信息）。
  - 保留全部原有逻辑：`PreferencesStorage.*` 读写、各路由跳转、`showImportDialog`、
    `showThemeBottomSheet`、`launchUrlExternal`、Logout 序列。

### 验证
- `flutter analyze`：**No issues found!**
- 已核对全部 shadcn_ui 0.56.1 API（`ShadCard.padding`、`ShadThemeData`、
  `ShadSlateColorScheme.light/dark` 构造参数、`textTheme.{p,small,muted}`、
  `colorScheme.{foreground,mutedForeground,border,destructive,primary}`、`ShadSwitch`）
  与 22 个 Lucide 图标常量名均存在。
- Windows 调试版构建验证进行中。

### 备份
- 原始文件备份于 `temp/backups/20260811_172915/`：`app.dart`、`app_theme.dart`、
  `pubspec.yaml`、`settings.dart`。

### 备注
- 集成模式为「叠加」：仅根 `MaterialApp` 换 `ShadApp.custom`，其余页面不受影响；
  后续可逐个页面迁移到 Shad 组件，风险低。
- 未提交 git（用户仅授权修改，未要求提交）。
