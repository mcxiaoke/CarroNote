# 变更日志 (2026-08-08)

## [2026-08-08 22:24] 修复大屏侧栏三处呈现问题（亮/暗配色、文字位置、宽度）
- **根因**：上一版用 `NavigationRail` + `labelType:all`（标签在图标下方），且前景色依赖主题默认值——本应用 Nord 主题下默认色在亮/暗模式均不正确（抽屉此前是显式按主题设色故正常）。
- **改为自定义 `HomeSidebar`**（文件仍 `lib/widgets/home_navigation_rail.dart`，类名 `HomeNavigationRail`→`HomeSidebar`）：用 `ListTile`（图标 + 右侧文字，单行横向）实现，文字在图标右侧（非下方）；显式按主题设前景色（暗=白 / 亮=深灰 grey.shade700），背景（暗=grey.shade900 / 亮=grey.shade100），分隔线同步；固定宽度 240（原过窄）。
- **home.dart**：`HomeNavigationRail(` → `HomeSidebar(`（构造参数名不变，仅改名）。
- 验证：`flutter analyze lib/views/home.dart lib/widgets/home_navigation_rail.dart` → No issues found。未提交 git，建议大屏亮/暗双模式实机回归。

## [2026-08-08 22:05] UI 桌面端适配 P2：NavigationRail 自适应导航（依据 docs/ui-adaptation-report-20260808.md，未提交）
- **新增组件** `lib/widgets/home_navigation_rail.dart` `HomeNavigationRail`：桌面端常驻左侧导航栏，镜像 Drawer 的同一组入口（导入/改密码/主题/生物识别/设置/调试/回收站/登出），`trailing` 放评分/FAQ/反馈外链。采用 `selectedIndex:null` + `labelType:all`（作竖向菜单，不高亮），订阅 `ThemeProvider` 以刷新主题图标/标签。
- **断点策略**（Material Design 3 / Flutter 官方桌面模板，按用户给定阈值）：`home.dart` `build()` 用 `MediaQuery.sizeOf(context).width` 取窗口尺寸——`< 600` Compact 用原 Drawer；`≥ 600` Medium/Expanded 用 `Row([HomeNavigationRail, Expanded(_homeBody())])`。用户在 guidance 中明确：布局形态是顶层决策，应用窗口整体尺寸 → 用 `MediaQuery.sizeOf`（而非 `LayoutBuilder` 的局部约束）。
- **重构去重**：抽 `_homeBody()` 方法供 Compact 与 Rail 两种模式共用；新增 8 个 `_nav*()` 动作方法（纯动作、不含 pop）作为 Drawer 与 Rail 的唯一真相源，`_buildDrawer` 在每个离开主页的入口前包 `Navigator.pop`，Rail 直接调，消除原抽屉里重复的内联闭包。
- **Row 用 `crossAxisAlignment.stretch`**：让 NavigationRail 撑满高度、背景与正文区齐平。
- **未做**：键盘快捷键（用户明确本次跳过）；NavigationRail 仅纯菜单形态，未做主从双栏（属报告 P3）。
- **验证**：`flutter analyze lib/views/home.dart lib/widgets/home_navigation_rail.dart` → No issues found。建议在桌面（≥600 宽）与手机（<600）双端实机回归导航与主题切换。
- **未提交 git**：等待用户审核。

## [2026-08-08 16:07] UI 桌面端适配 P0+P1（依据 docs/ui-adaptation-report-20260808.md，未提交）
- **范围**：仅 UI 渲染层，不触碰加密/会话/同步逻辑。
- **P0-1 网格自适应列数** `lib/views/home.dart` `_buildNotes()`：写死的 `crossAxisCount:2` 改为按网格真实可用宽度计算（`LayoutBuilder` + `max(2, (width/300).floor())`），手机保持 2 列、桌面随窗口变宽自动增列；用 `LayoutBuilder` 读受限宽度（body 已限宽 1300），避免直接用 `MediaQuery` 取到整屏宽度导致超宽屏列数过多。
- **P0-2 内容限宽居中** `lib/views/home.dart` `build()`：body 的 `Column` 外包 `Center` + `ConstrainedBox(maxWidth:1300)`，并设 `crossAxisAlignment.stretch`；大屏内容收束居中、手机（<1300）行为不变。
- **P1-4 可见滚动条** `lib/views/home.dart` `_buildNotesTile()`(ListView) 与 `_buildNotes()`(GridView)：外包 `Scrollbar`，桌面 `thumbVisibility:true` 常驻可见，移动端保持默认覆盖式。
- **P1-3 桌面跳过取向锁** `lib/main.dart` `_bootstrap()`：`SystemChrome.setPreferredOrientations` 外包 `isDesktopUi` 判断（Windows/macOS/Linux 且非 Web 时跳过），桌面窗口自由缩放；移动端行为不变。
- **桌面判定辅助**：`home.dart` 新增顶层 getter `_isDesktopUi`（`!kIsWeb && (isWindows||isLinux||isMacOS)`），Web 端短路不访问 `Platform`，安全 import `dart:io`。
- **验证**：`flutter analyze lib/main.dart lib/views/home.dart` → No issues found。未跑 `flutter test`（本次纯布局改动，且项目测试需完整环境；建议后续在桌面 + 手机双端实机回归）。
- **未提交 git**：等待用户审核。

## [2026-08-08 11:36] 补做跳过项 F-H01 + F-H04（损坏 manifest 的 bak 恢复，待用户审核，未提交）
- **依据**：`docs/manifest-reliability-design.md` §7.1/§7.2/§11.2。之前按约定跳过的两项，本次一并落地。
- **F-H04：bak 循环读侧落地** `sync_engine.dart` `_recoverFromCorruptRemoteManifest`（§7.1 步骤 2）。re-GET 仍失败时，`listManifestBackups()`（新到旧）逐份 `readManifestBackup` → `ManifestCrypto.deserialize` 一把过即命中（pubHash 预验 + GCM 解密，§7.2）。任何异常（含 `ManifestKeyMismatchException`，=旧 dataKey，非损坏）跳过试下一份（§11.2）；全部不可用才走本地重建（步骤 3），绝不误触发 scenario-b 强制重登。
- **F-H01：本地重建用空 etag PUT**。重建分支 `backupCorruptManifest` 已把损坏文件移走/删除（LocalFS rename / SafeServer move / WebDAV DELETE），端点已空——PUT 改用 `expectedEtag: ''`（If-None-Match / 首传语义）重建；re-GET/bak 成功路径仍用对应 etag。修复旧逻辑用旧 etag 在文件已不存在时报 412/Conflict。
- **接口变更**：`SyncBackend` 新增 `listManifestBackups()`（默认 `[]`）与 `readManifestBackup(name)`（默认 `null`）；LocalFS/WebDAV/SafeServer 三后端实现：按槽位从新到旧列名、读取密文。向后兼容：旧 Fake/后端默认空实现不破坏编译。
- **恢复来源追踪**：notes/journal/log 用 `recoveredSource`（reget/bak/local）区分三次路径。
- **测试**：`p0p1_self_heal_test.dart` 新增「bak 可用→恢复不丢远端数据」与「无 bak→本地重建空 etag 首传（F-H01）」两组回归；三个核心测试 Fake 与 App 侧 `change_password_multi_client_test.dart` Fake 补齐两方法。`readManifestBackup` 名字校验防路径穿越（正则白名单）。
- **验证**：`dart analyze packages/core` 与 `flutter analyze lib test` 均 0 issue；`dart test packages/core/test/sync` 241 passed（含混沌 3 seed）；根 `flutter test` 全绿。
- **未提交 git**：等待用户审核。

## [2026-08-08 11:36] - 按 review 报告修复 P0/P1（分块提交，全部测试通过）
- **依据**：[docs/flutter-code-review-final-20280808.md](file:///c:/Home/Projects/safenotes/docs/flutter-code-review-final-20280808.md) 的 P0（F-C01/F-C03）与 P1（F-H02~F-H18 部分）。按功能块分 6 次 git 提交，每次提交前跑对应测试。
- **P0 F-C03 + P1 F-H12（提交 f0a37e4）**：`lib/main.dart` `SafeNotesApp` 由 StatelessWidget 改 StatefulWidget。`sessionConfig.stream.listen` 从 build() 移入 State 管理：initState 订阅、dispose 取消订阅 + dispose 旧 config + 关闭 sessionStateStream；build() 每次重建前先取消旧 listener；超时配置（focusTimeout/inactivityTimeout）改为每次 build 动态读取，不再用构造期 final 快照，改设置即时生效。
- **P0 F-C01（提交 6b06dd9）**：`lib/models/biometric_auth.dart` 不再向 secure storage 写明文密码。新增随机 32B 包裹密钥 `_secureBiometricWrapKey`，密码以 AES-GCM 包裹态存储（`v1:` 前缀 + base64）；`authKey` getter 解包返回明文供 Keyring 解锁，解包失败返回空串走登录失败分支；旧版明文数据（无前缀）兼容原样返回、下次写入自动升级；`disable()` 移除"先写后删"（secure storage 下覆盖不比删除更彻底）；`enable()` 重新生成包裹密钥防开关周期复用。
- **P1 F-H09/F-H10/F-H11（提交 8e2a02c）**：`login.dart` 锁定倒计时顶层全局变量（`_lockoutTime/_counter/_timer/_controller/_startTimer`）移入 `EncryptionPhraseLoginPageState`，dispose 取消 Timer 并 close controller；`logout_alert.dart` `PreInactivityLogOff` 改 StatefulWidget，Timer/StreamController 生命周期归 State，移除 `_timer!` 非空断言。
- **P1 核心包 F-H02/F-H05/F-H07/F-H08（提交 b0e3664）**：
  - `sync_engine.dart` `_handleDownloadFailure` 自愈重传前校验 `local.contentHash == remoteItem.hash`（防旧内容覆盖远端 LWW 胜者，与 `_downloadNote`/`_repairRemote` 对齐）。
  - `sync()` 迁移重试与乐观锁冲突**分独立计数**（`conflictAttempts`/`migrationAttempts`），循环上界 `maxRetries*2` 兜底。
  - webdav/safe_server `putJournalObject` 非 2xx 抛异常，修复前异常被吞 + journal `_uploadedSeq` 无条件推进导致失败归档永不重传；现由 `syncToRemote` 外层 catch 兜底，水位只在真实成功时前进。
  - safe_server `deleteBlob` 对 405 静默降级（旧版服务端未实现 DELETE 端点），对齐 `server-api-spec §4`。
- **P1 F-H03/F-H06（提交 17da1bf）**：`SyncService.initialize` 幂等化（开头关闭旧 journal + 旧后端）；新增 `applyConfigToService()`，设置页改配置（后端类型/URL/凭据/关闭同步）后立即停用旧引擎或切换新后端，修复内存中旧 backend 继续被 autoSync 使用的问题。
- **P1 F-H18（提交 32dfc34）**：`.github/workflows/flutter-ci.yml` 增加 core 包 `dart pub get` + `dart analyze` + `dart test`（此前 CI 从不覆盖核心包回归）。
- **P1 F-H11 其余项（提交 3e67b99 / e6b412e）**：补齐 `set_passphrase.dart`（ScrollController+FocusNode）、`login.dart`（ScrollController）、`change_passphrase.dart`（3×TextEditingController+ScrollController）、`backup_passphrase.dart`（importPassphraseController）、`search_widget.dart`（controller）的 dispose；移除 `home.dart` 死代码 `isHiddenImport`/`importPassphraseController`；CI `java-version: '17.x'` → `'17'`。
- **明确跳过项**：F-H01/F-H04（损坏 manifest 的 `.bak` 恢复）——涉及数据安全与真实后端回归、改动大，按约定跳过；缺失编号的 P1 项按约定视为不重要跳过。
- **验证**：`dart analyze packages/core` / `flutter analyze lib` 均 0 issue；`dart test packages/core/test/sync` 239 tests passed（含混沌 3 seed）；`flutter test` 全绿。
