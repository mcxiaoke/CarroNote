# 变更日志

## [2026-07-31 13:10:38] - SafeServer v2.2 通用资源层

### 协议升级（docs/server-api-spec.md → v2.2）

- **新增通用资源层 `/api/v2/resources/<path>`**（§5.10）：用纯 REST/JSON 表达等价于 WebDAV `GET`/`PUT`/`DELETE`/`MOVE`/`MKCOL`/`COPY`/`PROPFIND` 的语义。
  - `GET` / `PUT` / `DELETE` 复用原生 HTTP 方法。
  - `move` / `copy` / `mkdir` / `propfind` / `stats` 通过 `POST /api/v2/resources/<path>` + JSON body `{op,...}` 触发。
  - 兼容任意 HTTP client（不使用 WebDAV 专有方法）。
- **消除"能力倒置"**：`safeServer` 软删除孤儿 blob 现可走 `move` 到 `blobs-orphan/`，与 `localFs`（`rename`）/`webdav`（`COPY`+`DELETE`）能力对齐，省去旧方案 `GET`+`PUT`+`DELETE` 三段往返与 `0rphan-` 前缀污染。
- **`manifest`/`blob` 端点保留为资源层便利接口**（§5.2–§5.9 语义不变），旧 v2.1 客户端继续兼容；旧服务端对资源层返回 405 时客户端静默降级。
- **新增路径安全红线 `ValidateVaultPath`**（§9.7）：禁止空路径/绝对路径/`..` 逃逸/NUL/反斜杠，规范化后校验最终路径停留在 vault 根内。
- 错误码补充：400（路径非法）、405（`mkdir` 目标已存在）、409（`move`/`copy` 冲突 / `mkdir` 父目录缺失）；`ResourceEntry` 字段定义（§5.10.4）。
- 版本历史追加 v2.2（§15）。

### 参考实现升级（Go + Node.js 同步）

- **Go**（`server/go/internal/storage/storage.go`、`fs.go`、`server/handlers.go`、`server/server.go`）：
  - `storage.go`：新增 `ErrInvalidPath`/`ErrExists`/`ErrConflict` 哨兵错误、`ResourceEntry` 结构、`ValidateVaultPath`、扩展 `Vault` 接口（7 个资源层方法）。
  - `fs.go`：`fsVault` 新增 `resolveResourcePath` 及资源层 7 方法实现；blob 的 Get/Put/Delete 委托到 `blobs/<hash>` 资源；`manifest` 路径特化回复用互斥锁实现。
  - `handlers.go`：新增 `handleGetResource`/`handlePutResource`/`handleDeleteResource`/`handleResourceOp`（move/mkdir/copy/propfind/stats）+ `mapResourceErr` 错误映射。
  - `server.go`：新增 `/api/v2/resources/` 路由分支（GET/PUT/DELETE/POST），启动日志改为 `SafeServer v2.2 (Go) starting`。
- **Node.js**（`server/nodejs/src/storage/storage.js`、`fs.js`、`src/handlers.js`、`server.js`）：
  - `storage.js`：新增 `ErrInvalidPath`/`ErrExists`/`ErrConflict`、`ResourceEntry` 类、`validateVaultPath`、`Vault` 基类 7 方法桩。
  - `fs.js`：`FsVault` 新增 `resolveResourcePath` 及资源层 7 方法；blob 委托到资源层；`manifest` 特化复用。
  - `handlers.js`：新增 `getResource`/`putResource`/`deleteResource`/`resourceOp` 及错误映射。
  - `server.js`：新增 `/api/v2/resources/` 路由分支，启动日志改为 `SafeServer v2.2 (Node.js) starting`。
- **实现文档**（`docs/server-implementation.md` → v2.2）：同步更新接口定义、目录结构、路由、存储布局、路径穿越防护、测试项、日志示例。

### 设计约束遵循

- 未超出 `localFs`/`webdav` 后端的能力上限（仅对齐，不超出），符合"三 backend 兼容、server 能力不超过 webdav/fs"的要求。
- 零知识不变：服务端不解析 blob 内容，仅操作路径与元数据。
- 未修改客户端 `safe_server_backend.dart`、未修改任何依赖文件（`pubspec.yaml`/`package.json` 未改动）。

## [2026-07-31 14:30:00] - 客户端 P0/P1 自愈增强 + 三 backend 资源层对齐

> 配套协议升级见上条 `SafeServer v2.2`（server 端已于独立提交落地）。本条目聚焦客户端 `lib/sync/*` 与测试。

### P0 修复（数据自愈 / 防误删）

- **P0-1 重建路径跳过 GC**：远端 manifest 为空（`remoteManifest == null`）或 header 解析 `FormatException`（损坏重建分支）时不调用末尾 `_gcOrphanBlobs`，避免误删其它设备的 blob。
- **P0-3 repairRemote blob 缺失兜底**：`repairRemote` 中 `blob == null` 时，若本机 `database.readNoteByUuid` / `readNoteByContentHash` 有明文（含孪生笔记），则 `_uploadNote` 重传补回并标记 `SyncActionType.heal`；无明文则记为 `failedNoteUuids`（绝不静默丢失）。
- **P0-4 普通下载 blob 缺失兜底**：下载路径 `envelope == null` 时，若本地有未删除且 `contentHash` 匹配的明文（含孪生查 `readNoteByContentHash`），则 `_uploadNote` 自愈补传并返回 `_DownloadHealed`；否则返回 `_DownloadFailed` 进入 `failedNoteUuids`。

### P1 修复（备份 / 隔离）

- **P1-1 manifest 代际环形备份**：`backupManifest([Uint8List?])` 接口落地——三后端一致把"即将被覆盖的旧 manifest 密文"写入后端侧 `manifest-backup/` 子目录的环形备份（`manifest.bak-0..4`，轮转槽位记 `.manifest-bak-index`）。LocalFS 落 vault 目录、SafeServer/WebDAV 落各自后端子目录，**统一不再写客户端临时目录**。
- **P1-2 GC 软删除隔离区**：`_gcOrphanBlobs` 改为 `deleteBlobSoft`（进 `blo-orphan/` 隔离区），循环结束调用 `purgeOrphans(_orphanRetention=30d)` 才真正物理删除，给一切误判留后悔药。
  - LocalFS：`rename` 到 `blobs-orphan/<hash>.<epochMs>`。
  - WebDAV：`COPY` + `DELETE` 到 `blobs-orphan/`。
  - SafeServer：v2.2 资源层 `move` 到 `blobs-orphan/`（`_resourceMove`），并保留旧服务端 `GET+PUT(0rphan-)+DELETE` 降级路径。

### 接口与抽象

- `SyncBackend` 新增带默认实现的接口方法：`deleteBlobSoft` / `listOrphanBlobs` / `purgeOrphans` / `backupManifest`（默认 no-op，子类按需覆盖）。
- 环形份数常量 `kManifestBackupRingCount = 5` 提到共享模块 `sync_backend.dart` 统一定义并导出，避免三后端重复定义。

### 测试

- 新增 `test/sync/p0p1_self_heal_test.dart`（10 项）：覆盖 P0-1 重建跳过 GC、P0-3 重传修复 / 无明文进 `failedNoteUuids`、P0-4 孪生明文自愈、P1-1 LocalFs 环形备份可反序列化、P1-2 孤儿隔离 / `purgeOrphans` 超期清理、以及混沌场景（运行中随机删/翻一个远端 blob，断言最终 heal 补回或进 `failedNoteUuids`）。
- 修复 3 个既有测试 `FakeBackend` 未实现新增接口方法导致的编译失败：`sync_engine_test.dart` / `multi_device_test.dart` / `change_password_multi_client_test.dart`。
- `safe_server_integration_test.dart` 新增"真实 server 上 `deleteBlobSoft` → `listOrphanBlobs` → `purgeOrphans`"链路断言（验证 v2.2 资源层 `move` 隔离与 `purgeOrphans` 物理清理）。

### 验证

- `flutter analyze lib/sync/` 零告警（含修复 `unintended_html_in_doc_comment` 与未使用 import）。
- 全量 `flutter test` 通过（含混沌 5 seed）；`safe_server_integration_test.dart` 对接真实 Go server 全绿。
