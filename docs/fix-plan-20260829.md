# SafeNotes 评审问题修复计划（2026-08-29）

* 依据：`docs/code-review-20260829.md` 及三份复核（verification / checkbyds / audit-msf）

* 定级方法：综合三份复核的源码验证结论，剔除误报与既有设计取舍，按"触发概率 × 数据/安全影响 × 改动成本"重排

## 关键裁定（与原报告的差异）

1. **P0-1 墓碑 GC 漏 purgedUuids —— 误报**。`hardDeleteByUuid`（database\_handler.dart:1640）在同一事务内调用 `_addPurgedUuidInTxn`，purged 登记已原子兑现，注释与代码一致。audit-msf 判定正确，其余两份复核未追进被调函数。不修。
2. **P0-2 读路径物理删坏行 —— 既有设计（M1 已拍板）**。删除坏行、不进 purgedUuids、靠云端同步与备份自愈是 M1 迭代中确认的取舍，隔离落盘被判定为过度设计。默认维持现状，仅做可选加固（C3）。
3. **P0-6 LWW 依赖本地时钟 —— 架构限制而非编码 bug**。HLC/版本向量属产品级长期改进，不在本计划修复批次内。

## 优先级列表

| 优先级 | 编号       | 问题                                                              | 复核共识                        | 改动量 | 状态    |
| --- | -------- | --------------------------------------------------------------- | --------------------------- | --- | ----- |
| A1  | P1-3     | DB Inspector 泄露 keyring（`sync_meta.value` 未隐藏，LAN `0.0.0.0` 可达） | 三方一致必修                      | 一行  | ✅ 已修复 |
| A2  | P0-3     | 迁移窗口 TOCTOU：`upsertNoteMeta` 等 8 处写路径零守卫，旧 key 密文 → 解不开被删       | checkbyds + audit-msf 必修    | 小   | ✅ 已修复 |
| A3  | P0-7     | 编辑页退出保存：`skipped` 误判为成功，用户文字静默丢失                                | checkbyds 第一优先；audit-msf 真实 | 中   | ✅ 已修复 |
| A4  | P1-11    | 响应体读取无超时 + sync 无总超时 → `_syncInProgress` 永久卡死                   | 两方必修                        | 小   | ✅ 已修复 |
| A5  | P1-14    | SafeServer move 非 204/404 即 DELETE 远端 manifest                  | 两方必修                        | 小   | ✅ 已修复 |
| A6  | P1-12    | URL 内嵌凭据明文进日志                                                   | 两方必修                        | 小   | ✅ 已修复 |
| A7  | P2-30    | Taskfile 引用不存在的 `generate_build_info.py`（CI 侧已修）                | 两方确凿                        | 一行  | ✅ 已修复 |
| B1  | P1-9     | base 不刷新 → 同步期间编辑产生"自冲突"副本增殖                                    | 高价值 / 降 P2                  | 中   | ⏸️ 搁置 |
| B2  | P1-8     | 下载/孪生路径 `synced_hash` 写 NULL → 下轮误判冲突                           | 必修 / 可自愈                    | 中   | ⏸️ 搁置 |
| B3  | P1-4     | meta 解密失败降级为空后被 NULL 覆盖，标签永久丢失                                  | 必修 / 中危                     | 小   | ⏸️ 搁置 |
| B4  | P0-4     | 迁移跳过解密失败行却报成功（`_readAllNotesStrict` 已先行拦截，剩单行损坏场景）              | 真实                          | 小   | ⏸️ 搁置 |
| B5  | P0-5     | 远端损坏重建丢弃他端条目（触发前提极严）                                            | 真实                          | 中   | ⏸️ 搁置 |
| B6  | P1-7     | SafeServer listBlobs 无 hash 白名单（三后端不一致）                         | 纵深防御                        | 小   | ✅ 已修复 |
| C1  | P1-1     | `unlockFromRemoteManifest` 切 key 零重加密（窄前提）                      | 加固项                         | 中   | 待修    |
| C2  | P1-2     | scenario-d 自动迁移无确认（防误改嫁）                                        | 属实                          | 小   | 待修    |
| C3  | P0-2     | 可选加固：删坏行前密文落盘 `temp/recovered-<uuid>.bin`                       | 尊重 M1 决策                    | 小   | 待修    |
| C4  | P1-16/19 | 同步闪骨架屏 + 滚动重置；密码框禁粘贴                                            | 真实 UX                       | 小-中 | 待修    |
| C5  | P1-5/6   | 幂等 ALTER + onDowngrade；WAL/busy\_timeout（P1-5 机理存疑，WAL 值得做）     | 真实                          | 小   | 待修    |
| C6  | P1-20 等  | i18n 补齐/注册 hi/bn/it、analysis\_options 收紧等工程化                    | 属实                          | 中   | 待修    |
| 不修  | —        | P0-1（误报）、P0-6（架构限制）、P1-5 降级崩溃链（接近不可达）                           | —                           | —   | 不修    |

## 第一批（A1–A7）—— ✅ 已完成（2026-08-29）

1. ✅ **A1**：`_inspectorHiddenColumns` 加 `'value'`（database\_handler.dart:58）
2. ✅ **A2**：给 `upsertNoteMeta` / `softDelete` / `hardDelete` / `hardDeleteByUuid` / `restoreNote` / `markSynced*` 补 `_checkNotMigrating` 守卫（与既有 15 处守卫口径一致；串行锁作为后续根治方案另行评估）；新增 `migratingForTesting` 测试钩子
3. ✅ **A3**：`addOrUpdateNote` 返回 `({saved, skipped})` + `waitForSave()`/`isSaving`；`_performAutoSave` 在 `skipped` 时 await 在途保存后用最新输入重试一次，重试仍被跳过则视为失败留在页面（H4 路径）
4. ✅ **A4**：`http_util.dart` body 读取循环包 `Stream.timeout`；`sync_service.dart` 给 `engine.sync()` 加总超时（实际取 10 分钟，兼顾大库慢网）并超时复位 `_syncInProgress`
5. ✅ **A5**：`backupCorruptManifest` 只认 204/404，其余抛 `BackendUnavailableException`，绝不删 manifest
6. ✅ **A6**：`SyncBackendDraft.normalized()` 剥离 URL userinfo 填入空缺的用户名/密码字段（显式字段优先）；`setWebdavUrl`/`setSafeServerUrl` 日志统一走 `_redactUrlForLog`；WebDAV `_mkcol` 异常 message 只拼 `scheme://host:port` 且响应体截断 200 字符
7. ✅ **A7**：Taskfile 改 `dart run scripts/generate_build_info.dart`

验证结果（2026-08-29）：

* `flutter analyze lib test packages/core`：No issues found

* `dart test packages/core/test`：506 通过（含新增 P1-3 / P0-3 回归 2 条）

* `flutter test`：246 通过（含新增 P0-7 / P1-12 回归 5 条）

* 变更摘要已记入 `docs/CHANGES-20260829.md`（顶部「评审第一批修复」）；未 commit，等用户确认

## 第二批（B1–B6）—— ✅ 已核实并处置（2026-08-29）

> 按用户要求逐项回源码核实严重性：**不严重且改动影响大的搁置**（同步核心逻辑改动易引入新问题）。结论：B6 修复，B1–B5 搁置。

| 项         | 处置    | 核实结论（源码依据）                                                                                                                                                                                                                                                                 |
| --------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1 (P1-9) | ⏸️ 搁置 | 后果仅"自己的上一版被存成冲突副本"（数据污染非丢失），`note_versions` 有历史兜底。修复需改 converged 白名单 + 上传成功追踪，动冲突判定核心——做错方向（虚假 fast-forward）会真丢数据，风险 ≫ 收益                                                                                                                                                  |
| B2 (P1-8) | ⏸️ 搁置 | **核实为可自愈**：下载后内容 == merged → converged → `markSyncedForUuids` 刷新 base；仅 PUT 失败才留 NULL base，下轮 local/remote hash 相等 → 不产副本（`shouldPreserveCopy` 要求 hash 不同）、LWW 时间戳相同 → 一轮收敛。改下载路径整行覆盖语义风险大收益趋零                                                                             |
| B3 (P1-4) | ⏸️ 搁置 | 行一旦解密失败，密文本身已不可恢复（正确 key 也解不开），覆盖与否不改数据命运；主要触发源（迁移窗口旧 key payload）已被 A2 守卫堵住，残余仅 bit rot。修复需给 NoteMeta 加降级标志并贯穿同步语义                                                                                                                                                        |
| B4 (P0-4) | ⏸️ 搁置 | **评审建议的"失败即中止"经核实为错误方案**：`_readAllNotesStrict` 先行严格解密——oldKey 错时迁移早中止走不到 meta 跳过；能跳过的只剩"notes 全可解但单行独立损坏"，该行迁移前就已不可恢复，跳过不丢任何可救数据；反而"中止"会让用户因一条已损坏行完全无法改密码                                                                                                                  |
| B5 (P0-5) | ⏸️ 搁置 | 前提 = manifest 损坏 + re-GET 失败 + 全部 bak 不可解（远端已彻底不可恢复）。他端独有条目在拥有设备下次同步经"仅本地有"分支重新上传（不在 purgedSet）；blob 有两阶段候选 + 30 天 purge 保留窗口。永久丢失需拥有设备 30+ 天不同步                                                                                                                           |
| B6 (P1-7) | ✅ 已修复 | `../manifest` 注入路径核实成立。修复不动同步逻辑：① SafeServer `listBlobs` 加 `^[a-f0-9]{64}$` 过滤（与 webdav/localfs 对齐）；② `listJournalObjects` 名字白名单（`[A-Za-z0-9._-]` + 无 `..`）；③ `_gcOrphanBlobs` 引擎侧统一兜底过滤（防后端实现不一致）。回归测试 `MaliciousListBackend` 验证恶意条目不触发 `deleteBlobSoft` 且不污染候选表、合法孤儿照常隔离 |

验证结果（2026-08-29）：`flutter analyze` 无 issue；`dart test packages/core/test` 507 通过（+1）；`flutter test` 246 通过。集成测试 `safe_server_integration_test` 的 listBlobs 用例同步改为合法 64-hex blob 名。

## 第三批（C1–C6，加固与工程化）

* C1 切换 dataKey 前校验本地库可解性；C2 迁移二次确认 + vaultId 校验；C3 坏行密文落盘；C4/C5 UX 与数据库配置；C6 i18n / lint / 依赖治理

