# 2026-08-15 变更记录

## 侧栏 / 抽屉底部布局微调（2026-08-15 10:15 GMT+8）

用户反馈：
1. Windows 侧栏版本号置底没问题，但 Lock 应紧跟在设置之下（不置底），且不要分割线。
2. Android 抽屉版本号没有置底；Lock 应与其他项目一致，不要分割线。

改动：
- `lib/widgets/home_navigation_rail.dart`：Lock 移入可滚动 ListView、紧跟在 Settings 之下（`sideItem`），删除 Lock 上方的分割线；版本号 footer 保持置底。
- `lib/widgets/drawer.dart`：抽屉改为 `Column = Expanded(SingleChildScrollView(导航项)) + footer`，版本号 footer 置底不随内容滚动；删除 Lock 上方的 `_divide`（Lock 与其他项目间距一致），移除不再使用的 `dividerSpacing` 变量。

验证：`flutter analyze` 无问题；`flutter test` 35 项全过；`flutter build windows --debug` 成功。

## 设置项与侧栏 IA 二次修订（2026-08-15 09:57 GMT+8）

用户对首轮 IA 重构提出 5 点修正：

1. **Change Passphrase 移入安全组**：删除账户 Account 组，`Change Passphrase` 并入安全 Security 组末尾（`lib/views/settings/settings.dart`）。
2. **删除关于分组**：设置页不再有独立 About 组，About 作为通用 General 组末尾入口（进入 About 页）。
3. **设置页底部版本信息移到 drawer / 侧栏**：删除设置页底部 `_VersionFooter`（版本号 + DEBUG 徽标连点逻辑一并移除），改为在 `drawer.dart` / `home_navigation_rail.dart` 底部复用 `widgets/footer.dart` 的 `footer()`（分多行小字 + DEBUG 徽标）。
4. **Dark mode 命名回归 + 弹层去重**：设置页「Switch Theme」改回「Dark mode」（`LucideIcons.moon`）；`theme_setting.dart` 底部弹层移除重复的 Theme Color 项（设置页已有显式「Theme color」入口），并清理 `currentSeed` / `AppThemeSeeds` 相关代码与 import。
5. **开发者模式入口移到 About 页**：`about_page.dart` 大 LOGO 改为 `_DevModeTapLogo`（StatefulWidget，连点 5 次开启 dev 模式，行为与旧版连点一致），删除设置页 `_VersionFooter` 的连点逻辑。

其他：settings.dart 移除 `core` / `build_info.g.dart` / `dev_mode.dart` / `footer.dart` import；theme_setting.dart 移除 `theme_seeds.g.dart` import。

文档同步：`docs/settings-sidebar-ia-design-20260815.md` §0/§3.1/§3.3/§6/§7 更新为 4 组 IA 与验收标准。

验证：`flutter analyze` 无问题；`flutter test` 35 项全过；`flutter build windows --debug` 成功。

## 设置项与侧栏信息架构重构（方案 A）（2026-08-15 09:40 GMT+8）

依据 `docs/settings-sidebar-ia-design-20260815.md` 方案 A 实施：设置页重排为 6 组、侧栏只保留「位置导航」。

改动：
- `lib/views/settings/settings.dart`：设置项按「使用频率 × 重要性」重排为 6 组 —— 外观 Appearance（Switch Theme / Theme Color / Notes Color / Compact Notes / Markdown / Relative Time / Sort by Modified）、数据 Data（Sync Settings / Backup / Export Backup / Import Backup）、安全 Security（Biometric / Logout on Inactivity / Secure Display / Incognito Keyboard）、账户 Account（Change Passphrase）、通用 General（Language / Auto Rotate）、关于 About（入口 → About 页）。
  - Theme Color 在设置页显性列出（复用 `/themeColorSettings`），与 Switch Theme 弹层快捷入口并存；tile 右侧显示当前主题色名（复用 `AppThemeSeeds.itemByIndex`）。
  - Auto Rotate 加平台判断，仅移动端显示（`!isDesktopPlatform`）。
  - Lock 从设置页移除（只留侧栏底部）；Source Code / Open Source License 迁至 About 页。
  - 移除 `_launch` 与 `url_launcher` / `session` import。
- `lib/widgets/drawer.dart`（移动端抽屉）：item 收敛为 Notes（新增，返回主页）/ Recently Deleted / Settings / Lock；移除 Switch Theme / Sync Settings / About；移除对应回调字段与 theme_setting import。
- `lib/widgets/home_navigation_rail.dart`（桌面侧栏）：item 收敛为 Recently Deleted / Settings / Lock；移除 Switch Theme / Sync Settings / About 及相关回调。
- `lib/views/home.dart`：`HomeSidebar` / `HomeDrawer` 构造移除 `onSyncSettingsCallback` / `onAboutCallback` / `onThemeCallback`；抽屉新增 `onNotesCallback`（pop 抽屉回主页）；删除 `_navTheme` / `_navSyncSettings` / `_navAbout`；移除 theme_setting import。
- `lib/views/settings/about_page.dart`：补充源码 Source Code / 开源许可 Open Source license / 帮助反馈 Help and Feedback 链接（`shadSettingsCard` + `shadNavigationTile`，复用 `SafeNotesConfig.sourceCodeUrl` / `openSourceLicense` / `mailToForFeedback`）。
- `assets/translations/zh-CN.json` / `en-US.json`：新增 `Appearance` / `Data` / `Account` / `Notes` 键。

文档修正（实施中发现原文档 §3.1 表格 About 组「项」列与 §4.2/§6 矛盾）：About 组在设置主页只留一个「About」入口，源码 / 开源许可 / 反馈链接均放在 About 页内；已修正 `docs/settings-sidebar-ia-design-20260815.md` 相应行。

验证：`flutter analyze` 无问题；`flutter test` 35 项全过；`dart test packages/core/test` 294 项全过；`flutter build windows --debug` 成功。
