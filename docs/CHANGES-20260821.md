# CHANGES-20260821

## 首页视觉柔和化（对照 Google Keep，改动 1/2/3，第 4 项暂缓）

> 时间：2026-08-21 09:5x (GMT+8)
> 范围：仅首页/中性主题观感调整；第 4 项（monochrome→neutral 全局 seed 衍生色）按用户要求暂缓，未动。

### 改动清单
1. **标题字重 bold → w500**（去"粗"）
   - `lib/widgets/note_card_body.dart`：`_buildFull` 与 `_buildCompact` 两处卡片标题
     `FontWeight.bold` → `FontWeight.w500`（正常模式 + 紧凑模式一致）。
2. **页面背景改带色相浅灰**（拉大卡片/背景对比，让卡片"浮起"）
   - `lib/models/app_theme.dart`：`scaffoldBackgroundColor` 对中性主题固定为
     `Color(0xFFEEEDF3)`（极淡蓝紫灰，Keep 风格），彩色主题仍用 `surfaceContainerLow`
     保留品牌色相；不动 seed 衍生色（surface/onSurface/outline/primary 均不变）。
3. **卡片边框降级**（减轻"重边框"）
   - `lib/widgets/note_card_body.dart`：卡片描边色 `cardScheme.outline` →
     `cardScheme.outlineVariant`（更浅），宽度保持 1。仅中性+非彩色模式生效。

### 备份
- `temp/backups/note_card_body.dart.20260821_095541.bak`
- `temp/backups/app_theme.dart.20260821_095541.bak`

### 验证
- `flutter analyze lib/widgets/note_card_body.dart lib/models/app_theme.dart` → No issues found.
- 未提交、未推送（遵守红线）。需用户在应用中运行查看实际观感。

### 待确认 / 风险
- 改动 2 仅对中性主题生效；彩色主题背景不变。若希望全局统一浅灰需另议。
- 改动 3 后卡片与背景对比增大，边框进一步显得次要，若仍偏重可改为 `width: 0.5` 或去边框。
- 第 4 项（seed 衍生色带色相）暂缓：涉及 primary/secondary/outline/surface 等全站色，
  影响 drawer、设置、PIN、AppBar 等几乎所有组件，待单独评估后实施。
